| Finding                                             | Verified Sev | Fix status | Note                                                                                                                                                                                                                          |
| --------------------------------------------------- | -----------: | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 — Tombstone not pending runtime reload           |     CRITICAL | FIXED\_OK  | Disable now retains the tombstone revision as `pending_restart`; empty-set identity is real, tombstones reconcile only after instance convergence, and UI refreshes runtime status.                                           |
| F2 — Role reconciliation fails open                 |         HIGH | NOT\_FIXED | The helper now throws, but Better Auth account/session hooks still catch and suppress every reconciliation failure after session creation. The test only exercises the helper, not authentication denial.                     |
| F3 — Invalid provider discards tombstones           |       MEDIUM | FIXED\_OK  | Validated tombstone IDs survive live-provider materialization failure and filter the pre-existing LKG, preventing the observed tombstone from being resurrected.                                                              |
| F4 — Edited providers cannot be revoked             |         HIGH | REGRESSION | Edited drafts can now revoke, but every draft is treated as disableable. Never-published drafts receive a backend-rejected Disable action, with no published-history discriminator or delete path.                            |
| F5 — Client secret stored as audit reason           |       MEDIUM | PARTIAL    | Replacement secrets are sanitized, but failure to resolve the current secret is ignored. A clear/update can therefore persist an opaque current secret in success or failure audit reasons; requested regressions are absent. |
| F6 — Auth settings lack CAS                         |       MEDIUM | REGRESSION | Backend CAS is implemented, but `GeneralSettingsPage` never sends required `expectedRevision`, producing a TypeScript/API-contract failure and preventing saves.                                                              |
| F7 — Empty enabled allowlist is unrestricted        |       MEDIUM | FIXED\_OK  | Shared validation, model validation, and a database check constraint reject enabled empty allowlists; migration normalizes existing invalid rows.                                                                             |
| F8 — Page-2 wizard uses stale revision              |       MEDIUM | PARTIAL    | Mutation responses are retained as the canonical provider, fixing the stale revision path, but no regression exercises save followed by test/publish for a provider outside page 1.                                           |
| F9 — Pending role mappings grow unbounded           |       MEDIUM | PARTIAL    | A hard cap makes growth bounded, but expiry remains activity-triggered, `discardIdentityProviderGroupRoleMapping` has no production caller, and the test triggers cleanup through the inspection helper itself.               |
| F10 — Tombstone outage test misses immediate outage |       MEDIUM | NOT\_FIXED | The revised test covers a reachable database with a co-provider missing its secret, not an actually unavailable database immediately after disable. The dangerous pre-tombstone-LKG outage remains untested.                  |
| F11 — PostgreSQL secret CAS test not scheduled      |       MEDIUM | REGRESSION | The secret CAS test is still absent from the workflow and scenario manifest. Additionally, the verifier now requires `--reports-dir`, but the workflow invocation omits it and will fail closed.                              |
| F12 — Missing admin locale entries                  |          LOW | FIXED\_OK  | All eight keys now exist in the source, en-US, and zh-CN catalogs. Remaining inline fallbacks are redundant but do not prevent localization.                                                                                  |
| F13 — Stale policy-step comment                     |          LOW | FIXED\_OK  | The comment now accurately states that login-time enforcement exists and that only the editor surface is deferred.                                                                                                            |

VERDICT: needs-rework

- **F2:** Move reconciliation to a fail-propagating pre-session boundary, remove the swallowing catches, and prove a failed demotion cannot create or retain an authenticated session.
- **F4:** Expose published-history state in the provider projection and show Disable only for drafts with published history; route never-published drafts to Delete and add the requested page regression.
- **F5:** Fail closed or redact the entire reason when the current secret cannot be resolved, and add success and failure audit tests using opaque replacement/current secrets.
- **F6:** Retain the fetched revision in the settings editor, send it as `expectedRevision`, and handle revision conflicts with an explicit refresh/discard flow.
- **F8:** Add a modal-level regression proving a page-2 provider uses each mutation response revision for the immediately following test or publish.
- **F9:** Wire discard into every terminal login-failure path and test expiry/capacity without invoking a helper that performs the sweep being asserted.
- **F10:** Add a genuinely failing-database startup immediately after disable, without a healthy post-disable load, and make the tombstone durable outside that unavailable read path so old LKG cannot resurrect it.
- **F11:** Register `secretStore.pgConcurrency.test.ts` in the workflow, command manifest, and evidence scenario, and pass the raw reports directory to the updated verifier.

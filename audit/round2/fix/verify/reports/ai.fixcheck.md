| Finding | Verified Sev | Fix status | Note                                                                                                                                                                                                                                                    |
| ------- | -----------: | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1      |         HIGH | PARTIAL    | Server and UI correctly prohibit hard-deleting `revision > 0` providers, but the new test only checks row preservation; it never exercises runtime resolution/BYOK behavior after the delete attempt.                                                   |
| F2      |       MEDIUM | PARTIAL    | CAS finalization and authoritative audit are transactional, but a superseded attempt observed while the newer attempt is pending throws inside the outer `catch`, which incorrectly appends a failure audit. Tests do not assert audit consistency.     |
| F3      |       MEDIUM | FIXED\_OK  | Batch paths now lock/load once and perform one dependency scan, eliminating repeated full-draft reloads and quadratic behavior. Remaining per-row writes and audits are linear.                                                                         |
| F4      |       MEDIUM | PARTIAL    | Sleeps were replaced, but `waitForUngrantedLock` counts every ungranted lock in the database rather than the contender’s lock, allowing unrelated activity to satisfy the assertion. Cleanup also still deletes from append-only audit/revision tables. |
| F5      |          LOW | FIXED\_OK  | The unused recursive `jsonContainsModelReference` helper was removed.                                                                                                                                                                                   |
| F6      |          LOW | FIXED\_OK  | Retry rejection is caught, surfaced through a localized toast, and no longer escapes the discarded click-handler promise.                                                                                                                               |

VERDICT: needs-rework

- **F1:** Extend the regression test through archive/delete rejection and `AiCatalogExecutionResolver`, asserting the managed provider remains fail-closed rather than producing `PLATFORM_NOT_FOUND`.
- **F2:** Treat supersession as a distinct non-audited outcome outside the generic failure path, and assert that discarded attempts create no audit entry.
- **F4:** Identify the specific contender backend/transaction when querying PostgreSQL lock state, and use `TRUNCATE`-based cleanup for append-only audit and revision tables.

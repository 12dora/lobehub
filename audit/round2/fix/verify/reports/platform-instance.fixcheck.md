| Finding | Verified Sev | Fix status | Note                                                                                                                                                                                                                                                  |
| ------- | ------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1      | MEDIUM       | FIXED\_OK  | Adds persisted revision, required `expectedRevision`, atomic CAS update/increment, conflict mapping, and stale-writer regression coverage.                                                                                                            |
| F2      | MEDIUM       | PARTIAL    | Owner-bound staged rotation is implemented atomically, but direct expired-stage, audit-rollback, and concurrent-rotation tests required by the fix are absent.                                                                                        |
| F3      | MEDIUM       | NOT\_FIXED | Migration 0145 still creates all four large indexes without `CONCURRENTLY`, including the GIN index inside its `DO` block.                                                                                                                            |
| F4      | MEDIUM       | PARTIAL    | Polling no longer loads full catalog payloads, but both token projections still scan and hash every catalog pointer on each poll; no bounded or incrementally maintained aggregate token exists.                                                      |
| F5      | MEDIUM       | FIXED\_OK  | The committed mutation response is now authoritative when refreshed pages omit the job, with a pagination-shift regression test.                                                                                                                      |
| F6      | MEDIUM       | REGRESSION | Removing the user grouping bounds the query, but emitted records now have blank user IDs while the enabled `GroupBy.User` UI derives its series solely from these records, leaving that view empty; model/provider cardinality also remains uncapped. |
| F7      | LOW          | PARTIAL    | The timer was removed, but the replacement is sequential and no longer exercises concurrent lock/CAS behavior.                                                                                                                                        |
| F8      | LOW          | NOT\_FIXED | The migration test remains unchanged: it replays only 0138, while 0145 is still checked textually and never applied to a legacy fixture.                                                                                                              |

VERDICT: needs-rework

- F2 — Add direct expired-stage, audit-failure rollback, and concurrent same-revision file-rotation tests that verify the old secret and staged row remain correct on failure.
- F3 — Move the high-volume indexes into a non-transactional migration using `CREATE INDEX CONCURRENTLY IF NOT EXISTS`, with `pg_trgm` capability handled beforehand.
- F4 — Maintain an aggregate catalog token incrementally, or otherwise bound the pointer scan so three-second polling is not linear in catalog size.
- F6 — Provide a capped top-N user projection with an “other” bucket and wire it to `GroupBy.User`; also explicitly cap model/provider cardinality.
- F7 — Add a deterministic real-PostgreSQL or instrumented two-transaction test proving concurrent writers with the same revision cannot both commit.
- F8 — Replay 0145 twice against a representative legacy/partial PostgreSQL fixture and assert data transformation, constraints, indexes, triggers, and retained rows.

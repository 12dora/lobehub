| Finding | Verified Sev | Fix status | Note                                                                                                                                                                                                                                                                                                                   |
| ------- | ------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1      | HIGH         | FIXED\_OK  | Freshness now accepts monotonic advancement of either `revision` or `draftSequence`, requires a changed token, and rejects rollback/equal CAS.                                                                                                                                                                         |
| F2      | HIGH         | FIXED\_OK  | Versions are canonically sorted by `createdAt` descending with an ID tie-break; explicit current/latest helpers and shuffled-order tests cover editor and publish selection.                                                                                                                                           |
| F3      | LOW          | N/A        | Intentionally skipped per verified scope: fail-closed filtering while the feature is disabled would contradict the layer’s documented flag-off behavior and hide owner data.                                                                                                                                           |
| F4      | MEDIUM       | PARTIAL    | CAS counters now better match production, but `draftTokenFromIdentity` truncates the hex-encoded identity to its first 64 characters. For long IDs, `draftSequence` lies beyond that prefix, so assignment mutations can leave the token unchanged; the test compares against a pre-publication token and misses this. |
| F5      | MEDIUM       | PARTIAL    | The resolver expands beyond 5,000 rows, but stops at another arbitrary 50,000-row ceiling and repeatedly refetches growing prefixes. Entitled agents beyond 50,000 hidden/duplicate rows remain omitted; tests cover only 1,200 hidden rows and six duplicates.                                                        |
| F6      | MEDIUM       | FIXED\_OK  | Prerelease/build metadata is removed under an explicit patch-bump policy, preventing `NaN`; representative SemVer cases are tested.                                                                                                                                                                                    |
| F7      | LOW          | FIXED\_OK  | Disabled and mandatory warnings are generated independently; server tests cover mandatory and combined warnings, and the mock is aligned.                                                                                                                                                                              |
| F8      | LOW          | FIXED\_OK  | The unused list-key helper and its misleading assertions were removed; the live cursor-aware key remains tested through the pagination hook.                                                                                                                                                                           |

VERDICT: needs-rework

- F4 — Replace the truncating token generator with a deterministic full-identity digest, assert the token immediately before and after draft-only assignments, and cover equal-revision assignment refresh through the real lock chain.
- F5 — Implement stable cursor/keyset paging until 1,000 visible unique winners or actual source exhaustion, without an arbitrary scan ceiling or repeated-prefix queries, and add regressions beyond 5,000 and 50,000 leading rows.

| Finding | Verified Sev        | Fix status | Note                                                                                                                                                                                                                                                  |
| ------- | ------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1      | HIGH                | FIXED\_OK  | Secret errors are rejected before persistence; regression verifies no version row or draft-sequence change.                                                                                                                                           |
| F2      | HIGH (availability) | PARTIAL    | Canonicalization recomputes metadata and validation checks size, but never verifies `resource.checksum` against content. The requested CRLF/NFD publish-to-resolution test is absent, and corrupt overrides can silently fall back to bundled skills. |
| F3      | HIGH                | FIXED\_OK  | Archived override tombstones force `enabled:true`; the disabled-draft regression covers catalog resolution.                                                                                                                                           |
| F4      | MEDIUM              | PARTIAL    | Fresh validation is persisted, but its transaction commits before the success audit instead of sharing one audited transaction. The differing-timestamp hook regression is also absent.                                                               |
| F5      | MEDIUM              | FIXED\_OK  | Update failures now return partial success with the committed draft; the test asserts both response and persisted rename.                                                                                                                             |
| F6      | MEDIUM              | REGRESSION | Archiving writes revision 1 without a current version or revision record. Runtime authority processes every positive revision and throws on this invalid pointer, potentially disabling the catalog; the archived shell also remains mutable.         |
| F7      | MEDIUM              | REGRESSION | Runtime fetch classification is fixed, but the new test ZIP uses highly compressible repeated text with default compression, so it remains below 1 MiB and fails its size assertion.                                                                  |
| F8      | LOW                 | FIXED\_OK  | The unsupported built-in source control was removed from the admin list.                                                                                                                                                                              |
| F9      | LOW                 | FIXED\_OK  | The empty test artifact was deleted.                                                                                                                                                                                                                  |

VERDICT: needs-rework

- F2 — Validate both UTF-8 size and SHA-256 checksum independently, add the requested CRLF/NFD publish-to-resolution regression, and prevent corrupt overrides from failing open to bundled skills.
- F4 — Persist validation and append its success audit in one transaction, then add a hook regression using distinct old and new timestamps.
- F6 — Replace the positive-revision pointerless shell with an atomic cancellation/deletion or a properly excluded revision-zero archived state, enforce terminal mutation rules, and verify catalog resolution afterward.
- F7 — Build an actually larger-than-1-MiB ZIP, such as with `zipSync(..., { level: 0 })` or incompressible bytes.

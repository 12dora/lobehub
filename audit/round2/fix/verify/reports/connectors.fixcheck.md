| Finding | Verified Sev | Fix status | Note                                                                                                                                                                                                                                                                              |
| ------- | ------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1      | HIGH         | FIXED\_OK  | Authoritative read failures now unconditionally return deny-all; same-epoch LKG restoration is removed and regression-tested.                                                                                                                                                     |
| F2      | MEDIUM       | PARTIAL    | Bounded reconciliation and fail-closed readiness were added, but no serverless cron/queue/HTTP entrypoint invokes the batch; an environment flag alone can falsely claim reconciliation exists.                                                                                   |
| F3      | MEDIUM       | FIXED\_OK  | Cache refreshes use `Promise.allSettled`; successful mutations retain success state, and save/delete rejection regressions cover the corrected behavior.                                                                                                                          |
| F4      | MEDIUM       | PARTIAL    | Publish now checks revision/token-bound success, but that state remains process-local. Test and publish requests routed to different serverless/multi-instance processes cannot satisfy the invariant; tests directly seed the map instead of exercising a durable or live probe. |
| F5      | MEDIUM       | FIXED\_OK  | Flag-off disconnect now calls the real revocation service; router coverage verifies routing, while existing service tests cover binding/token detachment.                                                                                                                         |
| F6      | LOW          | FIXED\_OK  | Successful deletion clears connection-test state after transaction commit; failed CAS deletion preserves it, with regression coverage.                                                                                                                                            |
| F7      | LOW          | FIXED\_OK  | The unused deprecated governance alias and comment were removed.                                                                                                                                                                                                                  |

VERDICT: needs-rework

- F2: Add an authenticated durable serverless cron/queue entrypoint that invokes `runConnectorRuntimeAuditBatch`, and make readiness verify that concrete integration rather than trusting a standalone opt-in flag.
- F4: Perform the live connection probe inside publish or persist test results transactionally by connector revision and draft token; replace direct in-memory seeding tests with real test-to-publish coverage.

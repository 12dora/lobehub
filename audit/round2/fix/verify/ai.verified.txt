  F1 (codex said CRITICAL; VERIFIED HIGH): Hard-deleting a published provider re-enables BYOK fallback
     scope-correction: Finding's code mechanism, cited lines, and impact chain are all accurate. Only the CRITICAL label is slightly overstated: the BYOK bypass yields real ungoverned model access only when the user independently possesses their own credentials for that provider key (e.g. a residual/self-built AiProvider row previously shadowed by managed AI), and the hard-delete does not disclose the platform's governed secrets or destroy audit/revision evidence. That precondition and the absence of secret/evidence compromise place it at HIGH rather than unconditional CRITICAL.
| Finding | Verified Sev | Fix status | Note                                                                                                                                                                                                                                                            |
| ------- | -----------: | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1      |     CRITICAL | PARTIAL    | Publication is transactional, but the claimed concurrency test merely fails after enqueue and calls `claimNext` after rollback; it never blocks the required audit append while another connection attempts a claim.                                            |
| F2      |         HIGH | FIXED\_OK  | `users.timeline` now requires conversation-read permission and rejects disabled content access before querying.                                                                                                                                                 |
| F3      |         HIGH | FIXED\_OK  | Conversation and timeline exports are gated at creation, worker execution, and download; revoked queued exports fail terminally while retaining metadata.                                                                                                       |
| F4      |         HIGH | FIXED\_OK  | `rankTopics` now requires both stats and conversation-read permissions, with positive and negative authorization tests.                                                                                                                                         |
| F5      |       MEDIUM | FIXED\_OK  | Cancel state, job state, and required audit writes are transactional; retention success completion also couples run, job, and outcome audit atomically.                                                                                                         |
| F6      |         HIGH | REGRESSION | Terminal paths gained purge outboxes, but `claimNext` can now dead-letter a final-attempt lease-expired worker without invoking export cleanup; a crash after upload can therefore leave a running export, dead job, and untracked object permanently orphaned. |
| F7      |       MEDIUM | FIXED\_OK  | Deletions are processed individually with lease renewal, and outbox completion plus run-count attribution commit in the same hold-locked transaction.                                                                                                           |
| F8      |       MEDIUM | FIXED\_OK  | Page-budget exhaustion now returns an explicit error instead of a silently truncated full-month result, with boundary coverage.                                                                                                                                 |
| F9      |       MEDIUM | FIXED\_OK  | Feed and pagination failures now render localized retries, pagination promises are caught, and refresh timestamps advance only after all active feeds succeed.                                                                                                  |
| F10     |       MEDIUM | PARTIAL    | A 256 MiB artifact cap was added, but upload, post-upload checksum verification, and download verification still materialize the complete artifact in memory.                                                                                                   |
| F11     |       MEDIUM | FIXED\_OK  | Retention batches now use targeted scope queries and bounded hold-class summaries instead of loading every active hold.                                                                                                                                         |
| F12     |       MEDIUM | PARTIAL    | Timeline and `rankTopics` authorization regressions were added; disabled-export lifecycle, real concurrent publication, cleanup retry, and slow-delete two-worker cases remain absent.                                                                          |

VERDICT: needs-rework

- **F1:** Add a real two-connection regression that blocks the required audit append inside the publication transaction and attempts `claimNext` before that transaction resolves.
- **F6:** Persist cleanup intent as soon as an uploaded object may exist and reconcile lease-expiry dead-lettering with domain failure/outbox creation, including a delete-fails-once regression.
- **F10:** Replace buffer-based upload and checksum reads with bounded streaming or multipart I/O for worker upload, verification, and download validation.
- **F12:** Add the missing disabled-policy create/execute/download, concurrent publication, cleanup-retry, and slow-storage two-worker regressions.

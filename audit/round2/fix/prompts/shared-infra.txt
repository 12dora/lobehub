You are a senior TypeScript engineer fixing VERIFIED bugs in the "AIHub" enterprise secondary-development (二开) layer (built on LobeHub, Next.js 16 + React 19 + Drizzle + tRPC).

CONTEXT TO READ FIRST:
- audit/round2/partitions/shared-infra.md  — the full findings for your partition (evidence, failure scenario, suggested Fix per finding).
- audit/round2/fix/verify/shared-infra.verified.txt — the VERIFIED severity + scope corrections from an independent cross-model review. TRUST THIS for scope: some findings codex marked HIGH/CRITICAL were verified down to MEDIUM/LOW with a precise reason. Fix the REAL defect at the RIGHT scope — do not over-engineer a MEDIUM into a huge refactor, and do implement the CRITICAL/HIGH fixes properly.

YOUR JOB: Fix EVERY finding in shared-infra.md that is a real defect, dead code, or rotten test:
- Functional bug (⑤): implement the Fix described (use your judgment; the report's "Fix:" is guidance, not gospel). Match surrounding code style, idioms, and error/CAS/transaction patterns already used in these files.
- Dead code (③): delete it (and its now-orphaned imports/exports/tests).
- Rotten test (②): FIX it to assert correct behavior, or DELETE it if it has no value, exactly as the finding says. When a finding says a test "locks in the bug", the test MUST be updated to assert the NEW correct behavior you just implemented.
- Missing regression (② ADD): add a focused test ONLY if it is quick and the test file/harness already exists nearby; otherwise skip and note it.
- Skip pure "split this large file into modules" refactors (dimension ① size-only) — note them as deferred; do not do large mechanical refactors.

STRICT RULES:
- Edit ONLY files under these OWNED PATHS:
  - src/enterprise/client/providers/**, nav/**, boot/**, errors/**, hooks/**, routes/**, services/**
  - src/enterprise/client/features/admin/layout/**, primitives/**, reauth/**, gates/**, overview/**, pages/**, unified/**, managedResources/**
  - apps/server/src/enterprise/routers/admin/managedResources.ts (+ *.test.ts)   [routers/F2]
  - apps/server/src/enterprise/routers/platform.test.ts   [routers/F8: delete this assertion-free duplicate test]
  - apps/server/src/enterprise/featureFlags/**, runtimeConfig/**, bootstrap/**, observability/**, testing/**
  - src/enterprise/server/**
  - src/business/**, packages/business/**
  - packages/database/src/models/platform/sidebarLayout.ts, packages/database/src/schemas/platform/sidebarLayout.ts   [db-core/F3 sidebar row-id CHECK + F4 mode CHECK — but you may NOT create a migration; if a CHECK needs a migration, note it for db-core]
- Do NOT edit any file outside your owned paths. In particular do NOT edit locale files (packages/locales/**, locales/**) — i18n is a SEPARATE batch. If your fix needs a new user-facing string, call t('<namespace>.<key>', { defaultValue: '<English>' }) with a defaultValue and list the new key in your summary; the i18n batch will add zh-CN.
- Do NOT run git (no add/commit/push/checkout). Leave changes in the working tree.
- Do NOT run the full test suite or a full build (too slow). You MAY read files freely and MAY run `bunx tsc`/typecheck on a single file if quick, but it is optional.
- Keep fixes MINIMAL and correct. Preserve public contracts unless the fix requires changing them; if a client and server contract both need changing and one side is outside your owned paths, implement your side and NOTE the counterpart needed.
- DATABASE MIGRATIONS: you should NOT need a new migration. If you believe one is genuinely required, do NOT create it — instead implement the fix at code level if possible, or note the required migration in your summary for a dedicated DB batch.

FINISH by printing a concise summary: for each finding ID in shared-infra.md, one line — FIXED (what you changed) / DELETED / TEST-FIXED / SKIPPED (why) / DEFERRED (why). Then list any new i18n keys and any cross-batch counterparts you could not touch.

PARTITION-SPECIFIC GUIDANCE:
shared-infra/F1: capture draft epoch before publish and only apply refreshed draft if epoch unchanged (mirror the ordinary-save path's draftEpochRef). F2 out-of-order admin-access checks (request-generation ref). F3 map only PLATFORM_NOT_FOUND to fallback/create, rethrow others. F4 unsaved-changes modal dismissal. F5 stale vault cred ref. F6 DataTable nested-control activation. routers/F2: track publishCommitted stage (not error class) to cancel the transition on pre-commit failure. F8 (shared-infra): localize the OAuth/IdP callback result HTML pages by request locale (add zh-CN variants inline — these are server-rendered .ts, NOT locale files, so you own them). routers/F8: delete platform.test.ts. F9 delete commented route exports. If sidebar CHECK needs a migration, NOTE it for db-core (do not create a migration).

You are a senior engineer fixing VERIFIED defects in the AIHub enterprise CI / release-gating tooling (Node/TypeScript scripts).

READ FIRST: audit/round2/partitions/scripts-tooling.md and audit/round2/fix/verify/scripts-tooling.verified.txt. IMPORTANT context from verification: these are CI/production-readiness gates, NOT production runtime. Several were codex-marked CRITICAL/HIGH but VERIFIED down (production-trust is currently DISABLED, so the gate fail-opens are latent). Still fix them properly — they must be sound before production-trust is ever enabled. Fix at the RIGHT scope per the verified notes; do not over-engineer.

YOUR JOB — fix every finding F1–F14:
- F1: make verifyExpandOnlyPostBaselineSql reject all destructive contract changes (DROP COLUMN, DROP CONSTRAINT, narrowing type/nullability, RENAME, schema-qualified DROP TABLE public.x) via a conservative tokenizer, with negative regression fixtures.
- F2: detectAutofixMutation must NOT treat a failed `git status` (nonzero exit) as "no mutation" — return tri-state or throw and fail the gate; add a failing-status regression.
- F3: check-path-boundaries + adapter must fail when zero files were scanned / roots missing / wrong CWD; never synthesize filesScanned=1.
- F4: require assertions on every passed gate and in signed passed payloads; compare signed vs submitted assertion summaries.
- F5: bind failure-drill verification to recomputed raw-report digests, not just aggregate JSON; reject forged/missing raw evidence.
- F6: either implement a real previous-version rollback stage or rename the gate/report to migration-upgrade-rerun so it stops falsely claiming rollback.
- F7: keep app-rollback fail-safe but make its integration test exercise a real path OR clearly mark the gate unavailable in the required set (do not leave a permanent false-required stub silently).
- F8: stream backup dump through hashing/pg_restore and batch publication-pointer queries (remove full-DB buffering + N+1); at minimum remove the fixed 64MiB buffer ceiling and add chunked/streamed reads.
- F9: parse compose YAML and validate the exact prometheus/otel service image/command/volume (not whole-file includes).
- F10: consume/ignore pg_restore stdio and escalate SIGTERM->SIGKILL on timeout; settle the promise once.
- F11: move Docker/network-dependent cases out of the unit .test.ts into an opt-in .integration.test.ts; keep parser tests hermetic.
- F12: make the backoff test use a deterministic/fake clock, not Date.now()+5.
- F13: delete the confirmed orphaned/deprecated exports (peekResourceTokenShape, deprecated aliases, isUnsafeBackupPath, runArgv, duplicate removeDirectoryExact, deprecated temp-path helpers, resolveAllowlistedArgv + its compat-only test).
- F14 (large-file split): SKIP — note as deferred; do not do a big mechanical refactor.

RULES:
- Edit ONLY files under scripts/enterprise/** EXCEPT the branding* files (scripts/enterprise/brandingLiterals*, brandingLiteralFiles.ts, brandingFormatFragments.ts, scan-branding-literals*, branding-literals-baseline.json — those are a DIFFERENT batch; do NOT touch them).
- Do NOT run git. Do NOT run the full suite. You MAY run a specific script's own test if quick.
- Keep changes minimal and style-matching. Preserve the gate contracts consumed by the upstream-rebase-ci/production-readiness runners.

FINISH: one line per finding F1–F14 — FIXED / TEST-FIXED / DELETED / DEFERRED (why).

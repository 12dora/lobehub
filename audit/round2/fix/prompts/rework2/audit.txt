You are fixing ONE narrow but real REGRESSION in the AIHub enterprise audit-export subsystem, introduced by a cross-fix interaction. Read audit/round2/fix/verify/reports2/audit.fixcheck.md finding F6.

REGRESSION (F6): db-core's job claimNext now dead-letters a final-attempt, lease-expired worker (transitions exhausted expired jobs to 'dead'). For an EXPORT job that crashed AFTER uploading its S3 object, this dead-lettering bypasses the worker's normal failure/cleanup path, so the export's purge-outbox is never created and the uploaded object (containing conversation evidence) is orphaned permanently.

FIX (in the AUDIT lane only): persist the cleanup INTENT as soon as an uploaded object may exist — i.e. create/record the purge-outbox entry for the export's deterministic object key at (or before) upload time, not only on the terminal success/cancel path. That way, however the job terminates (success, cancel, failure, OR dead-letter by lease-expiry), a durable outbox row exists and the existing retention/purge sweep will eventually delete the object. If a small reconciliation is cleaner (a sweep that finds dead export jobs with an uploaded key and enqueues their purge), that is acceptable too. Add a regression proving a dead-lettered export after upload still gets its object purged (delete-fails-once tolerated).

Edit ONLY: apps/server/src/enterprise/services/audit/**, packages/database/src/models/platform/auditExport.ts, packages/database/src/models/platform/auditRetention.ts. Do NOT change db-core's job.ts claim logic. Do NOT edit locale files or run git or the full suite. Keep the fix minimal and consistent with the existing outbox pattern. FINISH: describe the fix + the new test.

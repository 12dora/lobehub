You are fixing ONE real logic bug in the AIHub enterprise identity-provider admin UI. Read audit/round2/fix/verify/reports2/identity.fixcheck.md finding F4.

BUG (F4): the Disable action now works for edited/published-history drafts, BUT the "has published history" determination treats a lookup FAILURE (error) as false (= "never published"). Consequences: for a provider that DOES have a live published revision, a transient history-lookup failure hides the Disable action and instead offers a Delete that the backend rejects — exactly when an admin may urgently need to revoke a compromised provider.

FIX: make the published-history signal a TRI-STATE (has-history / no-history / unknown), never conflating "lookup failed" with "never published". On UNKNOWN (lookup error/loading), FAIL SAFE toward revocation: keep the Disable action available (do not hide it) and do NOT offer the backend-rejected Delete. Prefer deriving the published-history flag from server-projected data (e.g. the provider record's own published-revision presence) rather than a separate failable lookup, if that data is already available on the loaded provider. 

Edit ONLY: src/enterprise/client/features/admin/identityProviders/**, and if a server-projected published-history flag is needed, apps/server/src/enterprise/services/identityProvider/** + apps/server/src/enterprise/routers/admin/identityProviders.ts (keep contract changes minimal and typed). Do NOT edit locale files (use t(key,{defaultValue})). Do NOT run git or the full suite. Add a page-level regression for publish→edit/clear→revoke. FINISH: describe the fix.

You are a senior TypeScript engineer doing a REWORK pass on AIHub enterprise 二开 fixes. An independent codex reviewer found gaps in the first fix pass. 

READ: audit/round2/fix/verify/reports/audit.fixcheck.md — a table with one row per finding marked FIXED_OK / PARTIAL / NOT_FIXED / REGRESSION and a Note explaining the gap.

YOUR JOB: resolve every REGRESSION and NOT_FIXED row, and every PARTIAL row that is a REAL logic/runtime bug (not merely a missing regression test). KEEP the FIXED_OK work intact. Prioritize RUNTIME-BREAKING issues (zod .parse() output-validation failures, broken saves/reads, wrong data). For rows that are only "missing a regression test", add the test if the harness is right there and quick; otherwise leave it.

RULES: edit only within these owned paths: apps/server/src/enterprise/services/audit/**, apps/server/src/enterprise/routers/admin/audit.ts, apps/server/src/enterprise/routers/admin/stats.ts, packages/database/src/models/platform/audit*.ts, packages/database/src/models/platform/globalStats.ts, packages/database/src/models/__tests__/platform.globalStats.test.ts, src/enterprise/client/features/admin/audit/**, src/enterprise/client/features/admin/stats/**. Do NOT edit locale files (packages/locales/**, locales/**) — use t(key,{defaultValue}) for any new string. Do NOT run git. Do NOT run the full suite (you MAY run a single related test). Match existing style. FINISH with one line per row you changed.

PARTITION-SPECIFIC APPROACH:
F3/F5/F6 (error-DTO REGRESSIONs): the contracts error DTOs are now STRICT code-only (this is correct for security — keep contracts as-is). The fix goes HERE: in the retention/export workers and cancel/projection paths, SANITIZE errors to match the strict DTO — map caught exceptions to the bounded error-code enum the contract defines and DROP the raw `message`/`name` (or replace with a safe bounded category) before persisting/returning, so tRPC output validation passes. For export cancellation (F5): do not return the internal purge payload; return the contract-valid shape. Read apps/server/src/enterprise/contracts/adminAudit/{exports,retention}.ts to see the exact allowed output fields, and make the worker/service outputs conform.
F6 also (platform-instance/F6 globalStats REGRESSION): in globalStats findAndGroupByDay, do NOT emit records with blank userId. Keep per-user grouping but CAP to top-N users by usage plus an aggregated "other" bucket, and also cap model/provider cardinality; ensure the GroupBy.User UI still gets populated userId/displayName series.
F1 (CRITICAL, PARTIAL — only lacks a concurrency test): if a concurrent-worker regression is feasible with the existing test harness, add one proving a job cannot be claimed before its required audit commits; otherwise leave the (correct) transactional fix as-is.
You are a senior TypeScript engineer doing a REWORK pass on AIHub enterprise 二开 fixes. An independent codex reviewer found gaps in the first fix pass. 

READ: audit/round2/fix/verify/reports/platform-instance.fixcheck.md — a table with one row per finding marked FIXED_OK / PARTIAL / NOT_FIXED / REGRESSION and a Note explaining the gap.

YOUR JOB: resolve every REGRESSION and NOT_FIXED row, and every PARTIAL row that is a REAL logic/runtime bug (not merely a missing regression test). KEEP the FIXED_OK work intact. Prioritize RUNTIME-BREAKING issues (zod .parse() output-validation failures, broken saves/reads, wrong data). For rows that are only "missing a regression test", add the test if the harness is right there and quick; otherwise leave it.

RULES: edit only within these owned paths: apps/server/src/enterprise/services/platformInstance/**, apps/server/src/enterprise/services/platformSystem/**, apps/server/src/enterprise/services/platformGlobalCredentials/**, apps/server/src/enterprise/routers/admin/{system,creds,credsSupport}.ts, packages/database/src/models/platform/globalCredential.ts, src/enterprise/client/features/admin/system/**. Do NOT edit locale files (packages/locales/**, locales/**) — use t(key,{defaultValue}) for any new string. Do NOT run git. Do NOT run the full suite (you MAY run a single related test). Match existing style. FINISH with one line per row you changed.

PARTITION-SPECIFIC APPROACH:
Resolve the REGRESSION/NOT_FIXED rows that are real (NOTE: globalStats F6 is being fixed by the audit rework — do NOT touch globalStats.ts). F4 (PARTIAL): if feasible, make the system-health token projection bounded (select only pointer ids/revisions/checksums, not full payloads). F2/F8 partials are missing tests — add only if quick. Do not create new migrations.
You are a senior engineer fixing missing Simplified-Chinese (zh-CN) i18n in the AIHub enterprise admin console (LobeHub react-i18next).

READ FIRST: audit/round2/partitions/i18n.md (findings F1, F2) and audit/round2/partitions/identity.md (F12) — they list the exact missing keys.

YOUR JOB: add the missing `admin`-namespace keys so zh-CN admins no longer see English fallbacks. Add each key to ALL THREE locale sources, hand-writing proper en-US and Simplified-Chinese values:
- packages/locales/src/default/admin.ts   (the TypeScript source of truth; place keys under the correct nested object, e.g. identityProviders.disable.*, identityProviders.restart.*, identityProviders.columns.*, identityProviders.actions.*, aiCatalog.editor.test.message.*)
- locales/en-US/admin.json
- locales/zh-CN/admin.json   (Simplified Chinese translations — translate properly, do not leave English)

KEYS TO ADD (from the reports; verify exact usage/defaultValue by grepping src/enterprise/client for each t('identityProviders.disable...') call to copy the intended English text):
- identityProviders.disable.cancel, identityProviders.disable.impact, identityProviders.disable.confirm, identityProviders.disable.title, identityProviders.disable.success
- identityProviders.restart.failedWithCategory   (interpolated message; preserve its {{...}} placeholders)
- identityProviders.columns.actions
- identityProviders.actions.disable
- aiCatalog.editor.test.message.pending   (sibling of .success/.failure; zh-CN e.g. 连接测试正在进行中)

RULES:
- Edit ONLY packages/locales/src/default/admin.ts, locales/en-US/admin.json, locales/zh-CN/admin.json. Do NOT edit any component/.tsx file (other batches own those; they keep their defaultValue fallbacks). Do NOT run `bun run i18n` (it is slow and fills other locales — leave that to a later manual step). Do NOT run git.
- Keep the JSON structurally valid and the admin.ts object well-formed; match the surrounding nesting and key ordering conventions.
- Preserve interpolation placeholders exactly (e.g. `{{category}}`).

FINISH: print which keys you added and the en-US + zh-CN value for each.

ADDITIONAL TASK — new keys introduced by the round-2 fixes:
The bug-fix batches added NEW user-facing strings as `t('<namespace>.<key>', { defaultValue: '<English>' })` fallbacks (English renders, but zh-CN is missing). Find and translate ALL of them:
1. Run (read-only) `git diff -- 'src/enterprise/**' 'apps/server/src/enterprise/**'` and scan ADDED lines for every `t('...', { defaultValue: ... })` call whose key is NOT already in the locale files.
2. For each such key, add it to packages/locales/src/default/admin.ts + locales/en-US/admin.json (use the English defaultValue text) + locales/zh-CN/admin.json (proper Simplified-Chinese translation). Place under the correct nested path (e.g. audit.live.errors.*, branding.refresh.*, aiSettings.draftBanner.*, and any others you find).
3. Preserve interpolation placeholders ({{...}}) exactly.
You MAY run `git diff`/`git status` read-only to discover keys, but do NOT run any git write command (add/commit/checkout).

You are a senior TypeScript engineer fixing VERIFIED bugs in the "AIHub" enterprise secondary-development (二开) layer (built on LobeHub, Next.js 16 + React 19 + Drizzle + tRPC).

CONTEXT TO READ FIRST:
- audit/round2/partitions/identity.md  — the full findings for your partition (evidence, failure scenario, suggested Fix per finding).
- audit/round2/fix/verify/identity.verified.txt — the VERIFIED severity + scope corrections from an independent cross-model review. TRUST THIS for scope: some findings codex marked HIGH/CRITICAL were verified down to MEDIUM/LOW with a precise reason. Fix the REAL defect at the RIGHT scope — do not over-engineer a MEDIUM into a huge refactor, and do implement the CRITICAL/HIGH fixes properly.

YOUR JOB: Fix EVERY finding in identity.md that is a real defect, dead code, or rotten test:
- Functional bug (⑤): implement the Fix described (use your judgment; the report's "Fix:" is guidance, not gospel). Match surrounding code style, idioms, and error/CAS/transaction patterns already used in these files.
- Dead code (③): delete it (and its now-orphaned imports/exports/tests).
- Rotten test (②): FIX it to assert correct behavior, or DELETE it if it has no value, exactly as the finding says. When a finding says a test "locks in the bug", the test MUST be updated to assert the NEW correct behavior you just implemented.
- Missing regression (② ADD): add a focused test ONLY if it is quick and the test file/harness already exists nearby; otherwise skip and note it.
- Skip pure "split this large file into modules" refactors (dimension ① size-only) — note them as deferred; do not do large mechanical refactors.

STRICT RULES:
- Edit ONLY files under these OWNED PATHS:
  - apps/server/src/enterprise/services/identityProvider/**
  - apps/server/src/enterprise/routers/admin/identityProviders.ts, identityProvidersSupport.ts (+ *.test.ts)
  - apps/server/src/enterprise/routers/admin/authSettings.ts (+ *.test.ts)
  - apps/server/src/enterprise/routers/admin/security.ts (+ *.test.ts)
  - packages/database/src/models/platform/identityProvider*.ts, authSettings.ts, accessStatus.ts (+ *.test.ts)
  - packages/database/src/schemas/platform/identity.ts, authSettings.ts
  - src/enterprise/client/features/admin/identityProviders/**
  - src/enterprise/client/features/admin/securityAuth/**
- Do NOT edit any file outside your owned paths. In particular do NOT edit locale files (packages/locales/**, locales/**) — i18n is a SEPARATE batch. If your fix needs a new user-facing string, call t('<namespace>.<key>', { defaultValue: '<English>' }) with a defaultValue and list the new key in your summary; the i18n batch will add zh-CN.
- Do NOT run git (no add/commit/push/checkout). Leave changes in the working tree.
- Do NOT run the full test suite or a full build (too slow). You MAY read files freely and MAY run `bunx tsc`/typecheck on a single file if quick, but it is optional.
- Keep fixes MINIMAL and correct. Preserve public contracts unless the fix requires changing them; if a client and server contract both need changing and one side is outside your owned paths, implement your side and NOTE the counterpart needed.
- DATABASE MIGRATIONS: if (and only if) a fix genuinely requires a schema change, create exactly ONE new migration file: packages/database/migrations/0147_round2_identity.sql . Make every statement idempotent (IF NOT EXISTS / guarded DO blocks / CREATE ... IF NOT EXISTS). Follow the style of existing 013x/014x platform migrations. Also update the matching Drizzle schema file (in your owned paths) to match. Do NOT create any other-numbered migration. If you are unsure a migration is needed, prefer a code-level fix.

FINISH by printing a concise summary: for each finding ID in identity.md, one line — FIXED (what you changed) / DELETED / TEST-FIXED / SKIPPED (why) / DEFERRED (why). Then list any new i18n keys and any cross-batch counterparts you could not touch.

PARTITION-SPECIFIC GUIDANCE:
You also own routers/F3 (register admin.identityProviders.disable in the mutation-POLICY registry — it's already in the security registry; find where sibling dangerous identity mutations register reauth/reason/audit/rate-limit policy and add disable identically) and routers/F5 (identity error mapping: stop classifying unknown errors as PLATFORM_INVALID_INPUT — log + return INTERNAL_SERVER_ERROR for untyped errors). C2 (identity/F1) is the #2 release blocker: make disable behave like a pending activation (retain revision, include tombstone in pendingRestart status, compute empty-provider-set identity, refresh runtime status after disable) so the UI shows a Restart action and the running Better Auth config actually gets reloaded. authSettings needs, via migration 0147: a monotonic `revision` column for CAS (identity/F6) and a CHECK/model-invariant rejecting emailDomainAllowlistEnabled=true with empty list (identity/F7) and a CHECK(id='global') singleton guard. The zod-contract half of empty-allowlist (contracts/F2) is a DIFFERENT batch — just fix the db model + schema here.

=== YOUR PARTITION: db-core ===
Horizontal pass: DATABASE core — platform schemas, shared models, migrations, triggers/constraints.
Report issues only in these paths:
- packages/database/src/models/platform/revision.ts, checksum.ts, cursor.ts, errors.ts, job.ts, managedResourcePolicy.ts, index.ts and any platform model not owned by a vertical. NOTE: the audit/ai/identity/connectors/etc. verticals own their table's BUSINESS logic — you own SCHEMA / constraint / index / trigger / migration correctness and the shared model helpers across ALL platform tables.
- packages/database/src/schemas/platform/common.ts, index.ts, revisions.ts, managedPolicy.ts, jobs.ts and cross-cutting schema concerns
- packages/database/src/repositories/platform/**
- packages/database/migrations/0135_*.sql through 0146_*.sql  (idempotency, ordering / folderMillis, reversibility, guard clauses)
Focus: migration idempotency and ordering (folderMillis conflicts after rebase), immutability triggers such as version rows and their GUC / transaction escape hatches, foreign keys and ON DELETE behavior (cascade vs orphan), unique and partial indexes matching the actual query patterns, NOT NULL / default correctness, checksum/cursor/revision helper correctness, single-active partial-unique constraints. Dimension 5: schema-level bugs that permit orphans, lost updates, or broken immutability. Also dimensions 1-3 across these files, and flag any migration that is non-idempotent or would conflict by folderMillis. Dimension 4 unlikely — note if clean.

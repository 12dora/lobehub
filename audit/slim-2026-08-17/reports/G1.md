# G1 — Module core (backend)

## What changed

### Contract / env
- `packages/const/src/platform/modules.ts` — added `MODULE_SETTINGS_INVALIDATION_SCOPE` + `MODULE_SETTINGS_SNAPSHOT_TTL_MS`. Router-key lists verified against live routers; **no key corrections** (every listed admin/lambda/async/tools key exists; no duplicate lookup keys; worker names unique).
- `packages/const/src/platform/modules.test.ts` — presets, `LOBE_MODULES_DISABLED` (spaces/commas/unknown), legacy `ENABLE_PLATFORM_*=0`, `computeEffectiveModules` (missing row = all on; env wins over db true), `matchPreset`, uniqueness, live-router key check.
- `packages/const/src/platform/errorCodes.ts` — `PLATFORM_MODULE_DISABLED`.

### DB
- Schema `packages/database/src/schemas/platform/moduleSettings.ts` (`id='global'`, jsonb `modules` default `{}`, `setup_completed_at`, `revision` default 1, `updated_by`, timestamps).
- Model `get()` / `upsertWithCas()` with typed `PlatformRevisionConflictError`. Missing row = `null` (never fail-closed).
- Hand-written `migrations/0020_platform_module_settings.sql` (idempotent) + `_journal.json` idx 20 / when `1787500000000` / tag `0020_platform_module_settings`.
- `meta/0020_snapshot.json` copied from **0018** (0019 does not exist on this branch) and the table added.

### Service
- Replaced env-only stub internals in `apps/server/src/enterprise/services/moduleSettings/`. Contract unchanged.
- Hot path: `resolveModulesFromEnv` + DB via `DomainConfigCache` (TTL 30s, LKG, fail-open to env-only) + `publish({ scopes: ['modules'] })`.
- `updateModuleSettings` CAS + invalidate; `initBootModules` freeze; `getPendingRestartModules`; `assertModuleEnabled` / `moduleDisabledError`.

### Guards / routers
- `enterpriseErrors.ts` maps `PLATFORM_MODULE_DISABLED` → tRPC `FORBIDDEN`, `details.moduleId`.
- `platformPermission.ts`: after `ADMIN_FEATURE_DISABLED`, `MODULE_BY_ADMIN_ROUTER_KEY[path.split('.')[1] ?? path.split('.')[0]]` (verified path is `admin.audit.list`). Request-scope cache for `loadPlatformAuthContext` keyed by `ctx.resHeaders` (survives tRPC ctx spread) so one HTTP request = one RBAC join.
- `guards/moduleGuard.ts` `withModule(id)` on `platformAgents`, `platformSkills`, `platform.taskTemplates.list`.
- Deleted import-time worker/readiness side-effects from `routers/platform.ts` (14 calls + `warnIfPlatformMasterKeyMissing`) and `routers/admin.ts` (3 readiness). G2 re-homes.

### admin.modules
- `routers/admin/modules.ts` + `contracts/adminModules.ts`:
  - `get` SYSTEM_READ
  - `update` SYSTEM_OPERATE + audit `admin.modules.update` (before/after) + reauth **only** when turning OFF `audit` or `moderation`
  - `requestRestart` SYSTEM_OPERATE via `ProcessRestartController` (`after()` + SIGTERM)
- Response: `{ snapshot, pendingRestart, restart: { supported, reason? }, instanceId }`.
- Registered in both policy shards. Counts bumped **215/101/114 → 218/102/116**. Skipped optional `module?` metadata (not trivial).
- `restartController.resolveRestartCapability` also accepts `PLATFORM_RESTART_MODE=supervisor` (old `PLATFORM_OIDC_RESTART_MODE` kept).

### Capabilities / types
- `PlatformCapabilities.modules: PlatformModuleStateMap`.
- `DISABLED_PLATFORM_CAPABILITIES.modules = ALL_MODULES_ENABLED` (fail-open).
- Filled in `buildPlatformCapabilities` + `platform.getCapabilities`.
- `EnterprisePublicServerConfig.modules?: Record<string, boolean>` (already present; G4 fills globalConfig).

### Docs
- `docs/enterprise/modules.md` **does not exist**. Notes for G5/commander stitch:

> **Module settings storage & API.** Singleton `platform_module_settings` (`id='global'`). `effective[id] = envDisabled ? false : (db.modules[id] ?? true)`. Missing row = all ON. Hot snapshot: 30s `DomainConfigCache` + Redis scope `modules`. tRPC `admin.modules.{get,update,requestRestart}`. Disabled module → `PLATFORM_MODULE_DISABLED` / FORBIDDEN / `{ moduleId }`. Restart-kind diffs appear in `pendingRestart`.

### Extra (needed for audit catalog)
- `AUDIT_ACTIONS` + locale keys `audit.logs.action.admin.modules.update` (en-US / zh-CN / default). F1 owns locales; targeted insert only.

## How verified

```
# lint+test (direct CLI; bun run check dies on unrelated enterprise:check-branding / apps/cli/pnpm-lock.yaml)
bun run .agents/scripts/check/cli.ts --lint --test <G1 files>
# lint clean. Passed: moduleSettings service (7), moduleGuard (2), platformPermission (5),
# enterpriseErrors (2), workerBootstrap (2).

cd packages/const && bunx vitest run src/platform/modules.test.ts src/platform/errorCodes.test.ts
# 23 passed (18 modules + 5 errorCodes)

cd packages/types && bunx vitest run src/platform/capabilities.test.ts
# 3 passed

cd packages/database && bunx vitest run src/models/platform/moduleSettings.test.ts
# 5 passed (PGlite)

bunx vitest run apps/server/src/enterprise/services/platformCapabilities.test.ts \
  apps/server/src/enterprise/security/policy/adminMutationRegistry.test.ts
# 12 passed (mutation-registry AST parity includes the 2 new mutations)

# Other guards: reauth/rateLimit/managedResource/adminWebapi/connectorRuntime 47 passed
```

## Open / commander must do

1. **G4 TDZ** `apps/server/src/enterprise/services/platformInstance/catalogAuthority.ts:40` calls `dropAiCatalogSnapshotCache` before it is defined. Blocks `adminProcedureAuthorizationRegistry.test.ts` (live lambda import) and `managedPlatformAgent.test.ts`. One-line move below the `const`. Not G1-owned — did not touch.
2. **docs/enterprise/modules.md** — G5 creates; stitch the section above.
3. Registry live reconcile not re-run after (1). Expected counts after G1: **218 total / 102 queries / 116 mutations**.
4. Optional `module?` metadata on registry entries skipped (would touch all 215+ entries).
5. F1: locale key `audit.logs.action.admin.modules.update` already inserted so catalog tests stay green.
6. G2: workers/readiness/master-key warn were removed from `platform.ts` / `admin.ts` as specified; do not recreate those import-time calls.

## Round 2 (review rework)

Adjudication applied:

1. **F1 deviceGateway** — not gated. `lambda.device` mixes ~30 remote-gateway procedures with local enrollment (`register` / `updateDevice` / `removeDevice` / workspace mint+register+update+remove). Not a small subset. `lambdaRouterKeys` stays `[]`; comment recorded on the `deviceGateway` def. Gating is UI + tool-runtime (G3b).
2. **F2** — `platform.aiCatalog.getPublished` now `.use(withModule('managedAi'))`. `getCapabilities` / `getPublicSnapshot` stay ungated. Tests: env-off (`ENABLE_PLATFORM_MANAGED_AI=0`) and DB-off (`modules.managedAi=false`) both throw `PLATFORM_MODULE_DISABLED`.
3. **F3+F4** — `updateModuleSettings` reads the authoritative row (revision must match `expectedRevision`) *before* reauth; CAS write + audit share one transaction; invalidation publishes only after commit. Stale hot snapshot cannot skip reauth; audit throw rolls the settings row back.
4. **Renumber + F5** — migration is now `0021_platform_module_settings` (idx 21, when `1787500000000`). Snapshot copied as text from 0018 (Chinese literal `使用工作账号登录` restored). `0018 == 0021` minus ids + new table.
5. **F6** — `apps/server/src/enterprise/routers/admin/modules.test.ts` covers get / update / stale-cache reauth / CAS conflict / audit rollback / restart-unsupported.
6. **aiCatalogReadiness ReferenceError** — `runtimeReadiness.ts` no longer statically imports `runtimeAdapter` (that pulled catalogAuthority/model-bank at spec `start()` and raced a TDZ). Probe lazy-imports the adapter; registration is zero-I/O. Also consults hot `isModuleEnabled('managedAi')` so G2 can always register the spec. Did not change `workersBootstrap.ts` (G2 already dropped `moduleId` on the readiness specs).

### Round 2 verification

```
bun run .agents/scripts/check/cli.ts --lint <G1b files>   # clean
cd packages/database && bunx vitest run src/models/platform/moduleSettings.test.ts  # 5 passed
admin/modules.test.ts                 # 6 passed
platform.aiCatalog.test.ts            # 6 passed
moduleSettings/index.test.ts          # 7 passed
runtimeReadiness.test.ts              # 8 passed
workersBootstrap.test.ts              # 6 passed (incl. unmocked start())
adminProcedureAuthorizationRegistry   # 10 passed — 218 / 102 / 116
adminMutationRegistry                 # 6 passed
platformPermission + moduleGuard      # 7 passed
```

Commander: after rebase onto main's `0020_platform_network_proxy_subscription_issue`, set `0021_snapshot.prevId` to that 0020 snapshot id if drizzle cares. Registry counts stay 218/102/116 here; peer main +1 → 219/102/117.

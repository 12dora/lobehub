# F1 — Admin console: modules page, degradation, capability plumbing, poll knobs

Tree: `/Users/konata/code/AIHub-worktrees/slim` (branch `feat/slim-modules`). Nothing committed/staged.

## What changed

**New (fork)**
- `src/enterprise/client/boot/getBootModules.ts` (+ `.test.ts`) — sync boot read of
  `__SERVER_CONFIG__.config.enterprise.modules`; missing/partial/malformed ⇒ `ALL_MODULES_ENABLED`
  (fail-open, vite dev). `normalizeModuleStateMap`, `getBootDisabledModules`.
- `src/enterprise/client/hooks/useModuleEnabled.ts` — `useModuleStates` / `useModuleEnabled(id)` /
  `useDisabledModules()`. Prefers live `capabilities.modules`, falls back to boot; both fail open.
- `src/enterprise/client/services/adminModules.ts` — `admin.modules.get/update/requestRestart`,
  typed off G1's `@/server/enterprise/contracts/adminModules`; exports `ADMIN_MODULES_SWR_KEY`.
- `src/enterprise/client/features/admin/modules/**` — `ModulesPage`, `ModulePresetRow`,
  `ModuleSummaryBar`, `ModuleGroupList` (+ `CoreModulesFooter`), `ModuleRow`, `ModuleRestartBanner`,
  `ModuleWizard`, `SetupGuideCard`, `useAdminModules.ts` (SWR + 3-state restart driver),
  `moduleDraft.ts` (pure summary/diff/preset maths), `index.ts`, `moduleDraft.test.ts`,
  `ModulesPage.test.tsx`.
- `src/enterprise/client/features/admin/gates/ModuleDisabledRoute.tsx` (+
  `AdminPermissionOutlet.modules.test.tsx`) — connected wrapper that resolves `envDisabledBy[id]`
  off the shared modules cache so the surface can name the container parameter.
- `src/enterprise/client/features/admin/shared/pollIntervals.ts` (+ `.test.ts`) — `ADMIN_POLL_INTERVALS`.
- `src/enterprise/client/nav/adminNavMeta.modules.test.ts`.
- `src/enterprise/client/providers/enterprisePlatformContext.ts` — the platform context split out of
  `EnterprisePlatformProvider` (see caveat 8). The provider re-exports `useEnterprisePlatform` /
  `EnterprisePlatformContextValue`, so no existing call site changed.

**Edited (fork)**
- `nav/adminNavMeta.ts` — `AdminNavItem.moduleId?`, 27 leaf annotations, new `modules` nav item
  (`/admin/system/modules`, SYSTEM_READ, under `system`), `filterAdminNavByPermissions(…, disabledModules = ∅)`,
  `isAdminNavItemModuleDisabled`, `findAdminNavModuleId`. `nav.modules` label key.
- `nav/adminIcons.ts` — `modules: Package` (the route-tree test asserts every visible nav id has an icon).
- `nav/adminPageCatalog.tsx` — `modules` → `lazy(ModulesPage)`.
- `features/admin/layout/AdminSideNav.tsx`, `gates/GroupIndexRedirect.tsx` — pass the disabled set.
- `gates/AdminPermissionOutlet.tsx` — disabled module ⇒ `ModuleDisabledRoute`, never 404.
- `pages/AdminStateSurfaces.tsx` — `AdminModuleDisabledSurface` (title / what it is / one next step).
- `pages/OverviewPage.tsx` — renders `SetupGuideCard`.
- `systemGeneral/SystemGeneralPage.tsx` — network-proxy tab gated on `networkProxy` (tab-selection
  fallback already prevents stranding).
- `securityAuth/SecurityAuthPage.tsx` — 登录方式 tab gated on `databaseIdp`; 通用设置 stays (core).
- `errors/mapEnterpriseError.ts` (+ test) — `PLATFORM_MODULE_DISABLED → 'none'`.
- Poll sites now read the table: `networkProxy/hooks.ts` (also **visibility + `useModuleEnabled('networkProxy')`
  gated**), `audit/shared/useCursorPagination.ts`, `system/hooks/useAdminSystem.ts`,
  `identityProviders/useIdentityProviders.ts`.

**Edited (upstream, 1 line)**
- `src/store/serverConfig/selectors.ts` — `+ enterpriseModules` selector.

**i18n (targeted inserts, no re-sort)** — 137 new keys in `packages/locales/src/default/admin.ts`
(appended block) + alphabetical inserts into `locales/{en-US,zh-CN}/admin.json`:
`nav.modules`, `modules.{title,description,presets.*,summary.*,deps.*,groups.*,tags.*,status.*,envTooltip,
core.*,save,discard,pendingChanges,saved.*,errors.*,danger.*,restart.*,wizard.*,guide.*,disabledSurface.*}`,
`modules.items.<id>.{title,desc}` × 24, `enterprise.error.PLATFORM_MODULE_DISABLED`.

**Existing tests updated (mine)**
- `nav/adminNavMeta.test.ts` — the system-group child order now includes `modules`.
- `errors/mapEnterpriseError.test.ts` — one new case.

## How verified
- Final targeted sweep, `bunx vitest run --silent='passed-only'` over
  `features/admin/{modules,gates,shared,layout,securityAuth,systemGeneral}`, `boot`, `nav`, `errors`,
  `routes`, `networkProxy`: **all green** (227 tests over 26 files after the two fixes below).
  New suites: `moduleDraft` 16, `ModulesPage` 12, `adminNavMeta.modules` 10, `getBootModules` 6,
  `AdminPermissionOutlet.modules` 5, `pollIntervals` 4.
- Two failures I caused and fixed: the route-tree test needed a `modules` icon (added `Package` to
  `adminIcons.ts`), and `adminNavMeta.test.ts` pins the system-group child order.
- `bun run .agents/scripts/check/cli.ts --lint <all my files>` → clean (auto-fixes applied).
- No full `tsgo` (per rules). Contract types are imported from G1's real contract, so a mismatch
  would surface in the commander's single type pass.

## Things the commander must know
1. **I ran `git stash` once by accident** (~30 s, whole shared tree) and immediately `git stash pop`ed it.
   `git stash list` is empty and the tree was verified intact (156 changed paths, other agents' new files
   present). Nothing was lost, but if another agent saw a weird empty-tree moment around 16:05, that was me.
2. **Pre-existing red test, now fixed by me**: `enterpriseErrorLocale.catalog.test.ts` was already failing on
   `d1fc9295d2` — `PLATFORM_CONTENT_MODERATION_BLOCKED` had no `enterprise.error.*` copy in either locale.
   I backfilled EN + ZH (it is in files I own). Not related to this batch otherwise.
3. **Contract assumptions** (all matched G1's landed `contracts/adminModules.ts`): `get`/`update` return the
   same view; `update` takes a *partial* module map (I only send changed ids, so two admins editing different
   modules do not clobber each other); `requestRestart` takes `{}`. `restart.reason` is rendered verbatim in
   parentheses — if it is an internal token rather than prose, tell me and I will key it.
4. **Deviations from the brief, deliberate**:
   - The `audit` **group** carries no `moduleId` (its children do). Annotating the group would have hidden
     内容审计 whenever 审计 was off. Test pins this.
   - `identity-providers` (安全与认证) carries no `moduleId` either — it hosts the core registration/login
     policy tab. Only the 登录方式 tab is gated on `databaseIdp`, like the network-proxy tab.
   - `unified-management` stays unannotated; its two tabs self-gate. Its hidden deep links
     `/admin/settings` → `settingsPolicy` and `/admin/managed-resources` → `managedAi` are annotated.
   - `ai-memory` → `memory` (not in the brief's list, but it is the admin surface for that module).
   - Core-module footer is a static 4-row list (chat / users / auth / admin shell) because the constant
     table has no core entries.
5. **`window.__SERVER_CONFIG__.clientEnv` has no override channel** for poll intervals (it carries only
   market / pyodide / S3 paths and is an upstream type). Per the brief I left constants + documented it in
   `pollIntervals.ts`. The two platform-wide cadences still live in `providers/useEnterprisePlatformData.ts`
   (provider layer must not import `features/admin`); `pollIntervals.test.ts` pins them equal.
6. **Registry counts**: I added no admin procedures; the +3 from `admin.modules.*` are G1's.
7. Not done (out of my scope / not required): Playwright, `docs/enterprise/modules.md` copy (G5 owns the
   deployment sections), screenshots.
8. **I split `EnterprisePlatformContext` into `providers/enterprisePlatformContext.ts`.** Reading a
   capability previously required importing `EnterprisePlatformProvider`, which drags in the skill
   catalog → `packages/shared-tool-ui` → `antd-style` `keyframes`. That broke three unrelated suites
   (`SystemGeneralPage`, networkProxy `OutletSection` / `ScopesSection`) the moment a page read a module
   flag, and would have put the same graph into every chunk that gates on a module. The provider now
   imports and re-exports the context; all ten suites are green again. **Note for other agents**: prefer
   `providers/enterprisePlatformContext` for read-only capability access.
9. **Not mine, needs F2's attention**: `providers/EnterprisePlatformProvider.test.tsx` has 2 failures
   (`…catalog authority in ui-only managed mode`, `…same catalog path for every public managed Skill
   capability` — `getPublishedCatalog` spy not called). They fail identically before and after my provider
   edit and the only change in that data path is F2's `src/libs/swr/index.ts`
   `dedupingInterval: 0 → 2000`, which now dedupes the catalog fetch across mounts/tests. Either the tests
   need `provider: () => new Map()` per test, or the interval needs revisiting.

## Open questions
- Should switching a module **off** also warn when a *user-facing* upstream feature is in use
  (e.g. `knowledgeBase` with existing knowledge bases)? Today only `dependsOn` is surfaced.
- `cost.idleRssMb` is `null` for every module in the constant table, so the summary always shows
  "≥ 0 MB" + the unmeasured hint. Once B3 backfills real numbers the bar becomes useful with no code change.

---

# Round 2 — rework after REVIEW_F1

All ten findings accepted and fixed. Same tree, nothing committed/staged.

| # | Fix | Files |
|---|---|---|
| F1 | Only a **resolved** capability payload may override boot. The provider hands out `DISABLED_PLATFORM_CAPABILITIES` (whose `modules` is `ALL_MODULES_ENABLED`) while pending / failed / outside the tree, so identity against that shared sentinel is the exact "no answer yet" test. | `hooks/useModuleEnabled.ts` (+ new `useModuleEnabled.test.tsx`, 5 cases incl. pending-fetch and failed-fetch) |
| F2 | `/admin/unified` tabs now gate on `settingsPolicy` / `managedAi`, reusing the existing "fall back to the first offered tab" rule; both off ⇒ forbidden copy, not an empty shell. | `unified/UnifiedManagementPage.tsx` (+ new test, 5 cases) |
| F3 | Initial-load failure returns **early**: failure + retry only — no switches, presets, summary or Save over a state we never read. | `modules/ModulesPage.tsx` |
| F4 | `isModuleRevisionConflict()` (real `mapEnterpriseError` + `PLATFORM_REVISION_CONFLICT`) is the only trigger for reload + draft reset; every other failure keeps the draft and shows `modules.errors.conflict` vs `…saveFailed` appropriately. | `modules/useAdminModules.ts`, `ModulesPage.tsx` |
| F5 | `onSave(setupCompleted?)` is the single save path; wizard 完成 calls `onSave(true)`, so `DangerConfirm` cannot be bypassed. Save button uses `onClick={() => onSave()}` (an event arg would have read as `setupCompleted`). | `ModulesPage.tsx` |
| F6 | `summarizeModules` now reports `measured`; zero ⇒ 「未测量」 instead of `≥ 0 MB`. `PresetComparison.idleRssComparable` is false whenever either side is incomplete, and the bar then renders the jobs-only comparison. | `moduleDraft.ts`, `ModuleSummaryBar.tsx` |
| F7 | Wizard step 2 checks `error` **before** data: error copy + retry + an explicit "advisory, you can continue" note. 下一步 is disabled only while the probe is genuinely in flight. | `ModuleWizard.tsx` |
| F8 | `setupGuideDismissal.ts` — one `sessionStorage` key shared by the card's 稍后再说 and the wizard's exit; storage failures degrade to "keep showing". | new `setupGuideDismissal.ts` (+ test), `SetupGuideCard.tsx` (+ new test), `ModulesPage.tsx` |
| F9 | Restart reasons go through `modules.restart.reason.<token>` (4 known tokens + `unknown`), rendered via `modules.restart.unsupportedBecause`; `supervisor_not_configured` also shows the `PLATFORM_RESTART_MODE=supervisor` hint — the only reason an operator can act on. | `ModuleRestartBanner.tsx` |
| F10 | `ADMIN_POLL_INTERVALS` moved to `src/enterprise/client/shared/pollIntervals.ts`; the provider's two exports are now **derived** from it rather than duplicated. All five importers updated. | `shared/pollIntervals.{ts,test.ts}`, `providers/useEnterprisePlatformData.ts`, 4 feature hooks |

**i18n** — 12 more targeted keys (en + zh + `default/admin.ts`): `modules.summary.{unmeasured,compareStandardJobs}`,
`modules.errors.conflict`, `modules.restart.{unsupportedBecause,envHint,reason.*×5}`,
`modules.wizard.{infraFailed,infraFailedHint}`. Verified programmatically: en/zh/default all 3511 keys,
zero key diff, zero `{{placeholder}}` mismatches across the 148 `modules.*` keys.

**Verification**
- `bunx vitest run` targeted sweep — **39 files / 337 tests green** (`modules`, `unified`, `hooks`, `shared`,
  `gates`, `nav`, `boot`, `routes`, `systemGeneral`, `networkProxy`). New/extended: `ModulesPage` 12→21,
  `moduleDraft` 16→18, plus `useModuleEnabled` 5, `UnifiedManagementPage` 5, `setupGuideDismissal` 4,
  `SetupGuideCard` 4.
- Whole `src/enterprise/client`: 240 files / 1805 tests, all green except one unrelated flake (below).
- `bun run .agents/scripts/check/cli.ts --lint` clean on every touched file.
- Round-1 caveat 9 (EnterprisePlatformProvider ×2) is **resolved** — those suites now pass.

**One note for the commander**
`features/admin/connectors/adminConnectors.service.test.ts` times out at the default 5 s but passes at
`--testTimeout=60000`. It is a cold dynamic `import()` of the service, untouched by me; the batch's heavier
module graph pushed it past the limit. Likely to flake in CI — worth a bump or a static import.

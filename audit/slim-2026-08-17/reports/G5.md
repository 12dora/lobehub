# G5 — Docker / compose / runtime knobs / docs

Worktree: `/Users/konata/code/AIHub-worktrees/slim`. G0 spike was **not present**.

## What changed

| File | Change |
| --- | --- |
| `next.config.ts` | `isVercel ? vercel : isDocker ? docker : {}`. Docker excludes: `dist/{desktop,mobile,auth}/**`, `apps/{desktop,cli}/**`, `e2e/**`, `tests/**`, `**/*.tsbuildinfo`, `packages/database/migrations/**`, `changelog/**`, `docker-compose/**`, plus ffmpeg when `WITH_VIDEO=0`. |
| `src/libs/next/config/define-config.ts` | Deleted `dist/desktop/**` + `dist/mobile/**` includes (comment said exclude). Kept `public/_spa/**` + migrations. |
| `src/libs/next/config/dockerTracingExcludes.test.ts` | Locks picomatch `{contains:true}` so globs do **not** match `next/dist` / `es-toolkit/dist`. |
| `Dockerfile` | `ARG WITH_VIDEO=1`; busybox prune of `@vitejs/devtools` + ffmpeg when 0. Heap **not** baked into image `NODE_OPTIONS`. Layer split skipped (not trivially safe). Canvas hardlink skipped (scratch `COPY / /` explodes inodes). |
| `scripts/serverLauncher/startServer.js` | `SKIP_DB_MIGRATION=1`; `ENABLE_BOT_GATEWAY=0`; `LOBE_NODE_HEAP_MB` default 1536 appended to child `NODE_OPTIONS` (no clobber; `0` disables). Does not import G2's TS resolver. |
| `docker-compose/enhanced/docker-compose.yml` | profiles `redis` / `s3` / `search` (searxng); `depends_on.required: false` (Compose v5.1.4); S3 `:?` removed so minimal interpolates. |
| `docker-compose/enhanced/.env.example` + root `.env.example` | `COMPOSE_PROFILES`, `LOBE_MODULE_PRESET`, `LOBE_MODULES_DISABLED`, `LOBE_NODE_HEAP_MB`, `ENABLE_BOT_GATEWAY`, `SKIP_DB_MIGRATION`. |
| `docs/enterprise/modules.md` + `generate-modules-table.mts` | English deploy doc; table generated from `packages/const/src/platform/modules.ts`. Markers `<!-- G1: storage & API -->` `<!-- F1: admin page -->`. |
| `docs/enterprise/README.md`, `docker-compose/enhanced/README.md`, `README.md` | Links + short preset paragraph. |
| `docker-compose/enhanced/searxng-settings.yml` | Copied from `deploy/`. |

## Size table (arm64, `aihub:slim-g5` `4a818f065a4d`)

| | E3 baseline (`aihub:demo`) | G5 after |
| --- | --- | --- |
| `docker images` | 1.72 GB | **1.20 GB** |
| `COPY / /` layer | 1.34 GB | **944 MB** |
| `/app` | 1.25 GiB (≈1280 MiB) | **748 MiB** (765476 KiB) |
| `/app/dist` | 270 MiB | **absent** |
| `/app/apps` | desktop 28 + server 19.5 + cli 9.7 | **server 19.5 only** |
| `/app/src` | 43 MiB | 43 MiB (not excluded, E3 E1) |
| `/app/packages` | 42 MiB | 42 MiB (not excluded) |
| `/app/.next` | — | 201 MiB |
| `/app/node_modules` | 268 MiB | 258 MiB (`@vitejs/devtools` gone; ffmpeg kept; canvas ×3 kept) |
| `*.tsbuildinfo` / e2e / tests / changelog / docker-compose | present | **absent** |

First build (`c27b574f8a35`, 1.19 GB) used `./dist/**` and **broke** app routes (`app-route-turbo.runtime.prod.js` + `es-toolkit/dist` dropped: Next `makeIgnoreFn` is picomatch `{contains:true}`, and `.` is "any char"). Rebuilt with explicit `dist/{desktop,mobile,auth}/**`. Do **not** revert to a bare `dist/**`.

## Compose usage matrix (verified `docker compose config --services`)

| Command | Services |
| --- | --- |
| `docker compose up -d` (`COMPOSE_PROFILES=redis,s3,search`) | lobehub + postgres + lkg-init + redis + rustfs + rustfs-init + searxng |
| `COMPOSE_PROFILES=redis,s3 docker compose up -d` **or** `COMPOSE_PROFILES= docker compose --profile redis --profile s3 up -d` | above minus searxng |
| `COMPOSE_PROFILES= docker compose up -d` | lobehub + postgres + lkg-init |
| empty `S3_ENDPOINT` + minimal profiles | interpolates (no `:?` fail). App: `file.ts` S3 optional; `enableUploadFileToServer` false without secret. |

## Boot (perf DB, rebuilt image)

```
docker run --rm -d --name aihub-slim-g5 --network aihub-dev_aihub-dev
  --env-file AIHub/.env.development --env-file demo/.env.platform
  -e DATABASE_URL=...lobechat_perf -e REDIS_URL=.../3 -e PORT=3210 -p 127.0.0.1:3021:3210
  -e LOBE_NODE_HEAP_MB=1536 …ENABLE_*… aihub:slim-g5
```

- Migration: `✅ database migration pass.`
- `/` → **302** `/signin`; `/admin` → **302** `/signin`; `/signin` → **200**
- Gateway: `Started successfully`
- Child `next-server` pid 28 environ:
  `NODE_OPTIONS=--dns-result-order=ipv4first --use-openssl-ca --max-old-space-size=1536`
- IdP LKG snapshot errors are pre-existing (no `platform-oidc-lkg` volume). QStash skipped (no token).
- Container removed after checks.

`require.resolve` in the image: `@napi-rs/canvas`, `canvas-linux-arm64-gnu`, `sharp`, `ffmpeg-static` all resolve.

## Tests

`bun run .agents/scripts/check/cli.ts --lint --test` on G5 TS files: **lint clean, 3 passed**.
`startServer.js` pre-existing CJS lint noise (require / regex groups) — not reformatted.
`bun run check` branding gate fails on unrelated `apps/cli/pnpm-lock.yaml` (not G5).

## Could not / did not

- **G0** never arrived; item 1 finalized from E3 + the real `next build`.
- **`src/` / `packages/` / `apps/server`** still in the image (E3 E1). Do not exclude.
- **Canvas ×3 / sharp-libvips ×2**: hardlink does not survive scratch `COPY / /`. Deleting .pnpm skia copies unsafe (`js-binding.js` `require('@napi-rs/canvas-linux-*')` from each nested canvas). Two sharp versions are different SONAMEs.
- **Layer split** of `COPY / /` skipped (not trivial from scratch).
- **`WITH_VIDEO=0` image** not built (default 1; prune path is in the Dockerfile).
- **SKIP_DB_MIGRATION / ENABLE_BOT_GATEWAY=0** not live-booted (code paths only).
- Commander: stitch G1/F1 markers in `docs/enterprise/modules.md`; do not commit a `dist/**` exclude.

## Ownership notes

Did not touch G1/G2/G3/G4/F1 files. `ENABLE_BOT_GATEWAY=0` is launcher-only; G2 still no-ops the handler when `bots` is off.

---

# Round 2 (review rework)

Adjudication applied: F1 heap unset = no cap; F2 redis/s3 always-on, searxng-only profile, `docker-compose.minimal.yml`; F3/F6 `dockerTracingExcludes()`; F4 dropped `WITH_VIDEO` (kept one-line `@vitejs/devtools` prune); F5/F7 docs.

## Size table (`aihub:slim-r2` `27dbabf79219`, whole slim tree + commander `platform.ts` literals)

| | E3 demo | G5 r1 (`slim-g5`) | G5 r2 |
| --- | --- | --- | --- |
| image | 1.72 GB | 1.20 GB | **1.07 GB** |
| `COPY / /` | 1.34 GB | 944 MB | **824 MB** |
| `/app` | 1280 MiB | 748 MiB | **634 MiB** (648708 KiB) |
| `/app/src` | 43 MiB | 43 MiB | **80 KiB** (4 leftover `*runtime*` files) |
| `/app/packages` | 42 MiB | 42 MiB | **20 MiB** (database 11.5 + model-runtime 6.5) |
| `/app/apps/server` | 19.5 MiB | 19.5 MiB | **1.0 MiB** |
| `/app/dist` | 270 MiB | absent | **7.4 MiB** leftover `*runtime*` vite chunks |
| next-server runtimes | complete | complete | complete (`app-route-turbo` present) |
| `@vitejs/devtools` | ~9.5 MiB | gone | gone |

No `Dynamic filesystem access causes tracing of the whole project` lines in the r2 `next build` log. The commander `platform.ts` literal-segment fix is what collapsed `/app/src` (43 MiB → 80 KiB). Remaining `src/` files: `runtimeState.ts`, `runtimeErrorMessage.ts`, `runtimeBranding.ts`, `runtimePresentation.ts` — do **not** add `src/**` to excludes. No remaining whole-project-trace warning to file:line.

## Compose matrix (verified `docker compose config --services`)

| Command | Services |
| --- | --- |
| `docker compose up -d` (no `COMPOSE_PROFILES`) | lobehub + postgres + lkg-init + redis + rustfs + rustfs-init |
| `docker compose --profile search up -d` | above + searxng |
| `docker compose -f docker-compose.minimal.yml up -d` | lobehub + postgres + lkg-init |

Upgrade path: old `.env` without `COMPOSE_PROFILES` still gets redis + rustfs.

## Boot (`aihub:slim-r2`, perf DB, `LOBE_NODE_HEAP_MB=1536`)

- Migration pass. Gateway started.
- `/` 302, `/admin` 302, `/signin` 200.
- `GET /trpc/lambda/healthcheck` → **200** `{"result":{"data":{"json":"i'm live!"}}}`
- `GET /trpc/lambda/platform.getPublicSnapshot` → **200** (not G0's 500). POST → 405 (query procedure; expected).
- Child `NODE_OPTIONS=--dns-result-order=ipv4first --use-openssl-ca --max-old-space-size=1536`
- `docker exec ls /app/src` → `enterprise/ store/ utils/` (80 KiB)
- Pre-existing IdP LKG noise. One G2 worker: `aiCatalogReadiness` `ReferenceError` (not a tracing 500).
- Container removed.

`startServer.js` no longer defaults the heap. Compose `environment` still has `LOBE_NODE_HEAP_MB=${LOBE_NODE_HEAP_MB:-1536}`.

# G1 — lazy server-runtime registry + first-chat graph

## Files
- `apps/server/src/services/toolExecution/serverRuntimes/index.ts` — 32 static `./x` imports → per-identifier lazy factories
- `apps/server/src/services/toolExecution/serverRuntimes/__tests__/registry.test.ts` — new
- `apps/server/src/services/toolExecution/serverRuntimes/calculator.ts` — ExecutionRuntime from `./executionRuntime`
- `apps/server/src/services/toolExecution/serverRuntimes/task.ts` — lazy `taskRouter`
- `apps/server/src/services/aiAgent/index.ts` — one-path `await import()` (not G3 persona/settings)
- `apps/server/src/routers/lambda/aiAgent.ts` — drop unused eager Hetero ctor; lazy at ingest/finish
- `packages/builtin-tool-calculator/src/index.ts` + `package.json` — root no longer re-exports ExecutionRuntime; added `./executionRuntime`

Public API unchanged: `getServerRuntime` (async), `hasServerRuntime`, `getServerRuntimeIdentifiers`. Identifiers from light `@lobechat/builtin-tool-*` roots. Gate (`assertToolModuleEnabled`) still runs **before** `import()`. `Map<id, Promise<factory>>` shares the first load. `has`/`list` stay eager from the static registration list.

## Seams (file:line → what)
| seam | what dropped from first-chat static graph |
|---|---|
| `serverRuntimes/index.ts` all 32 `lazyRuntime(...)` | every `./x` + executionRuntime + SearchService / discord.js / editor / OTel-via-memory / … |
| `builtin-tool-calculator/src/index.ts:1` (E1 #3) | mathjs 16.8M + nerdamer 3.1M + top-level `create(all)` |
| `aiAgent/index.ts:1888,5096` (E1 #5) | `@lobechat/heterogeneous-agents` (119k, only graph edge) |
| `aiAgent/index.ts:1936` + `lambda/aiAgent.ts:1341,1399` (E1 #1) | `HeterogeneousAgentService` + agent-tracing/`gpt-tokenizer` 55.9M |
| `aiAgent/index.ts:2009,2328` | cloud/remote hetero context builders |
| `aiAgent/index.ts:1007` (E1 #9) | `enterprise/services/agentCatalog` (186k) — platform-agent path only |
| `aiAgent/index.ts:3403` | `generateSystemPrompt` (remote-device path) |
| `aiAgent/index.ts:3743` | `buildTaskManagerDefaultsPrompt` (task-scope path) |
| `serverRuntimes/task.ts:711` (E1 #10) | `routers/lambda/task` — no longer defeats phase-1 lazy routers |

Not converted (every first chat, or not owned): model-runtime barrel (`AgentRuntimeService`), skillCatalog / agentSignal / Composio+Market ctors, `DocumentService`→canvas.

## Static reachable bytes (`serverRuntimes/index.ts` root, E1 scanner, type-only edges skipped)
| | files | bytes |
|---|---|---|
| before | 2192 | 13 452 686 |
| after | 560 | 2 188 311 |
| **Δ** | **−1632** | **−11 264 375 (~11.3 MB source)** |

After set is identifier roots + `toolModuleGate` + types/const/prompts/database pulled by those roots. mathjs/nerdamer/hetero/editor no longer appear. Commander still measures Docker RSS.

## Tests
- `apps/server/src/services/toolExecution` — **28 passed / 1 failed** (296 tests). Fail = `serverRuntimes/__tests__/memory.test.ts` **same as main** (`@/database/schemas` mock missing `topics`).
- `registry.test.ts` — 5 passed: static list (32 ids), async factory, calculator instantiates, unknown → `undefined`, gate-before-import, concurrent singleton.
- `task.test.ts` — 25 passed.
- `apps/server/src/services/aiAgent` — 24 files / 243 passed; 12 failed in `execAgentPlatform` / `resumeAnchor.integration` / `execAgent.modelOverride` (`PLATFORM_AGENT_*` code-as-message, `PLATFORM_MASTER_KEY`). Resolver unit tests already expect the code strings — **G3/enterprise drift, not this registry**. Hetero + memory execAgent tests passed.
- Lint: `check/cli.ts --lint` on G1 files **clean**. `bun run check --lint` dies on pre-existing `apps/cli/pnpm-lock.yaml` branding.

## Consumers
`getServerRuntime` already awaited in `builtin.ts`. No sync factory consumer. `hasServerRuntime` remains sync (map membership).

## G3 region
Did **not** edit persona/user-settings reads. G3 landed `getLatestPersonaDocumentMemo` at `aiAgent/index.ts:3614` while this ran; left it untouched.

## Commander
- RSS on real image after first chat (expect −30 MB if memory/editor/mathjs/gpt-tokenizer stay off the path).
- Do not commit `memory.test.ts` “fix” here; known red.

## Round 2 (REVIEW_G1)

**#1 BLOCKER — `./manifest` subpaths, roots not reshaped.** Added `"./manifest": "./src/manifest.ts"` to 29 `builtin-tool-*` packages. Identifiers re-exported from existing `manifest.ts` (or new `builtin-tool-agent-signal/src/manifest.ts` → `shared/identifiers` only). `serverRuntimes/index.ts`, `aiAgent/index.ts` (~10–21), `toolModuleGate.ts`, `deviceToolRegistry.ts` now import `@lobechat/builtin-tool-x/manifest`. remote-device: `DeviceAttachment` moved to `src/types.ts` so `./manifest` + `./systemRole` no longer touch ExecutionRuntime; `generateSystemPrompt` lazy-imports `…/systemRole`. self-iteration inject comes from `./inject`. `packages/builtin-tools/src/index.ts` untouched. `aiAgent` also `await import('@lobechat/builtin-tools')` at its two use sites.

Scanner (`serverRuntimes/index.ts` root, type-only skipped):

| | files | bytes | ExecutionRuntime/client-exec |
|---|---|---|---|
| R1 after | 560 | 2 188 311 | still present via leaking roots |
| R2 | **500** | **2 040 289** | **0** |
| **Δ R1→R2** | **−60** | **−148 022** | |

`aiAgent/index.ts` root still shows ExecutionRuntime via **unowned** edges: `deviceToolRegistry` → `@lobechat/builtin-tools` catalogue (index.ts left as required), `Mecha` → same helper, `builtin-agents` inbox value-imports agent-documents/user-interaction roots, `deviceGateway` `import { type DeviceAttachment }` (scanner treats inline `type` as value).

**#2 BLOCKER — task.ts.** Dropped `TaskCaller` shim + casts. `import type { taskRouter } from '@/server/routers/lambda/task'` + `ReturnType<typeof taskRouter.createCaller>`. Only runtime change remains `await import()`.

**#3 MAJOR — registry.test.ts.** All 32 `./x` modules mocked. Every static identifier `await getServerRuntime` → `{ id }`. Concurrent first-init uses deferred `./brief` mock: one init, same singleton.

### Tests (R2)
- `toolExecution`: 28 passed / 1 failed (`memory.test.ts` same known red). `registry.test.ts` 4 passed. `task.test.ts` 25 passed.
- `aiAgent`: 22 files / 237 passed; remaining fails = G3 `PLATFORM_AGENT_*` message drift + `PLATFORM_MASTER_KEY` + PGlite hook timeouts under load. `deviceToolRegistry.test.ts` + hetero passed.
- Lint: `check/cli.ts --lint` on R2 files clean.
- `bunx tsgo --noEmit -p tsconfig.json`: 4 errors, **none in G1 files** (baseline `mdx/Image.tsx:34`; plus `platform.job.test.ts` ×2 and `LazyBrandIcon` — not ours).

## Round 3 (REVIEW_G1_round2)

Restored `packages/builtin-tool-remote-device/src/{types,ExecutionRuntime/types}.ts` to HEAD (`git checkout --`). Kept `./manifest` + `./systemRole`. `DeviceAttachment` is now `import type { DeviceAttachment } from './ExecutionRuntime/types'` in `types.ts` and `systemRole.ts` (erased; no runtime edge).

Scanner from `serverRuntimes/index.ts`: **500 files / 2 039 300 bytes**; remote-device files on the graph = `manifest.ts` + `systemRole.ts` + `types.ts` only — **ExecutionRuntime not reachable**.

`bun run check --lint` on the three files: clean. `toolExecution`: 28 passed / 1 failed (`memory.test.ts` same known red).

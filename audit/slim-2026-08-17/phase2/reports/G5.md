# G5 — P5 image layering + native-dep convergence

Worktree `/Users/konata/code/AIHub-worktrees/slim2`. Images: `aihub:p5-1` / `aihub:p5-2` (967 MB). `aihub:p5-0` is the discarded busybox-explosion experiment (1.76 GB).

## Diff

| File | Change |
| --- | --- |
| `Dockerfile` | Final scratch: two `COPY --from=app` (`/layer-a` then `/layer-b`). App-stage last RUN assembles them. `/layer-a` = `/bin/node` + `/bin/proxychains` + `/lib` + `/lib64` + `/usr` (curl-impersonate) + `/etc` + `/app/node_modules`. `/layer-b` = `/app` minus `node_modules`. Prune `.pnpm/**/skia*.node`. Four-stage shape / base / curl-impersonate / `serverExternalPackages` / `dockerCanvasTracingIncludes` untouched. |
| `src/libs/next/config/dockerTracingExcludes.ts` (+ test) | Exclude the two `.pnpm` `skia*.node` trees; hoisted `node_modules/@napi-rs/canvas-linux-*` stays. |
| `apps/server/src/enterprise/guards/ffmpegStatic.ts` (+ test) | `isBootModuleEnabled('imageGen')` then `await import('ffmpeg-static')`. |
| `apps/server/src/services/generation/video.ts` | `getFfmpegPath` → `await resolveFfmpegStatic()` (two call sites already async). |
| `apps/server/src/enterprise/guards/index.ts` | Re-export. |
| `docs/enterprise/modules.md` | Image section: two layers, one skia, ffmpeg gate, sharp ×2 stays. |

`bun run .agents/scripts/check/cli.ts --lint --test` on G5 TS: lint clean, 3 passed.

## Layers

| | phase-1 `aihub:slim-final3` | p5-0 (full `/bin` COPY) | **p5-1 / p5-2** |
| --- | --- | --- | --- |
| `docker images` | 1.10 GB | 1.76 GB | **967 MB** |
| final COPY | 846 MB ×1 | 862 + 404 | **374 + 382** |
| `/app` | ~603 MiB | — | **518 MiB** |
| `/app/node_modules` | 227 MiB | — | **174 MiB** |
| `/bin` | busybox applets + node (hardlinked) | applets exploded, 414 inodes | **node + proxychains only** |

p5-2 (comment-only touch in `ffmpegStatic.ts`):

```
#35 COPY --from=app /layer-a /   CACHED
#36 COPY --from=app /layer-b /   rebuilt
```

RootFS layer 0 identical: `sha256:5f118127377ce0d0a91349fc6da744f6140a6f017c17a68131901c4cc3fb56db`. Layer 1 changed (`1f33e495…` → `a572a48d…`). BuildKit content-hash of `/layer-a` is stable across a source-only rebuild even when `COPY packages` / `pnpm i` / `next build` reran (other agents touched `packages/`).

**Busybox pitfall:** `cp -al /bin` + `COPY /layer-a` exploded 400 applets (nlink 1, +~450 MiB). A tiny busybox-only Dockerfile preserves hardlinks; the real app snapshot does not (overlay copy-up after `COPY --from=base /distroless/ /`). Do not put the full busybox `/bin` in layer-a. Entrypoint is `/bin/node`; `startServer.js` only needs `/bin/proxychains` when `PROXY_URL` is set.

## Natives

**Canvas** — three 26.43 MiB `skia.linux-arm64-gnu.node` (distinct inodes) in `slim-final3`:

1. hoisted `node_modules/@napi-rs/canvas-linux-arm64-gnu/` ← **`require()` target**
2. `.pnpm/@napi-rs+canvas@0.1.100/node_modules/@napi-rs/canvas-linux-arm64-gnu/`
3. `.pnpm/@napi-rs+canvas-linux-arm64-gnu@0.1.100/…`

`js-binding.js` does `require('@napi-rs/canvas-linux-arm64-gnu')` and walks up to (1). Deleted (2)+(3) via exclude + prune. **−52.9 MiB. One copy left. `require('@napi-rs/canvas')` + `createCanvas(2,2).toBuffer('image/png')` OK.**

**sharp-libvips** — **both stay**. `require('sharp')` → `sharp@0.34.5` + libvips **1.2.4** (16.0 MiB). Next 16.3.0 depends on `sharp@0.35.3` + libvips **1.3.2** (17.0 MiB, SONAME 8.18.3). Lockfile change not done (last resort; would be upstream-heavy and would break Next image opt).

**ffmpeg-static** — still in the image (48.8 MiB). `serverExternalPackages` lists it; phase 1 `WITH_VIDEO` prune → `MODULE_NOT_FOUND`. Gate only. Default full boot still resolves the path.

## Other >10 MB

| Item | Size | Action |
| --- | --- | --- |
| `.pnpm` extra skia ×2 | 52.9 MiB | **removed** |
| busybox applets | ~1 MiB hardlinked / 450 if exploded | **not shipped** |
| `ffmpeg-static` | 48.8 MiB | kept (gated) |
| sharp-libvips 1.2.4 + 1.3.2 | 33 MiB | kept (two SONAMEs) |
| `locales/` | 23.3 MiB | kept (E3 D5) |
| `packages/` remnants | 18.9 MiB | kept (E3 D9) |
| `public/_spa` `es-*.js` | 14.8 MiB | F4 |
| `*.map` (1853) | 10.2 MiB | left — not proven unused |
| leftover `dist/*runtime*` | 7.3 MiB | already in excludes; still traced |
| `.d.ts` / test fixtures | 2.3 / 3.5 | left (<10, not grepped out of chunk graph) |
| `@larksuiteoapi/node-sdk` | 5.9 MiB | bots |

No bare `dist/**`. No lockfile edit.

## Runtime (`aihub:p5-2`, net `aihub-dev_aihub-dev`, DB `lobechat_perf`, Redis /3, `AUTH_COOKIE_PREFIX=aihub-perf`, `LOBE_NODE_HEAP_MB=1536`; container `aihub-slim-p5` removed)

- Migration pass. `✓ Ready`. Gateway started.
- `GET /` 302 `/signin`; `/signin` 200; `/api/version` 200.
- `GET /api/healthcheck` **404** (no such route). `GET /trpc/lambda/healthcheck` **200** `{"result":{"data":{"json":"i'm live!"}}}`.
- `GET /trpc/lambda/platform.getPublicSnapshot` **200**.
- `docker exec -w /app … require('@napi-rs/canvas')` + `require('sharp')` + `require('ffmpeg-static')` OK; canvas PNG buffer produced.

## Commander

- Docs image paragraph already updated. Do **not** restore `WITH_VIDEO`. Do **not** `pnpm dedupe` the lockfile for sharp.
- Safe to `docker rmi aihub:p5-0`. Keep `aihub:p5-2` for the next demo tag if wanted.
- Another session still owns the Dockerfile **base** stage; this diff is app-stage tail + final two `COPY`s only.

---

## Round 2 — top-level parity (`aihub:p5-3`)

Commander catch: layer-a had omitted `/tmp` (1777), `/opt` (future `cursor-agent`), `/var`/`/home`/`/root`/…. Assembly is now `for p in /* /.[!.]*` minus `/app` `/bin` `/proc` `/sys` `/dev` `/layer-a` `/layer-b`, then `/bin/{node,proxychains}` only, `mkdir -m 1777 -p /layer-a/tmp && chmod 1777`, `cp -al` node_modules, `chown nextjs:nodejs` the two `/app` roots.

First rebuild failed on G1 mid-edit (`packages/builtin-tool-agent-management` duplicated `AgentManagementIdentifier` export). Retried after their file settled; did not touch their tree.

### Export parity (`docker create` + `docker export | tar`, vs `aihub:slim-final3`)

Top-level names **identical** (`app bin dev dockerenv etc home lib lib64 proc sys root tmp usr var`). Mode/owner **identical**, including `/app` `0755 1001:1001` and `/tmp` `1777 0:0`. `stat` in-image: `tmp 1777`.

Allowed diffs only:

| | slim-final3 | p5-3 |
| --- | --- | --- |
| `/bin` entries | 414 (busybox applets + node + proxychains) | **2** (`node`, `proxychains`) — 412 applets dropped |
| `skia*.node` | 3 copies | **1** (hoisted); two `.pnpm` copies gone |
| `/opt` | absent | absent (loop will copy it when the base-stage cursor-agent lands) |

### Runtime (`docker exec -u nextjs -w /app`, uid 1001)

- `fs.mkdtempSync(os.tmpdir()+'/x')` → `/tmp/xOLnIWC` OK (`os.tmpdir()` is `/tmp`, mode 1777).
- `require('@napi-rs/canvas')` + PNG buffer, `require('sharp')`, `require('ffmpeg-static')` OK.
- `fs.accessSync('/usr/local/bin/curl-impersonate', X_OK)` OK.

### Cache

Comment-only rebuild: `#35 COPY --from=app /layer-a /` **CACHED**. Layer-a digest unchanged `sha256:d31895f912c786e2320e76be06bee1a25efc747b1996016874881cde9991bf70`. Layer-b changed (`0265040c…` → `6c162d02…`). Image still **967 MB** (374 + 382).

Proof container removed. Tag `aihub:p5-3`.

---

## Round 3 — BusyBox applets as symlinks (`aihub:p5-4`)

Codex BLOCKER: `child_process.exec` needs `/bin/sh`; MCP/`which`/`grep` need applets. `cp -al /bin` explodes hardlinks (+450 MiB). Fix: copy one `/bin/busybox`, then `ln -s busybox` for every `busybox --list` name (skip existing `node`/`proxychains`). Also copy `/bin/getconf` (separate binary in busybox:latest, not an applet) so `/bin` names match 414/414.

### Size / layers

| | p5-3 | **p5-4** |
| --- | --- | --- |
| image | 967 MB | **969 MB** (+busybox 1.1 MiB; symlinks ~0) |
| layer-a / layer-b | 374 / 382 | **375 / 382** |

### Export vs `slim-final3`

Top-level names/modes/owners identical. `/bin` **414 = 414**, no name diffs. `/tmp` 1777 both. **Only remaining content diff: two pruned `.pnpm` `skia.node` copies.** `/bin/sh` is a symlink → `busybox`.

### Runtime (`-u nextjs`)

- `exec('node --version && which grep && sh -c "echo ok"')` → `null`, `v24.19.0\n/bin/grep\nok\n`.
- `mkdtemp(/tmp/x…)` OK; canvas PNG / sharp / ffmpeg-static / `curl-impersonate` X_OK OK.

### Cache

Comment-only rebuild: `#35 COPY /layer-a` **CACHED**. Layer-a digest `sha256:7b2b04ebf908decd705971f2a055fb39513d8e27f5b1d173d54c92d986dbb07a` unchanged; layer-b `11c1de2d…` → `964f3cac…`.

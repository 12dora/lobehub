# G4 — first-chat graph seams (part 2)

Scanner: E1 `lib.ts` / `build()`, `SNAP_REPO=slim2`. Roots = `aiAgent` router + service + `serverRuntimes` + `AgentRuntime`. Live tree already had G1 lazy cuts (E1 HEAD was 2208f / 13405k / 113 ext).

| | files | source | externals |
|---|---:|---:|---:|
| live before G4 | 2019 | 11752k | 101 |
| live after G4 | 1972 | 11488k | 97 |
| **G4 delta** | **−47** | **−264k** | **−4** (`gpt-tokenizer`, `fast-glob`, `execa`, `fflate`) |

## Seam 1 — `agent-tracing` viewer (E1 #1) — DONE

`packages/agent-tracing/src/index.ts` no longer re-exports `./viewer` (dropped L13–20). `package.json` adds `"./viewer": "./src/viewer/index.ts"`. CLI already imports `../viewer` / `../analysis/toolFeedback`; `toolFeedback` (gpt-tokenizer) was never on the root barrel. No `@lobechat/agent-tracing` consumer used the six render/analyze fns; `llm-generation-tracing` has its own viewer. `heterogeneousAgent/index.ts` already imports only `parseOperationId` / `ISnapshotStore` — **not edited**.

Ablation of `./viewer` before the cut: **−46k / −2f / −gpt-tokenizer**. After: viewer + `agentSignal.ts` gone from the first-chat graph; remaining 5 files are recorder/store/reconstruct. `gpt-tokenizer` (55.9 MB n_m) is no longer a static first-chat external.

## Seam 2 — `extractExecutor` OTel `/node` (E1 #2) — DONE (first-tool-call)

`/node` is **not** a thin wrapper: `node.ts` top-level-imports NodeSDK + auto-instrumentations + OTLP exporters (~4.9 MB / 7 pkgs). `/modules/memory-user-memory` is only `@opentelemetry/api` — left static, same as `SpanStatusCode` from `/api`.

`packages/memory-user-memory/src/services/extractExecutor.ts`: dropped static `/node` import; `attributesWithCommon` does `await import('@lobechat/observability-otel/node')` at the three span/metrics call sites. No other sibling in this package imported `/node`.

First-chat delta: **0** (G1 already lazy’d `./memory`; extractExecutor is off the chat graph). Own-root (`extractExecutor.ts`): 537f / 2913k / 33 ext → **536f / 2909k / 24 ext** (−`node.ts`, −8 OTel SDK pkgs). First memory-tool *call* still loads `/node` when a span is recorded.

## Seam 3 — `parseSSOProviders` (E1 #4) — SKIPPED

`src/libs/better-auth/utils/server.ts` is already a pure string splitter (no imports). `globalConfig/index.ts:13` already imports that module. Ablating it: **0f / 0k**. The 41-file better-auth subtree (nodemailer / resend / bcryptjs / `@better-auth/*`) is pulled by tRPC context:

`aiAgent.ts:14 → rbacPermission → trpc/lambda/init → context:5 → src/auth.ts → define-config.ts`

Not G4-owned. Left `globalConfig` (and its phase-1 memo) untouched.

## Seam 4 — `device-control` (E1 #6) — DONE

`apps/server/src/enterprise/services/skillCatalog/validator.ts:1`: `@lobechat/device-control` → `@lobechat/device-control/inlineSkillResources` (subpath already existed; file is a pure path checker, no deps). Only in-edge to `device-control` on the first-chat graph.

Ablation of the root import: **−224k / −46f / −fast-glob,fflate,execa**. After the subpath: 1 file remains (`inlineSkillResources.ts`); `local-file-shell` 36→0. Matches the −45f / ~218k share of the live −264k.

## Tests / lint

- `packages/agent-tracing` (`reconstruct` + `agentSignal`, node config): **16 passed**
- `packages/memory-user-memory` extractors (no extractExecutor test exists): **25 passed**
- `skillCatalog/validator.test.ts`: **25 passed**
- `globalConfig` tests (unchanged): **8 passed**
- `bun run .agents/scripts/check/cli.ts --lint` on the 4 files: clean (prettier also rewrapped 5 pre-existing unions in extractExecutor)
- `bun run check` itself is red on `enterprise:check-branding` (unrelated `apps/cli/pnpm-lock.yaml` + `moduleSettings`) — did not use that wrapper
- Repo-wide `tsgo` skipped (COMMON_RULES; commander runs it)

## Upstream files (mechanical)

| file | change |
|---|---|
| `packages/agent-tracing/src/index.ts` | delete viewer re-export block |
| `packages/agent-tracing/package.json` | add `./viewer` export |
| `packages/memory-user-memory/src/services/extractExecutor.ts` | static `/node` → `await import()` |
| `apps/server/src/enterprise/services/skillCatalog/validator.ts` | one-line subpath (fork) |

Not touched: `heterogeneousAgent`, `globalConfig`, `llm-generation-tracing`, G1 trees.

## Commander

- Auth graph still on every tRPC chat request via `src/auth.ts` — biggest remaining G4-adjacent cost, not in this ownership set.
- Viewer consumers should import `@lobechat/agent-tracing/viewer` going forward (none in-tree today).

/**
 * Stable machine reason codes persisted in the audit trail for managed-resource
 * nested controls. Localized at render time via `users.audit.autoReason.*`.
 */
export const SHARED_OAUTH_AUTO_REASON = 'admin.connectors.shared_oauth';

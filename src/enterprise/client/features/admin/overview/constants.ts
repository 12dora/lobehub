/** Default lookback window for overview KPIs (aligned with totals activeDays). */
export const OVERVIEW_WINDOW_DAYS = 30;

/** How many rank rows to show in the overview summary cards. */
export const OVERVIEW_RANK_LIMIT = 5;

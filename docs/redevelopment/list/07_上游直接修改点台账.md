# 上游直接修改点台账

<!-- prettier-ignore -->
| 上游文件 / 区域 | 修改目的 | 模块 | 冲突风险 | 控制方式 |
| --- | --- | --- | --- | --- |
| `src/business/client/BusinessDesktopRoutes.tsx` | 注册 Enterprise Admin Route 数组 | M00/M03 | 低 | 保持单行 import/export；路由实现放新增目录。M00 PR-003：`...EnterpriseDesktopRoutesWithoutMainLayout`（默认空数组，Flag 关闭零路径） |
| `src/business/client/BusinessGlobalProvider.tsx` | 挂 Runtime Branding/Platform Provider | M00/M12 | 低 | 只包装 Provider。M00 PR-003：包装 `EnterprisePlatformProvider`，默认能力快照为 disabled |
| `apps/server/src/routers/lambda/index.ts` | 挂 admin/platform Router | M00 | 中 | 只增加 import 和键。M00 PR-003：`platform: platformRouter`（只读 getCapabilities/getPublicSnapshot） |
| `apps/server/src/globalConfig/index.ts` | Global Config 注入 enterprise.enabled | M00 | 低 | 第 4 挂载点。`serverConfig.enterprise: { enabled }` 门控客户端 platform.\*；Flag 全关时 enabled=false，冷启动零 platform 请求 |
| `packages/types/src/serverConfig.ts` | GlobalServerConfig.enterprise 类型 | M00 | 低 | 仅增加可选 `enterprise?: { enabled: boolean }`，不泄露 Flag 名 / Secret |
| `package.json` | `enterprise:check-paths` 脚本 | M00 | 低 | PR-004：路径边界 CI 入口 |
| `packages/database/src/schemas/index.ts` | 导出 platform Schema | M01 | 低 | 新增单行 `export * from './platform'`；实现位于 `schemas/platform/` |
| `packages/database/src/utils/idGenerator.ts` | 增加 platform\_\* 表 id 前缀命名空间 | M01 | 低 | 仅追加 prefixes 键，不改现有命名空间 |
| `.prettierignore` | 忽略 drizzle `migrations/meta/` 生成物，防 prettier 重排造成迁移快照噪声 | M01 | 低 | 仅追加忽略路径 |
| `packages/const/src/rbac.ts` | 增加 platform 权限和 scope 规则 | M02 | 中 | **已实施 PR-009**：`isPlatformPermissionCode` + SYSTEM\_DEFAULT\_ROLES 扩展；既有 workspace 策略回归测试 |
| `packages/database/src/models/rbac.ts` | 严格 Global Query / 按作用域替换 | M02 | 中 | **已实施 PR-009**：`hasGlobalPermission` / `replaceGlobalUserRoles` / last-super-admin helpers；`updateUserRoles` 标注 deprecated |
| `packages/business-server/src/trpc-middlewares/rbacPermission.ts` | 企业 Flag 下真实实现或转接 | M02 | 高 | **已实施 PR-013**：保持 export shape；workspace 仍 no-op；平台鉴权走 `withPlatformPermission` |
| `apps/server/src/routers/lambda/index.ts` | 挂 admin Router | M02 | 中 | **已实施**：`admin: adminRouter`（auth/roles/easyauth/audit） |
| `packages/database/src/utils/idGenerator.ts` | EasyAuth snapshot id 前缀 | M02 | 低 | **已实施 PR-013A**：`platformEasyauthGrantSnapshots` |
| `src/app/(backend)/.well-known/easyauth-app.json/route.ts` | EasyAuth 描述符 HTTP | M02 | 低 | **已实施 PR-013A**：仅依赖 `@/const/platform/*` |
| `packages/trpc/src/lambda/index.ts` | 全局 `authedProcedure` 挂 `enterpriseAccessGate` | M02 | 高 | **已实施 B3**：所有业务 lambda router 共享该 procedure；Flag 关放行，未准入返回 `PLATFORM_ACCESS_NOT_GRANTED`；改动全仓 authed 基础 |
| `src/libs/better-auth/define-config.ts` | `session.create.after` 触发 EasyAuth 登录同步 | M02 | 高 | **已实施 B3**：`syncEasyauthOnLogin`；Flag 关 return；try/catch 不阻塞登录（与 M11 OIDC Snapshot 加载共存同文件） |
| `src/business/client/BusinessMobileRoutes.tsx` | 注册 Flag-gated `/admin` 移动端不支持面 | M03 | 低 | **已实施 PR-014**：Flag 关闭时路由数组为空；不加载桌面表格 |
| `apps/server/src/globalConfig/index.ts` | 公开 `enterprise.platformAdmin` 能力位 | M03 | 低 | **已实施 PR-014**：仅公开 Feature existence，不公开角色 / 权限 |
| `packages/types/src/serverConfig.ts` | `platformAdmin?: boolean` 类型 | M03 | 低 | **已实施 PR-014**：保持可选字段兼容旧配置 |
| `src/routes/(main)/settings/stats/features/components/StatsFormGroup.tsx` | 兼容 adapter 转到共享 `SectionGroup` | M03 | 低 | **已实施 PR-017**：旧 Settings caller 签名不变 |
| `packages/database/src/schemas/user.ts` | 增加 auth invalidation epoch 与 retained-session id | M04 | 高 | **已实施 PR-018–021**：Migration 0119；列可空，Flag 关闭不改变上游行为 |
| `packages/database/src/models/rbac.ts` | M04 全局角色替换 / 最后超管事务语义加固 | M04 | 高 | 只扩展 global-role 安全路径；workspace role 回归测试守护 |
| `packages/trpc/src/lambda/context.ts` | 每次认证校验封禁与安全 epoch，可信 reauth metadata | M04 | 高 | focused tests 覆盖当前 Session 保留例外、过期 / 伪造 exception fail-closed |
| `src/app/(backend)/api/auth/[...all]/route.ts` | Better Auth 入口阻断已封禁 / 失效会话 | M04 | 高 | Feature Flag 门控；管理插件 mutation 另由 enterprise security block |
| `src/features/Auth/SignIn/useSignIn.ts` | 管理 reauth 模式向 OIDC 追加 `prompt=login,max_age=0` | M04 | 高 | 仅 `reauth=1` 注入；普通登录对照测试保证不变 |
| `src/libs/better-auth/sso/index.ts` | 将 reauth 参数传入 Authentik/Generic OIDC authorization params | M04 | 高 | 不信任客户端 authMethod；服务端可信方法决定 reauth 流程 |
| `src/libs/oidc-provider/access-control.ts` | OIDC session/auth\_time 与平台失效 epoch 联动 | M04 | 高 | credential issued time 与 authenticated time 分离；现有 token/session fail-closed |
| `docs/development/database-schema.dbml` | 记录 M04 users 增量列 | M04 | 低 | 与 0119 schema 保持一致 |
| `packages/locales/src/default/admin.ts`、`locales/{en-US,zh-CN}/admin.json` | 用户管理与 reauth 文案 | M04 | 低 | 手写 en-US/zh-CN；未运行全量 i18n，其他 locale 留后续生成 |
| `apps/server/src/routers/lambda/user.ts` | Effective Settings / Override / 旧更新兼容 | M05 | 高 | **已实施 PR-024–025**：Flag 关闭保持旧行为；keyVaults 不进入平台 Override |
| `src/routes/(main)/settings/{memory,about}/**` | 挂设置来源与 locked/hidden 控件语义 | M05 | 中 | **已实施 PR-026**：复用 `PlatformSettingSourceBadge`；服务端仍为安全边界 |
| `apps/server/src/routers/lambda/{aiProvider,aiModel,agentSkills,connector,agent,agentGroup,composio,home,oauthDeviceFlow,agentDocument}.ts` | 添加 Managed Resource Guard 与窄豁免 | M06 | 高 | **已实施 PR-028–030**：99 Mutation registry；Flag 关 no-op；个人 OAuth 必须本地及远端 owner proof |
| `apps/server/src/services/agentDocumentVfs/{index,path}.ts` | 抽取 canonical VFS path normalizer 供 Skill 路径 Guard 共用 | M06 | 高 | **已实施 PR-030**：非法、模糊或 Skill source/target fail-closed |
| `src/routes/(main)/settings/**`、`src/routes/(main)/agent/**`、`src/routes/(main)/home/**` | 普通用户受管配置入口收口、Agent 创建边界与 Connector OAuth 保留 | M06 | 高 | **已实施 PR-029–030**：定义编辑隐藏；使用 / 会话 / Tool 权限 / 个人 OAuth 保留；Flag 关保持上游 UI |
| `src/store/tool/slices/composioStore/**` | 收窄托管态 Composio OAuth 客户端契约 | M06 | 中 | **已实施 PR-030**：只传本地 identifier/catalog/binding 字段，不上传 Tool 定义或任意远端 account id |
| `packages/{const,types,locales}/**`、`locales/{en-US,zh-CN}/**` | Managed Resource 类型、错误码、角色权限与文案 | M06 | 中 | **已实施 PR-027–030**：AI Admin 获得 policy 管理；只手写 en-US/zh-CN |
| `apps/server/src/globalConfig/index.ts` | 注册窄 AI Runtime bridge | M07 | 中 | **已实施 PR-034–036**：Flag 关闭 no-op，不公开 Secret |
| `apps/server/src/modules/ModelRuntime/{index,platformAiRuntimeBridge}.ts` | Published execution resolver、环境覆盖与全操作 model allowlist | M07 | 高 | **已实施 PR-034–036**：builtin/custom 规范化单一来源；Secret/SDK 前置 preflight |
| `packages/model-runtime/src/core/ModelRuntime.ts` | 增加 TTS/ASR preflight hooks | M07 | 高 | **已实施 PR-036**：无 hook 公共签名与上游行为不变 |
| `apps/server/src/routers/lambda/aiProvider.ts` | Picker/runtime state 改为 Published Secret-free 单一来源 | M07 | 高 | **已实施 PR-034–036**：shadow comparison 有固定上限与 total/truncated |
| `src/app/(backend)/webapi/models/[provider]/{route,pull/route}.ts` | 托管态 model list/pull 执行门禁 | M07 | 高 | **已实施 PR-036**：GET 仅 Published；pull 在 Secret/SDK 初始化前稳定拒绝 |
| `apps/server/src/services/memory/userMemory/{extract,persona/service}.ts` | Memory/Persona 使用同一 Published Catalog 和 model preflight | M07 | 高 | **已实施 PR-036**：未发布 / 类型不匹配时不解密 Secret、不初始化 SDK |
| `apps/server/src/services/toolExecution/serverRuntimes/agentBuilder.ts` | AgentBuilder 模型目录只读 Published Catalog | M07 | 高 | **已实施 PR-036**：Flag 关闭保留上游排序和行为 |
| `src/store/aiInfra/slices/aiProvider/action.ts` | 普通用户 Picker 从单一 runtime state 构建模型组 | M07 | 中 | **已实施 PR-035–036**：客户端不二次合并平台 Catalog，公开 config 严格白名单 |
| `packages/const/src/platform/errorCodes.ts`、`packages/locales`、`locales/{en-US,zh-CN}` | M07 稳定错误码与管理 UI 文案 | M07 | 中 | **已实施 PR-031–036**：`PLATFORM_AI_MODEL_NOT_PUBLISHED`；只手写 en-US/zh-CN |
| `packages/database/src/schemas/platform/ai.ts`、`packages/database/src/models/platform/{aiCatalog,redact,revision,index}.ts` | AI Catalog、Secret 历史、连接测试与 Revision/Redaction 窄扩展 | M07 | 高 | **已实施 PR-031–034**：Secret 不进 Revision/Audit；resourceType 隔离 |
| `packages/database/src/repositories/platformAiCatalog/**`、`packages/database/src/utils/idGenerator.ts` | Catalog 查询 / 发布仓库与 ID 命名空间 | M07 | 高 | **已实施 PR-031–034**：游标、唯一性、resourceType 碰撞回归 |
| `packages/database/migrations/0121*`、`packages/database/migrations/meta/**`、`docs/development/database-schema.dbml` | M07 Schema 迁移、快照与 DBML | M07 | 中 | **已实施 PR-031–034**：journal/snapshot 122/122 |
| `packages/database/src/{schemas,models,repositories}/platform/**`、`packages/database/migrations/0122*`、`packages/database/migrations/meta/**` | Skill identity、不可变版本、发布快照与历史解析 | M08 | 高 | **已实施 PR-037–039**：版本 checksum、CAS、依赖锁与 archive 历史解析共用一条 Repository 边界 |
| `apps/server/src/routers/lambda/agentSkills.ts`、`apps/server/src/services/{aiAgent,toolExecution}/**` | 受管 Skill 用户执行投影、operation pin 与独立激活路径收口 | M08 | 高 | **已实施 PR-039–040**：精确 `skillKey+version+checksum`；受管时忽略个人定义；Flag 关闭零额外读取 |
| `packages/context-engine/src/engine/skills/**`、`packages/types/src/platform/skills.ts` | 固化平台 Skill operation snapshot 与分发语义 | M08 | 高 | **已实施 PR-039–040**：客户端/服务端共用 mandatory/default/optional × pinned/auto/disabled 纯函数 |
| `src/routes/(main)/settings/**`、`src/features/{ChatInput,ProfileEditor}/**`、`src/store/tool/**`、`src/services/{chat,skill}/**` | 用户已发布 Skill 目录、Picker、Preload 与客户端 Runtime 收口 | M08 | 高 | **已实施 PR-040**：只读来源/版本/分发；定义写入不挂载；web/mobile/desktop 共用 exact published resolver |
| `scripts/enterprise/pathBoundaries.ts`、`packages/locales/src/default/setting.ts`、`locales/{en-US,zh-CN}/setting.json` | M08 稳定挂载点与用户侧文案 | M08 | 中 | **已实施 PR-040**：仅 allowlist 审核过的 Runtime/目录挂载点；只手写 en-US/zh-CN |
| `packages/device-control/**`、`packages/electron-client-ipc/**`、`apps/desktop/src/main/controllers/LocalFileCtr.ts`、`apps/server/src/{routers/tools/market.ts,services/deviceGateway/index.ts}`、`src/services/electron/**` | exact inline Skill operation workspace 的 sandbox / device / desktop 受控执行边界 | M08 | 高 | **已实施 PR-040 安全返工**：仅物化 validator-verified inline text；operationId + 完整 checksum 隔离；0700/0600、显式 finally cleanup + TTL；禁止个人 ZIP / skillKey 路径派生与 Web 进程 spawn |
| `packages/database/src/{schemas,models,repositories}/platform/**`、`packages/database/migrations/012{3,4}*`、`packages/database/migrations/meta/**`、`scripts/migrateServerDB/predeployM09ConnectorIndexes.ts` | Connector/Tool/Secret/Binding/OAuth state 持久化与安全上线 | M09 | 高 | **已实施 PR-041/044**：`migration_required` 隔离遗留行，不复制旧 Secret；NOT VALID/VALIDATE；concurrent index predeploy；journal/snapshot 125/125 |
| `apps/server/src/enterprise/security/outboundHttp/**`、`packages/ssrf-safe-fetch/**` | 统一 Connector 出站安全边界 | M09/M13 | 高 | **已实施 PR-042**：Metadata/NAT64/SIIT 禁止、DNS pin、重定向、response/deadline 有界、动态版本化 allow-private/allowlist；无通用端口 allowlist/content-type 仍登记 `RESIDUAL.md` |
| `apps/server/src/{routers/lambda/connector.ts,routers/tools/mcp.ts,services/connector/**,services/mcp/**,services/aiAgent/**,services/toolExecution/**,services/agentRuntime/**}` | 将旧 Connector/MCP/Tool 执行收口到 Published Revision、Binding、Policy、approval 和审计 | M09 | 高 | **已实施 PR-043–045**：旧同步传输禁止 stdio；平台 deny 优先；Agent/call/credential 精确绑定；Flag 关闭保持上游路径 |
| `src/app/(backend)/oauth/connector/callback/**`、`apps/server/src/enterprise/{connectorOAuthCallback.ts,routers/user/connectors.ts}` | 每用户 OAuth callback 与 Binding 生命周期 | M09 | 高 | **已实施 PR-044/046**：callback 仅信任签名 state/attempt；用户隔离、刷新租约、重放/过期/篡改拒绝；UI 状态绑定本次 attempt |
| `src/enterprise/client/features/admin/connectors/**`、`src/features/PlatformConnectorAuthorization/**`、`src/routes/(main)/**/settings/connector/**` | Connector Admin/User UI、全 settings 路由 Guard 与 OAuth UX | M09 | 高 | **已实施 PR-046**：只读 auditor、本地 CAS/dirty/conflict、Secret 仅内存；普通用户仅授权/撤销与 Tool 风险，单一 `enterpriseAdapter.ts` 跨边界 seam |
| `packages/{const,types,locales}/**`、`locales/{en-US,zh-CN}/**`、`scripts/enterprise/pathBoundaries.ts` | M09 稳定错误码 / 契约 / 文案与路径边界 | M09 | 中 | **已实施 PR-041–046**：只手写 en-US/zh-CN；普通用户 Connector 仅 allowlist 一个 enterprise adapter；没有扩大全局 test 豁免 |
| `apps/server/src/routers/lambda/{agent,home}.ts` | 普通用户统一列表合并 effective 平台 Agent（picker queryAgents / 侧栏 getSidebarAgentList / 搜索 searchAgents）；managed 入口 active-user 与 managed 本地行 mutation guard | M10 | 高 | **已实施 PR-049 + REWORK**：仅经 `PlatformAgentUserListService` enterprise adapter 合并，`HomeRepository`/`AgentModel` 不依赖 enterprise；Flag 关闭零 catalog 访问、保留 legacy；列表绝不物化；确定排序/分页/关键字/去重。REWORK-3：三入口 `withActiveUserWhenManagedAgents`（flag ON 拒 banned/inactive/epoch-invalid，flag OFF no-op）。**RR2-7**：`withActiveUserWhenManagedAgents` 仅 gate 于 `ENABLE_PLATFORM_MANAGED_AGENTS`——MANAGED=0+ADMIN=1 不再对这些 ordinary legacy 入口新增 ban/epoch 限制（admin 路由自带 `withActiveUser`）。**RR2-4 统一 guard**：新增 `withManagedLocalAgentGuard` tRPC 中间件（+ 数组版 `assertAgentsNotPlatformManaged`、pickers），在 handler 前 owner-scoped 逐项拒绝托管本地行、拒绝时零写入；覆盖 agent 路由全部 agent-scoped 写（publish/setVisibility/create·delete·toggle File·KnowledgeBase/duplicate/transfer/lock + 迁移的 remove/updateConfig/pin）、agentGroup（add/removeAgentsFromGroup 逐项、updateAgentInGroup）、home.updateAgentSessionGroupId，以及 `agentDocument` 共享写 procedure 一处覆盖全部 ~27 文档写（含 clone source/target）；flag OFF 完全 legacy |
| `apps/server/src/services/aiAgent/index.ts` | `execAgent` 增加 platform 聊天入口分支 | M10 | 高 | **已实施 PR-049 + REWORK**：list identity 与 owner-scoped 物化本地 id 均仅作请求入口，不作授权依据；`beginOperation` owner-scoped 一次授权并 pin exact snapshot；撤权 fail closed（REWORK-2）；runtime config 仅取 snapshot，不回读本地 row；exact dependency 经现有 M07/M08/M09 validator fail-closed 校验、无 latest fallback（REWORK-4）；Skill 池按 immutable pinned refs exact historical 解析注入 SkillEngine、替换普通池、无 latest（SKILL-EXACT）；Connector manifests/allowlist 按 immutable pinned refs exact historical revision/checksum 解析注入、只暴露 pin 允许的 tools、tool escalation fail closed、替换普通 connector 路径、无 latest（CONNECTOR-EXACT）；归属用物化后真实 user-owned Agent；managed 入口 active-user（REWORK-3）；local/builtin 无回归；**RR5 kind-keyed paused resume（取代 RR4-1/RR3-1）**：**仅** approval/tool-result 为 paused resume，经 `resolveResumePlatformPin` 按 `pausedResumeKind`（`approval`/`toolResult`）匹配**服务端专属** kind-keyed id（`metadata.pendingResumeAnchors[kind]`，`@>` 精确、kind 不可交叉）——**不再信任** client 可写的 `message.parentId`/`message.metadata`；再以 `isEntitled` 复核**实时**授权后才 replay，仅 `waiting_for_human` 可外部 resume（`waiting_for_async_tool` 内部同 operationId CAS 续跑），伪造 tool/kind 交叉/撤权/跨 owner/错 scope/缺绑定一律 NOT_FOUND fail closed、绝不 fresh beginOperation；**RR5-5** 裸 `parentMessageId`（regenerate/continue）**不再**复用 paused pin，走 `beginOperation` 新 operation（当前 entitlement+snapshot），保留 skip-user-message；**RR5-2** `resumeToolResult` 校验持久化 `intervention.kind==='toolResult'` 且经 `approvePendingToolResultPlugin` kind-guarded single-winner CAS 写 approved（拒绝 stale/双击/错 kind/无 kind），`resumeApproval` 亦校验 `kind==='approval'`，Zod 与 service 双层强制 `resumeApproval` XOR `resumeToolResult`；**RR2-5 脱敏**：`mapPlatformConfigError` 未知 DB/driver/runtime 错误不再原样抛（脱敏为稳定 materialization 错误）；`resolvePinnedPlatformSkillRuntimeSnapshot`/Skill runtimeAdapter 失败不再回显 `skillKey@version`/resource path；aiAgent 路由 `execAgent`（单）与 `execAgents`（批，`.use(withActiveUserWhenManagedAgents())` — RR2-3）不再拼裸 `error.message`，统一 `publicExecErrorMessage` 稳定脱敏（curated TRPCError 透传，其余折叠为通用消息，无 SQL/标识/secret） |
| `packages/database/src/models/agent.ts` | `queryAgents` 增加 `excludeAgentIds` 过滤 | M10 | 中 | **已实施 PR-049**：仅追加可选参数，空数组为 no-op（legacy 不变）；用于统一列表按 owner-scoped 物化集合去重，分页在 SQL 内精确 |
| `apps/server/src/services/agentRuntime/{AgentRuntimeService,CompletionLifecycle,types}.ts`、`packages/database/src/models/{agentOperation,message}.ts`、`packages/agent-runtime/src/{executors/humanApprove,types/state}.ts`、`packages/types/src/platform/agents.ts` | operation 持久化 secret-free platform pin 与 resume/retry 复用（REWORK-2）+ exact model pin（MODEL-EXACT） | M10 | 高 | **已实施 REWORK**：`createOperation` 把 `{platformAgentId,versionId,checksum}` 写入 `metadata.platformOperation`、exact model ref `{providerKey,modelKey,providerRevision,providerChecksum}` 写入 `metadata.platformModel`（均无 secret）；exact skill refs 写入 `metadata.platformSkills`（SKILL-EXACT）、exact connector refs+allowlist 写入 `metadata.platformConnectors`（CONNECTOR-EXACT，均无 secret）；新增 `findPlatformOperationRef`（供 model runtime 区分 platform vs ordinary）；`findPlatformModelPin` owner+workspace scoped 保留；`CompletionLifecycle.recordStart` 对 platform operation 持久化失败**不再吞错**，稳定无标识 fail-closed 中止启动（普通 operation 仍 fire-and-forget）。**RR5 kind-keyed server-owned resume anchor（取代 RR4-1/RR3-1）**：删除 fuzzy `findLatestPlatformOperationPin` 与 `findPlatformOperationPinByOperationId`；**不再信任** client 可写的 `message.parentId`/`message.metadata`。**RR5-2** anchor 按**服务端派生的 interaction kind** 分列存 `metadata.pendingResumeAnchors:{approval?:string[];toolResult?:string[]}`——kind 由 human-approve executor 于 park 时从 `state.toolManifestMap` 的 `humanIntervention`（静态 `'always'`→`toolResult`，否则 `approval`）派生并盖在新建 pending tool 的 `intervention.kind`（server-owned）。**RR5-1** skip-create（re-park）分支**只**取运行时 `state.messages` 中当轮 pending（`status='pending'`+父 assistant，且带 server kind）的可信 id，**绝不**再按 `tool_call_id` 反查可被伪造（同 tool_call_id+回填 createdAt）消息；公开 `MessageService`（create/batchMutate/updateMessagePlugin）**剥离** client 传入的 `intervention.kind`，使其纯 server-owned。**RR5-3** `CompletionLifecycle` 经 `parkForHumanIntervention` 把 `waiting_for_human` 状态与 kind-keyed anchors 合并进**单条** owner/workspace + expected-prev-status(`running`/`waiting_for_human`) CAS（`||`+`jsonb_build_object` 只并入在场 kind，不动兄弟 kind 与 pins）；affected≠1 时 platform operation **FATAL**（`PLATFORM_OPERATION_PARK_PERSIST_FAILED`，`dispatchHooks` 于 finally 后重抛），普通 operation 仍 fire-and-forget；已 cancel/done/interrupted 的行不被 late park 复活。`findResumablePlatformOperationPin` 按 kind 精确匹配服务端 id（`pendingResumeAnchors[kind]` 之一，`@>` 精确、kind 不可交叉），**联合校验** owner/workspace/必需 topic/thread leg/目标 platformAgent，且**仅** `waiting_for_human` 可外部 resume（`waiting_for_async_tool` 走内部同 operationId state CAS，终态不可派生）——RR4-2；service 层再以 `isEntitled`（snapshot-free）复核实时授权后才 replay，伪造 tool/kind 交叉/撤权/跨 owner/错 scope/缺绑定一律 NOT_FOUND fail closed、绝不 fresh beginOperation。**RR5-4** 新增共享 `classifyPlatformStart(meta)→ordinary|complete|partial`（`recordStart` 复用）：任一 platform marker 在场即要求**完整**绑定，partial 一律 fail closed，绝不 ordinary 降级。`PlatformOperationMetadata` 强类型 `pendingResumeAnchors` kind-keyed；`ToolIntervention` 加 server-owned `kind`。**RR4-5 统一 metadata 剥离**：`MessageModel` 在**最终写入边界**（merge 后）统一剥离服务端保留键 `operationId`，覆盖 create/update/updateMetadata/batchCreate/**updateToolMessage**（公开单条 router + batchMutate 均经此）。**RR4-3/RR4-4 完整 + 原子 platform start**：`recordStart` platform 冲突校验要求四类 pins **全部存在**（skills/connectors 空数组有效、undefined 无效）**加** assistant 绑定，两侧都完整且逐字一致才幂等；同 pins 不同/缺 assistant、缺任一 pin、ordinary/跨 owner 一律 `PlatformOperationStartConflictError`（稳定脱敏，阻止 runtime 落到 legacy latest）；insert+冲突校验放**同一事务**并对冲突行 `FOR UPDATE` 锁，杜绝 TOCTOU（existing 消失/变更/不完整均 fail closed）；普通 operation 幂等保留。仅新增可选参数/方法，legacy operation 无回归 |
| `apps/server/src/modules/ModelRuntime/{index,platformAiRuntimeBridge}.ts`、`apps/server/src/modules/AgentRuntime/adapters/{ServerLLMTransport,serverCallLlmExecutor}.ts` | 平台 operation 按 exact historical provider revision 运行（MODEL-EXACT） | M10 | 高 | **已实施 REWORK**：新增 `resolveExecutionConfigAtRevision`/`initPlatformExactModelRuntime`/`initOperationModelRuntime`；仅当 operation 带 owner-scoped model pin 且 provider+model 命中时解析 exact 历史 revision（缺失/禁用/checksum/revision 不符 fail closed 脱敏，凭据按该 revision 每次解密、不入 metadata）；否则逐字走 legacy `initModelRuntimeFromDB`（普通 local/builtin 与 flag off 零额外 M07 查询、无回归）；**RR2-2 no-downgrade**：`initOperationModelRuntime` 先按 `findPlatformOperationRef` 区分 platform vs ordinary——platform operation 缺 model pin、provider/model 与 pin 不符、或 DB 读错一律 `PlatformExactModelUnavailableError` 稳定 fail closed（**绝不**回落 managed latest），只有真正 ordinary/builtin 才走 legacy |
| `apps/server/src/services/aiAgent/index.ts`、`apps/server/src/services/agentRuntime/{AgentRuntimeService,CompletionLifecycle}.ts`、`packages/agent-runtime/src/{executors/humanApprove,types/state}.ts`、`packages/database/src/models/agentOperation.ts`、`packages/types/src/{message/common/tools,platform/agents}.ts` | RR6 不可伪造 paused-resume provenance、generation park CAS 与可信平台分类 | M10 | 高 | **已实施 PR-049 RR6（覆盖并取代上方 RR5 kind-keyed anchor / `always` 推导说明）**：仅三个审计过的官方 UI 交互 `lobe-agent/askUserQuestion`、`lobe-user-interaction/askUserQuestion`、`lobe-web-onboarding/showAgentMarketplace`，且 operation tool snapshot 中服务端可信 `sourceMap` 必须标记为 `builtin`，方可作为 `toolResult`；同名的 MCP/Composio/Skill、一般及未知 `humanIntervention='always'` 均为 `approval`。每个 anchor 保存 `{operationId,assistantMessageId,messageId,toolCallId,kind,fingerprint}` 完整 provenance，fingerprint 为绑定 identifier/apiName/toolCallId/type/arguments 的 SHA-256，并与本轮 `pendingToolsCalling`、服务端创建的 pending tool message 绑定；公开消息入口剥离 `kind` 与 `provenance`。`pendingResumeAnchors` 改为完整集合数组；park 以 `pendingResumeGeneration` 单调 CAS **整集合替换**，同代同集合可幂等，旧代/并发不同集合/终态 late park 均不可覆盖或复活；平台 park 同时逐项匹配 operation/model/skills/connectors/assistant 五字段 start binding，affected≠1 fatal。共享 `classifyPlatformStart` 统一 `ordinary/complete/partial`；任一 platform marker 的 partial/complete start 持久化失败均 fatal，运行期保存服务端分类与规范化五字段 binding，park 不允许以持久化 DB 查询替代缺失或矛盾的运行期 proof，缺失、矛盾、partial 或读取失败均 fail closed。外部 resume 仍只允许 `waiting_for_human`，并精确匹配 owner/workspace/topic/thread/platformAgent/provenance 后复核实时 entitlement，绝不降级 fresh/latest |
| `apps/server/src/services/agentRuntime/HumanInterventionHandler.ts`、`apps/server/src/services/aiAgent/index.ts`、`apps/server/src/services/message/index.ts`、`packages/database/src/models/message.ts`、`packages/types/src/message/common/tools.ts` | RR6 approval / reject / reject-continue / tool-result kind guard 与原子单赢家消费 | M10 | 高 | **已实施 PR-049 RR6**：四条消费路径统一落在 `MessageModel.resolvePendingIntervention` 事务边界；仅当 owner/workspace、`status='pending'`、服务端 `kind` 全部匹配时执行 pending→approved/rejected CAS，plugin intervention/state 与 tool-result/rejection content 在同一事务提交，消息缺失会整体回滚。approval 与 reject、halt 与 continue、并发 tool-result 均仅一个赢家；stale/双击/错 kind/无 kind/跨 owner 返回失败且零内容覆盖。service 层继续强制 `resumeApproval` XOR `resumeToolResult`，并只从持久化 plugin 重建待执行 tool receipt |
| `apps/server/src/modules/AgentRuntime/adapters/{operationModelRuntime,ServerMessageTransport}.ts`、`apps/server/src/services/agentRuntime/AgentRuntimeService.ts`、`packages/database/src/models/agentOperation.ts` | RR6 每次 LLM 调用核验可信平台 start binding，并稳定持久化显式 tool message id | M10 | 高 | **已实施 PR-049 RR6**：`createOperation` 在 caller `appContext` 展开后覆盖写入服务端 `platformStartClassification` 与规范化五字段 `platformStartBinding`；`initOperationModelRuntime` 每次调用把可信运行期 binding 与 owner/workspace-scoped 持久化 binding 做规范化精确比较，complete 仅走 exact historical model，ordinary 仅在可信分类 ordinary 且 DB row ordinary/不存在时走 legacy；unknown/partial/矛盾 binding/DB 读取失败全部 `PLATFORM_MODEL_UNAVAILABLE` fail closed。`ServerMessageTransport` 仅在 runtime 已生成 tool message id 时显式传给 `MessageModel.create`，确保 provenance 的 messageId 与真实持久化主键一致，普通 tool create 保持原签名 |
| `apps/server/src/modules/AgentRuntime/adapters/operationModelRuntime.ts`、`apps/server/src/services/agentRuntime/CompletionLifecycle.ts`、`apps/server/src/services/aiAgent/__tests__/execAgentPlatform.test.ts`、`apps/server/src/enterprise/services/{platformDependencyLock,agentCatalog/dependencyValidator}.ts`、`packages/database/src/repositories/{platformSkillCatalog,platformConnectorCatalog}/index.ts` | RR7 升级前 ordinary runtime/park 兼容、resume 安全测试真实化、dependency validation 并发与批量 exact read | M10 | 高 | **已实施 PR-049 RR7（补充并覆盖 RR6 的 upgrade ordinary 说明）**：queued/resume/park state 若来自升级前且缺 `platformStartClassification`/binding，仅在 owner-scoped persisted ref=ordinary/不存在时保留 legacy；persisted complete/partial 即使 flag off 仍 fail closed，DB 读取失败稳定拒绝，绝不拿 DB pin 替代缺失的平台运行期 proof。`execAgentPlatform` 使用带 kind/apiName/arguments/identifier/type/toolCallId 的真实 plugin mock，并断言 SHA-256 fingerprint + toolCallId，确保实际进入 provenance 安全分支。只读 validate 使用同 namespace `pg_advisory_xact_lock_shared`，并发 read-read 不互阻；所有 dependency publish/archive/reference write 继续 exclusive，read-write 双向互斥。Provider/Model exact 校验不变，最多 100 Skill + 100 Connector refs 各以单条批量 exact query 解析并继续逐项校验 status/enabled/identity/checksum/tool allowlist，无 latest fallback |
| `packages/types/src/home.ts`、`src/services/agent.ts` | `SidebarAgentItem` / `AvailableAgentItem` 增加可选 `platform` 元数据 | M10 | 低 | **已实施 PR-049**：仅新增可选字段（source/platformAgentId/distribution/managed），兼容既有消费者；不泄露 admin-only 元数据 |
| `apps/server/src/services/{agent,aiAgent,onboarding}/index.ts`、`apps/server/src/routers/lambda/user.ts`、`apps/server/src/services/toolExecution/serverRuntimes/webOnboarding.ts`、`apps/server/src/modules/{Mecha,AgentRuntime}/**`、`src/store/user/slices/onboarding/action.ts`、`packages/types/src/{agent/item,platform/agents}.ts` | 稳定 builtin inbox 身份接管与旧写入口收口 | M10 | 高 | **已实施 PR-051 + RR1/RR2**：`default-inbox` 的 Published exact version 覆盖 title/avatar（`null` 为权威清空）/prompt/provider/model/params 及 exact Skill/Connector 依赖，同时保持 `INBOX_SESSION_ID`、builtin id/slug/URL 与既有 topic/message/session/workspace 归属。Managed runtime 只使用服务端 operation pin、pinned Connector allowlist 和审计过的静态 builtin allowlist；忽略 request model/provider/instructions/functionTools/additional/selected plugins，不注入 page/task/project/document/SOUL 或普通 discoverable tools；RR2 又在 AiAgent、AgentRuntime state/executor 和最终 MessagesEngine 三层清除未 pin 的 agentGroup、bot/Discord/eval、initial context、memory/timezone/topic/onboarding/动态变量等 prompt side channel，ordinary operation 保持原行为。Fresh operation 捕获当前 default；所有 paused resume（包括 builtin pending provenance 已捕获的 pin）都先复核实时 `isEntitled`，撤权/归档 A 后 NOT_FOUND，仍有授权则 A→B 后继续精确重放旧 A/version，绝不 latest。公开 Agent metadata 只保留 `source/managed/distribution`，exact version/checksum/dependency/tool 证据仅留服务端。Flag OFF 零 catalog IO；真实无 Published default 才回落 legacy，resolver/DB/dependency 错误 fail closed。`defaultAgent.*` settings、onboarding identity/reset、SOUL 和普通 Agent mutation 均在任何写前 guard，capture 失败保证 User/Agent/document 零部分写。无 M12 全局品牌替换、无管理 UI、无迁移 |
| `packages/database/src/{models,repositories}/**`、`apps/server/src/enterprise/services/agentCatalog/userListProjection.ts` | default-inbox 专用非复制身份与统一列表投影 | M10 | 中 | **已实施 PR-051 + RR1/RR2**：普通平台 Agent 继续以 owner-scoped materialization 集合去重；`default-inbox` 不写 `(userId, platformAgentId)` 普通 materialization 映射，不创建副本、不需迁移。picker/sidebar/search 都通过真实 Agent service 显式加载固定 builtin Inbox，去掉所有 base/encoded 重复后覆盖 Published title/avatar/managed 标记；搜索只命中当前 Published title，分页、侧栏均显式回归且恰好一项。RR2 要求 Flag OFF 在任何 builtin/catalog 初始化前首行返回：picker 调用原 `AgentModel.queryAgents(input)`（原参数/排序/分页/结果不重建），sidebar/search 返回同一 base value，且 managed loader 零调用。`workspaceId` 从 agent/home entry 显式贯穿 `PlatformAgentUserListService → AgentService → AgentModel`；真实 PGlite 验证 personal/workspace 的 picker/sidebar/search 只出现各自 builtin，workspace id 只在 workspace runtime 解析、跨 scope 不泄漏。Mutation batch guard 在 flag gate 后先去重并限制 100 个 id，仅执行 owner-scoped materialization + builtin slug 两条批量查询，无 N+1 |
| `src/libs/better-auth/define-config.ts` | 加载 DB OIDC Published Snapshot | M11 | 高 | 单一 Adapter + LKG |
| `apps/server/src/globalConfig/getServerAuthConfig.ts` | 公开登录 Provider 列表 | M11 | 中 | 读取启动快照 |
| Metadata/Manifest/AuthShell/ 邮件模板 | Runtime Branding | M12 | 中 | Provider/fallback，避免全仓替换 |

## 规则

1. 任何新增上游直接修改必须更新本表，说明替代方案为何不可行。
2. 优先新增方法、Adapter 和注册器，不改旧公共签名。
3. 每次上游 Rebase 生成冲突报告和行为回归报告。
4. 对高风险文件建立 focused tests，尤其 RBAC Middleware、User Settings、Better Auth 和 Router Root。

# 上游直接修改点台账

| 上游文件 / 区域                                                   | 修改目的                              | 模块    | 冲突风险 | 控制方式                                                                                                                             |
| ----------------------------------------------------------------- | ------------------------------------- | ------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `src/business/client/BusinessDesktopRoutes.tsx`                   | 注册 Enterprise Admin Route 数组      | M00/M03 | 低       | 保持单行 import/export；路由实现放新增目录。M00 PR-003：`...EnterpriseDesktopRoutesWithoutMainLayout`（默认空数组，Flag 关闭零路径） |
| `src/business/client/BusinessGlobalProvider.tsx`                  | 挂 Runtime Branding/Platform Provider | M00/M12 | 低       | 只包装 Provider。M00 PR-003：包装 `EnterprisePlatformProvider`，默认能力快照为 disabled                                              |
| `apps/server/src/routers/lambda/index.ts`                         | 挂 admin/platform Router              | M00     | 中       | 只增加 import 和键。M00 PR-003：`platform: platformRouter`（只读 getCapabilities/getPublicSnapshot）                                 |
| `apps/server/src/globalConfig/index.ts`                           | Global Config 注入 enterprise.enabled | M00     | 低       | 第 4 挂载点。`serverConfig.enterprise: { enabled }` 门控客户端 platform.\*；Flag 全关时 enabled=false，冷启动零 platform 请求        |
| `packages/types/src/serverConfig.ts`                              | GlobalServerConfig.enterprise 类型    | M00     | 低       | 仅增加可选 `enterprise?: { enabled: boolean }`，不泄露 Flag 名 / Secret                                                              |
| `package.json`                                                    | `enterprise:check-paths` 脚本         | M00     | 低       | PR-004：路径边界 CI 入口                                                                                                             |
| `packages/database/src/schemas/index.ts`                          | 导出 platform Schema                  | M01     | 低       | 新增单行 `export * from './platform'`；实现位于 `schemas/platform/`                                                                  |
| `packages/database/src/utils/idGenerator.ts`                      | 增加 platform\_\* 表 id 前缀命名空间  | M01     | 低       | 仅追加 prefixes 键，不改现有命名空间                                                                                                 |
| `.prettierignore`                                                  | 忽略 drizzle `migrations/meta/` 生成物，防 prettier 重排造成迁移快照噪声 | M01     | 低       | 仅追加忽略路径                                                                                                                       |
| `packages/const/src/rbac.ts`                                      | 增加 platform 权限和 scope 规则       | M02     | 中       | 测试上游权限生成                                                                                                                     |
| `packages/database/src/models/rbac.ts`                            | 严格 Global Query / 按作用域替换      | M02     | 中       | 优先新增方法不改旧签名                                                                                                               |
| `packages/business-server/src/trpc-middlewares/rbacPermission.ts` | 企业 Flag 下真实实现或转接            | M02     | 高       | 保持 export shape；充分回归                                                                                                          |
| `apps/server/src/routers/lambda/user.ts`                          | Effective Settings / 旧更新兼容       | M05     | 高       | 通过服务封装减少散改                                                                                                                 |
| `apps/server/src/routers/lambda/aiProvider.ts` 等                 | 添加 Managed Guard                    | M06     | 中       | 统一中间件                                                                                                                           |
| `src/libs/better-auth/define-config.ts`                           | 加载 DB OIDC Published Snapshot       | M11     | 高       | 单一 Adapter + LKG                                                                                                                   |
| `apps/server/src/globalConfig/getServerAuthConfig.ts`             | 公开登录 Provider 列表                | M11     | 中       | 读取启动快照                                                                                                                         |
| Metadata/Manifest/AuthShell/ 邮件模板                             | Runtime Branding                      | M12     | 中       | Provider/fallback，避免全仓替换                                                                                                      |

## 规则

1. 任何新增上游直接修改必须更新本表，说明替代方案为何不可行。
2. 优先新增方法、Adapter 和注册器，不改旧公共签名。
3. 每次上游 Rebase 生成冲突报告和行为回归报告。
4. 对高风险文件建立 focused tests，尤其 RBAC Middleware、User Settings、Better Auth 和 Router Root。

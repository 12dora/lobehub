# 验收与回归矩阵

| 领域          | 最低验收标准                                                          | 责任模块    |
| ------------- | --------------------------------------------------------------------- | ----------- |
| 权限          | 普通用户和 workspace owner 访问 /admin/API 均拒绝；最后超级管理员保护 | M02/M03/M15 |
| 用户          | 搜索 / 筛选 / 封禁 / 解封 / Session / 角色均审计                      | M04         |
| 设置          | 未覆盖继承新默认；显式覆盖保持；locked API 拒绝；单路径恢复           | M05         |
| Managed Guard | 直接调用旧 Mutation 失败；用户仍能使用发布资源                        | M06         |
| AI            | Draft/Test/Publish/Rollback；Secret 不回显；Runtime 一致              | M07         |
| Skill         | 版本不可变；校验 / 依赖 / 回滚；用户不可编辑                          | M08         |
| Connector     | SSRF/OAuth 隔离 / Tool deny；每用户绑定                               | M09/M13     |
| Agent         | 全用户 Assignment、Rollout、Inbox 兼容、依赖校验                      | M10         |
| OIDC          | Authentik 测试、Pending Restart、LKG、Break-glass                     | M11         |
| Branding      | 登录 / 导航 / Title/Manifest/ 邮件 / Inbox 一致；内部标识不改         | M12         |
| 运维          | Revision 收敛、Redis/KMS/Job/ 实例状态可观测                          | M14         |
| 升级          | 2.2.10 数据升级、应用回滚、上游 Rebase E2E                            | M15         |

## 发布签字

- 产品：业务规则、UI 文案、用户影响。
- 架构：数据所有权、运行时解析、上游兼容。
- 安全：RBAC、Secret、SSRF、OIDC、审计。
- DBA：迁移、索引、锁、备份和回滚。
- 运维：Feature Flag、监控、告警、Runbook、滚动重启。
- QA：权限矩阵、E2E、回归、性能和灾备演练。

## W2 验收证据

- M03：普通用户 /workspace owner 不挂载管理业务数据；无权限显示 403，未知路径 404；Flag 关闭时 admin registry 为空；desktop route 双配置同步。
- M04：搜索 / 状态 / 角色 / 日期 /cursor/page-size 真实请求证据；封禁 / 解封 / Session / 角色写入均要求 reason，高风险动作 reauth，成功 / 失败 / 拒绝均审计。
- M04 安全：最后超管保护、workspace role 不受影响、现有 Session 安全 epoch、当前 Session 保留例外实时验证、OIDC 强制重新登录、popup state/origin/source/replay、unmount/destroy abort 均有对抗测试。
- 合并后 19 个聚焦测试文件 192 项通过，完整 type-check 与企业路径边界通过；Migration 0119 journal/snapshot 为 120/120。

## W3 验收证据

- M05：未覆盖继承新默认、显式覆盖保持、locked/hidden API 拒绝、单路径恢复与旧设置兼容写均有后端 / UI 回归；发布 / 回滚使用 revision + draft token CAS、reason、reauth 和审计。
- M06：10 个 Router、99 个 Mutation 静态登记并由真实 caller 测试守护；observe/ui-only 仅观测，enforced 拒绝旧写入口，发布资源的读取、会话与个人 OAuth 使用仍保留。
- M06 安全：AgentDocument canonical VFS path fail-closed；Composio create/update/get/delete 同时校验本人本地 projection 与远端 owner，遗留伪造 binding 四路拒绝且 delete 无副作用。
- 合并后服务端 76、前端 33、数据库 4 项聚焦测试通过，完整 type-check 与 10,072 文件企业路径边界通过；Migration journal/snapshot 为 121/121，M06 无新增迁移。

## W4 验收证据

- PR-031：Provider/Model 严格输出、Repository 唯一性 / 游标 / 全局筛选 /resourceType 隔离；Migration `0121`，journal/snapshot 122/122。
- PR-032：Secret `keep\|replace\|clear`、结构化多字段凭据、嵌套敏感键 / 凭据 URL / 自由文本脱敏、连接结果持久化和 attempt CAS。
- PR-033：Draft/Publish/Rollback/Archive 使用 revision + draft token CAS；依赖阻断在共享 PostgreSQL advisory lock 内重查，发布后失效通知不伪装成事务回滚。
- PR-034：Published metadata 与 server-only Secret 分离；Model Bank 安全合并、共用 Test/Runtime adapter、环境应急覆盖和有界 shadow comparison。
- PR-035：Provider/Model 真实页面、只读 auditor、本地草稿 Secret fail-closed、raw JSON rebase、URL Back/Forward、持久化 Test 和 write epoch 旧 CAS 阻断。
- PR-036：chat/object/image/video/embedding/TTS/ASR、Memory、Persona、AgentBuilder、WebAPI model list/pull 只允许 Published model，并在 Secret/SDK 初始化前 fail-closed；Flag 关闭保持上游路径。
- 三路独立 reviewer 最终 PASS。根代理整合后 30 个测试文件、186 项通过；116 个代码文件 lint clean；完整 type-check、10,145 文件企业路径检查和 `git diff --check` 通过。

## W5 验收证据

- PR-037–040：Skill 不可变版本 / checksum、有界 Validator / URI / Prompt Injection 扫描、依赖、发布 / 回滚 / 归档、精确 operation snapshot 与历史解析有 focused tests；普通用户不能写平台定义。
- M08 执行安全：只物化 validator-verified inline text，operation id + checksum 工作区隔离，0700/0600、finally cleanup + TTL；server/device/desktop 共用可验证的 trusted proof，Flag-off 旧 proof fail-closed。独立 reviewer 最终 PASS，合入 `1ab72e9`。
- PR-041–043：Connector/Tool/Binding/Secret/OAuth state 分层；Migration `0123` 在真 PostgreSQL 从 M01 shell 双跑，orphan/遗留行/旧 Secret 回归，concurrent index predeploy 与 >10k 大表门禁均有证据。
- PR-042 SSRF：Metadata（含 IPv4-mapped 与 RFC 6052 NAT64/SIIT）永久禁止；DNS pin/重绑、重定向逐跳重验、Secret-bearing 跨源 fail-closed、有界 body/deadline、动态 allow-private→allowlist 紧缩均有对抗测试。
- PR-044–045：OAuth state 篡改 / 重放 / 过期 / 并发刷新与撤销、用户 A/B Token 隔离、共享 Secret 不回显、平台 deny 不可重开、Agent/call/approval 精确绑定和旧 MCP 管道 fail-closed 均有回归。
- PR-046：Admin/User 真实 UI、只读 auditor、RBAC 子路由不挂载、Secret 内存草稿、dirty/conflict 锁、OAuth authorization-attempt 轮询、全 settings 路由 Guard 和 en-US/zh-CN 均有聚焦回归。
- M09 迁移、管理 API、UI 和跨层 reviewer 均在首轮 REWORK 后由原代理修正、同 reviewer 复核 PASS。跨层最终 129 项、UI 105 项通过（另 2 项条件跳过）；根代理 40 + 36 项定向复验、完整 type-check、10,356 文件企业路径检查和 `git diff --check` 通过。
- Drizzle journal/snapshot 为 125/125，最新迁移 `0122`、`0123`、`0124`；W5 分别以 `1ab72e9` / `82c9786` 合入。未运行被禁止的全量 `bun run test` 或全量 `bun run i18n`。

## W6 验收证据

- M10 PR-047–052 的 Agent/Version/Assignment、exact dependency runtime、Admin、稳定 builtin Inbox 接管与 Rollout 均经独立 reviewer 返工至 PASS；默认 Inbox 回滚沿 publication pointer，不复制第二个 Inbox。
- operation pin、paused resume entitlement、intervention 单赢家消费、rollout lease/checkpoint/失败集重试/反向 rollout 与 PostgreSQL 微秒 cutoff 均有 focused 回归；真实多连接 PostgreSQL rollout 门禁仍须在上线环境执行。
- Migration `0125`、`0126` 合入后 journal/snapshot 为 127/127。PR-063 只完成默认关闭的桌面 opt-in profile，真实签名与发布不计入本地验收。

## W7 验收证据

- M11 PR-053–057 覆盖 Provider/Secret/Revision、Discovery/Test、Published Snapshot/LKG、Admin 向导、发布/回滚、reauth/audit 受控重启、可信钉钉 claim、break-glass 与正式登录安全链；M12 PR-058–062 覆盖 Runtime Branding 契约、消费者、Admin、i18n/视觉扫描与 Inbox 展示。
- OIDC 正式路径绑定 state、S256 PKCE、RFC 9207 `iss` 与一次性 nonce；shared verification store 在配置 secondary 时使用 Redis，否则使用 DB。sign-in/link、missing/wrong/expired/replayed nonce/state、跨实例、并发与存储更新失败均有真实 BetterAuth handler focused tests。
- 最终本地确定性 Authentik fixture clean E2E 与独立 reviewer 重跑均为 `1 passed (5.0m) / 1 skipped`；重启 gen1→gen2 同端口、旧实例自然 stale、claims/隐私/伪造拒绝/RBAC/业务拒绝/break-glass 通过，资源与敏感信息扫描归零。
- `skipped` 明确表示外部真实 Authentik 租户凭据未提供；DingTalk 二维码/2FA、PR-063 真实签名发布仍待外部环境验收，不得用 fixture 替代。
- W7 Migration `0127`–`0131` 合入后 journal/snapshot 为 132/132，latest 为 `0131`；各批次 type-check、路径/品牌扫描与 diff-check 均通过。

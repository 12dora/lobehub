# 数据库表与迁移清单

## 1. 目标表

| 模块 | 表                                    | 用途                                          | 关键约束 / 索引                                                                                                                           |
| ---- | ------------------------------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| M01  | platform\_resource\_revisions         | 所有发布型资源不可变快照与 checksum           | resource\_type/resource\_id/revision 唯一；created\_at                                                                                    |
| M01  | platform\_audit\_logs                 | 管理员和系统关键动作追加审计                  | created\_at；actor\_id；target\_type+target\_id                                                                                           |
| M01  | platform\_jobs                        | 分发、迁移、测试和重试任务                    | status+lease\_until；type+idempotency\_key 唯一                                                                                           |
| M04  | users（上游表增量）                   | 封禁 / 会话安全 epoch 与当前 Session 保留例外 | `auth_invalidated_at`、`auth_invalidated_excluded_session_id`；email/username/normalized\_email 的 `lower(...) text_pattern_ops` 前缀索引 |
| M05  | platform\_setting\_policies           | 路径级默认 / 锁定 / 隐藏策略                  | path+status；published revision                                                                                                           |
| M05  | user\_setting\_overrides              | 用户显式设置意图                              | user\_id+path 唯一                                                                                                                        |
| M06  | platform\_managed\_resource\_policies | 受管资源与 enforcement 模式                   | resource 唯一                                                                                                                             |
| M07  | platform\_ai\_providers               | 全局 Provider 定义与连接测试状态              | provider\_key 唯一；status；enabled+sort；connection test attempt CAS                                                                     |
| M07  | platform\_ai\_models                  | Provider 下模型定义                           | provider\_id+model\_key 唯一；enabled+sort；status；provider\_id                                                                          |
| M07  | platform\_ai\_provider\_secrets       | 不可变加密凭据历史                            | provider\_id+fingerprint 唯一；provider\_id 索引；FK `ON DELETE CASCADE`                                                                  |
| M08  | platform\_skills                      | 稳定 Skill identity                           | skill\_key 唯一                                                                                                                           |
| M08  | platform\_skill\_versions             | 不可变 Skill 版本                             | skill\_id+version 唯一                                                                                                                    |
| M09  | platform\_connectors                  | Connector 定义与凭据模式                      | connector\_key 唯一；status                                                                                                               |
| M09  | platform\_connector\_secrets          | 不可变平台 Connector Secret 版本             | connector\_id+kind+fingerprint 唯一；仅服务端密文                                                                      |
| M09  | platform\_connector\_tools            | Connector Tool 和平台策略                     | connector\_id+tool\_key 唯一                                                                                                              |
| M09  | platform\_user\_connector\_bindings   | 每用户 OAuth/Token 绑定                       | user\_id+connector\_id 唯一；status                                                                                                       |
| M09  | platform\_connector\_oauth\_states    | 签名 OAuth state / authorization attempt 生命周期 | state digest 唯一；connector/user/revision 绑定；outcome+finished\_at 一致性                                                |
| M10  | platform\_agents                      | 平台 Agent identity/systemKey                 | agent\_key 唯一；system\_key 部分唯一                                                                                                     |
| M10  | platform\_agent\_versions             | 不可变 Agent 配置和依赖                       | agent\_id+version 唯一                                                                                                                    |
| M10  | platform\_agent\_assignments          | 目标用户 / 角色 / 全局分配                    | agent\_id+target\_type+target\_id                                                                                                         |
| M11  | platform\_identity\_providers         | 外部登录 OIDC Provider                        | provider\_key 唯一；status                                                                                                                |
| M12  | platform\_branding                    | Branding Draft/Published                      | status；revision                                                                                                                          |

## 2. Migration 顺序

1. **Migration 0 / Expand：** 创建 M01 公共表与全部业务空表，所有新列可空或有安全默认值，不改变现有行为。
2. **Migration 1 / RBAC：** 插入平台权限、固定角色和超级管理员种子；先不启用 Guard。
3. **Migration 2 / Settings：** 建策略 / Override，运行 Shadow Resolver；不改用户设置。
4. **Migration 3 / AI：** 导入或创建 Provider/Model Draft，发布测试 Catalog。
5. **Migration 4 / Skill/Connector：** 迁移定义，Token/Secret 单独处理。
6. **Migration 5 / Agent：** 建模板 / 版本 / Assignment，不批量创建用户 Agent。
7. **Migration 6 / Identity/Branding：** 配置 Draft，OIDC 未重启前不激活。
8. **Contract 清理：** 至少经过一个稳定版本后，才评估清理旧路径或无用字段。

### 已实施迁移

- `0117`：M01 平台公共表。
- `0118`：M02 Global RBAC / EasyAuth grant snapshot。
- `0119_m04_users_auth_invalidated_and_search_indexes.sql`：M04 两个安全 epoch 列与三个前缀搜索表达式索引；SQL 支持 `IF NOT EXISTS`，生产可预建 concurrent indexes。
- `0120_m05_settings_policy_visibility_and_bundle.sql`：M05 策略 visibility、settings bundle pointer 与用户 Override revision；SQL 仅含 M05 新对象。
- M06 复用 `platform_managed_resource_policies`，无新增迁移。
- `0121_m07_platform_ai_runtime_safety.sql`：M07 不可变 Provider Secret 历史表，以及 Provider 连接测试的 8 个持久化 / CAS 字段；SQL 仅含 M07 对象。
- `0122_m08_platform_skill_versions.sql`：M08 Skill 不可变版本、checksum / manifest / content / resource 与 Published 指针；无旧对象 DROP/RENAME。
- `0123_m09_connector_catalog_expand.sql`：M09 Connector/Tool/Secret/Binding/OAuth state 扩展；遗留不完整行标记 `migration_required`，不复制旧 Secret。大表唯一索引需先运行 autocommit concurrent predeploy；>10k 且未预建时硬阻断，仅 DBA 明确维护窗口可放行阻塞 fallback。
- `0124_m09_oauth_attempt_outcome.sql`：OAuth authorization attempt 增加 `authorization_outcome` / `finished_at` 与 NOT VALID → VALIDATE 一致性约束，用于 UI 精确识别本次授权结果。
- 当前 `_journal.json` 与 `meta/*_snapshot.json` 均为 125 条 / 份；最新 journal `idx=124`、tag `0124_m09_oauth_attempt_outcome`。M10 预留 `0125`。

## 3. 数据库设计规则

- 所有平台表使用独立前缀 `platform_`，避免与上游核心表语义混淆。
- ID 使用仓库现有 `createNanoId`/`idGenerator` 约定。
- Secret 不写 Revision JSON 和 Audit Diff；业务表仅存加密字段 /fingerprint/configured 状态，M07 另使用不可变 `platform_ai_provider_secrets` 历史表。
- 每个外键明确 `onDelete`；发布历史通常采用 restrict/soft archive，不级联删除。
- 列表查询必须有游标索引；禁止依赖 OFFSET 处理大用户量。
- Migration 必须在空库和脱敏生产快照运行，检查锁时长、索引创建和回滚兼容。

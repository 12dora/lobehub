# 后续开发交接说明

> 更新日期：2026-07-20。本文供**接手后续开发的 agent** 直接续上，自包含，不依赖任何会话临时状态。
> 已完成部分见 `04_W1实施进度记录.md`、`06_W2实施进度记录.md`、`07_W3实施进度记录.md`、`08_W4实施进度记录.md`、`09_W5实施进度记录.md`、`10_W6实施进度记录.md` 与 `11_W7实施进度记录.md`; 开工决策见 `02_开工门禁与业务决策.md`; 环境见 `03_阶段0_环境准备记录.md`。

## 0. 一句话现状

W1–W7 已全部带独立审核合入 `main`。M11 PR-053–057 的 merge commit 为 `0735a097`、`ed11cbc6`、`2ca21c70`、`4fd64e62`、`9a8aee08`、`06c756a7`；M12 扫描前置与 PR-058–062 为 `78ae6857`、`7a8eb323`、`b39f8127`、`11febb5d`、`461471b9`、`4a52abad`。W8 Batch 1 的 Vault Key Provider 与安全策略 baseline 已分别以 `88d73f8e`、`ad613ed1` 合入。**当前仍是 W8：继续收口 M13/M14/M15 的全域 re-wrap/job、生产凭据轮换 / 告警 / 灾备、可观测、多实例、迁移回滚、上游 Rebase 与生产验收。**

## 1. 剩余未完成工作 (按波次)

| 波次 | 模块                             | 说明                                                                                                                       | 前置依赖 | 特殊要求                                                   |
| ---- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------- |
| W8   | **M13 剩余** + **M14** + **M15** | 补齐 registry gap、限流、全域 re-wrap/job、生产 Vault 运维；缓存 / 可观测 / 多实例；全量测试 / 迁移回滚 / 上游 Rebase 演练 | W1–W7    | 集中收口；fake、临时基础设施或缺凭据 skip 不得记作生产完成 |

**不得调整的顺序**(来自 `00_实施顺序.md` §3):M05→驱动运行时；M06→M07–M10 托管；M07–M09→M10;M11 独立灰度；M12 最后切名。

W7 已完成 M11/M12：数据库 OIDC Provider、LKG、受控重启、可信 claim、state/PKCE/nonce、Runtime Branding、Admin / 用户侧消费者与 Inbox 展示均已带独立审核合入。确定性 Authentik fixture 与独立 reviewer clean E2E 均为 `1 passed / 1 skipped`；skip 是外部真实租户凭据缺失，不能冒充真实 Authentik/DingTalk 验收。nonce binding 使用 BetterAuth 当前 shared verification store（配置 secondary 则 Redis，否则 DB），不使用进程 Map、全局 verification 双写或伪 CAS。已实施迁移新增 `0127`–`0131`，当前 journal/snapshot 为 132/132，latest 为 `0131`。PR-063 的发布代码与 W7 集成修复已完成，但真实签名、受保护 environment、私有品牌资产与 S3 发布未执行，不能写成已发布。

W8 Batch 1 已把 W1 的 Vault stub 替换为应用级 KV v2 provider：支持 AppRole（优先）或显式低权限 token，`lookup-self` 拒绝 root，读取 active/history，处理 lease/cache/single-flight；SecretID provider 使用独立 deadline，每次 Vault HTTP 请求使用各自的 deadline，size limit 只约束 HTTP 响应流。sealed、权限、网络或 schema 失败均 fail closed，选择 Vault 后不会回退 Env。默认 fake 测试为 `61 passed / 1 real gate skipped`；独立临时 Vault gate 为 `1 passed`，仅证明集成路径，不代表生产 Vault 或实际发布验收。

## 2. 编排工作流 (本项目约定，必须沿用)

**每模块一个 git worktree，实现子代理高强度实施 → 编排 agent 派出的独立审核子代理审核 → REWORK 交回原实现子代理，同一 reviewer 复核至 PASS → 合入 main。并行最多 2 个实现子代理。审核子代理不是实现代理自审，也不由实现代理派出。**

W4–W6 按用户指示由编排 agent 派出的实现子代理接手编码；独立 reviewer、原实现代理返工、同 reviewer 复核至 PASS 与分批提交 / 推送规则未改变。后续若恢复 Grok，`--reasoning-effort high` 仍为硬性要求。

### 2.1 起一个模块

```bash
export PATH=/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.bun/bin
git worktree add /Users/konata/code/AIHub-worktrees/<mXX> -b feat/<mXX>-<name> main
cp <repo>/.env.development /Users/konata/code/AIHub-worktrees/<mXX>/   # gitignored,含 dev 端口与 Authentik 配置
cd /Users/konata/code/AIHub-worktrees/<mXX> && pnpm install --prefer-offline
# 写一份 prompt-<mXX>.md(见 §2.4 要点),用新 uuid 起 headless grok:
grok --prompt-file <prompt> -s $(uuidgen|tr A-Z a-z) --reasoning-effort high --r-approve --no-plan --check
```

- **`--reasoning-effort high` 是硬性要求**(用户明确指定)。
- grok CLI:`/opt/homebrew/bin/grok`;headless 单轮用 `-p/--prompt-file`,`--check` 追加自校验循环。

### 2.2 返工 (保留 grok 会话上下文)

```bash
grok --resume <同一 uuid> --reasoning-effort high --always-approve --no-plan --check --prompt-file <rework-<mXX>-rN.md>
```

### 2.3 审核 (编排 agent 派出的独立子代理，不是 Grok)

- 由当前编排 agent 使用子代理工具派出 general-purpose 或专职审核 agent，范围 `main...HEAD`,**只读 + 跑测试，不改代码**；不得让 Grok 派审核或用 Grok 自检替代。
- 审核依据：模块 `plan/Mxx*.md` 逐节 DoD + `list/04_权限矩阵.md` + `list/06_验收矩阵.md` + `AGENTS.md`; 安全项 (Secret 泄露 / Flag 默认关 / 越权 / SSRF) 必查；实证验证 (自建对抗测试、探针、迁移快照比对), 不采信 grok 自述。
- 输出首行 `VERDICT: PASS` 或 `VERDICT: REWORK` + 编号问题清单 (blocker/major/minor + 文件：行 + 给 grok 的修复指令)。
- 复核用 SendMessage 续同一审核 agent (保留上轮上下文)。
- **教训:grok 每个模块都自报 PASS, 但 W1 四个模块首轮审核全部 REWORK**—— 自检不可信，审核不能省。

### 2.4 派活 prompt 要点 (每个都要写清)

按 PR 顺序实施；每 PR `bun run check`(变更文件) 全绿 + 涉及服务端跑 `bun run check --type` 全仓 0 error 才 gitmoji 提交；**禁全量 `bun run test`**(\~10 分钟); 禁 push、禁建 PR; 上游修改登记 `list/07` + `docs/enterprise-patches/patch-ledger.md`;Flag 默认关、关闭时行为与上游一致 (回归测试);Secret 禁入代码 / 测试 / 文档 / 日志；i18n 只写 en-US+zh-CN、禁跑 `bun run i18n`;**并行分区**(见 §3)。

### 2.5 合入

```bash
git merge --no-ff --no-verify feat/ "🔀 merge: Mxx …(审核通过)" < mXX > - < name > -m
git worktree remove /Users/konata/code/AIHub-worktrees/ < mXX > --force
```

## 3. 并行防冲突

- worktree 隔离 + 避开共享热点文件:**两份 desktop router 配置**(`src/spa/router/desktopRouter.config.tsx` 与 `.desktop.tsx` 必须同步，`desktopRouter.sync.test.tsx` 守护)、**locales 公共文件**、**`packages/const` 桶导出**、**`list/07` 台账**(合并时手工取并集)。
- 每个 prompt 显式写 "另一 agent 在实施 Myy, 你不要碰 <它的目录>"。
- 契约先行：服务端 agent 先定稿 tRPC contract + zod schema (小 PR),UI agent 对着契约并行开发，最后联调 (工作流 B)。

## 4. 遗留移交事项 (接手时按归属处理)

| 事项                                                                                                                                                                   | 归属                                          | 详情位置                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| redaction 对 `maxTokens`/`contextWindowTokens` 过度脱敏                                                                                                                | **已由 M07 闭环**                             | `08_W4实施进度记录.md`；仅通过窄 `isBenignKey` 保留安全数值字段，Secret 形态仍脱敏                                                                                                                                                              |
| EasyAuth 定时同步 worker (`platform.easyauth.sync_user` job 仅入队无消费者，登录 hook 已兜底)                                                                          | 后续 / M14                                    | 04 §3                                                                                                                                                                                                                                           |
| `easyauthLoginSync`/`accessStatus` 因 path-boundary 各重实现 token 读取 / 映射，抽到 `packages/const` 共享                                                             | 重构                                          | 04 §3                                                                                                                                                                                                                                           |
| VaultKeyProvider 应用接入                                                                                                                                              | **W8 Batch 1 已完成；M13/M14/M15 / 运维继续** | AppRole / 显式低权限 token、root 拒绝、KV v2 active/history、lease/cache/single-flight、有界流 / 期限与 fail-closed 已完成；剩余全域 re-wrap/job，以及生产凭据轮换、告警、灾备和真实发布验收，见 04 §3 与 `docs/security/vault-key-provider.md` |
| M14 企业 Prometheus 参考告警（12 `ENTERPRISE_ALERT_INTENTS`）                                                                                                          | **W9 O06 已落地参考规则；生产通知仍属运维**   | 已提供 checked-in PromQL + compose 只读挂载 + `promtool` fail-closed 校验与 intent↔YAML 1:1 对账；**未**配置 Alertmanager 接收端，**不**宣称已部署生产；阈值 / 路由 / 值班见 `docs/enterprise/runbooks/enterprise-prometheus-alerts.md`         |
| SafeOutboundHttpClient 残余：已由 M09 闭环 NAT64/SIIT、Metadata、DNS pin / 重定向、动态 allowlist；仍无通用端口白名单和基础层 content-type 校验，依赖宿主 DNS resolver | M13/M14/M15 剩余                              | `apps/server/src/enterprise/security/outboundHttp/RESIDUAL.md`；按 caller 收紧、限流 / 威胁模型 / 运维监测                                                                                                                                      |
| M09 遗留 Connector 行                                                                                                                                                  | 上线数据治理                                  | `migration_required` 行不进入管理列表 / Runtime；上线前按 `0123_m09_connector_catalog_predeploy.md` 处理，不自动复制旧 Secret                                                                                                                   |
| M10 真实 PostgreSQL rollout 门禁                                                                                                                                       | **上线前 / M15**                              | `rolloutService.multiconn.pg.test.ts` 的 3 个多连接 / 索引用例需同时设置 `TEST_SERVER_DB=1` 与 `DATABASE_TEST_URL`；W6 本地环境未执行，常开 PGlite 不能替代                                                                                     |
| PR-063 首次真实 AIHub 桌面发布                                                                                                                                         | **发布运维**                                  | 代码与 focused tests 已 PASS；真实签名、仅允许 `main` 的 `aihub-desktop-release` environment、固定私有品牌资产 commit 与专用 S3 发布仍需外部环境留证                                                                                            |
| M13 剩余：M04 已落地管理危险操作 reauth guard；W8 已落地威胁模型与 admin mutation registry baseline，继续通用化、补 registry gap、审计加固、限流、渗透 / 生产验证      | **W8**                                        | plan/M13、`docs/security/enterprise-threat-model.md` 与 06 §3                                                                                                                                                                                   |
| PGlite 交错模拟 → `TEST_SERVER_DB=1` 真 PG 竞争进回归；2.2.10 快照升级测试；审计 / Job 保留期清理；运行手册                                                            | **M15**/ 运维                                 | 04 §3                                                                                                                                                                                                                                           |
| Q06 生产 Preflight / 备份恢复 / 应用回滚演练 / Runbook（harness 已落地；生产授权证据与外部 Vault / 签名 / 租户仍未执行）                                               | **M15 / 运维**                                | `docs/redevelopment/overview/14_Q06生产就绪门禁运行说明.md`、`docs/enterprise/runbooks/**`、`scripts/enterprise/production-readiness/**`                                                                                                        |
| pathBoundaries 对 `*.test.*` 全局豁免待收紧                                                                                                                            | **M15**                                       | scripts/enterprise                                                                                                                                                                                                                              |

## 5. 仓库坑 (踩过的，务必知道)

1. **blob:none 部分克隆**:`git commit`/`git worktree add` 会触发向 GitHub 惰性拉取对象，慢属正常，不是死锁。
2. **主仓 main 提交必须 `--no-verify`**:`.githooks/pre-commit` 在 main 分支跑全量 type-check (慢), 且 lint-staged 的 remark/prettier 会重写 `.md` 破坏 `MANIFEST.md` 的 sha256。
3. **改了 `docs/redevelopment/` 任何文件后必须刷新 MANIFEST**:`MANIFEST.md` 是 `| 路径 | 字节 | sha256 |` 表，用 legacy 目录名 (overview→`00_实施总览/`、plan→`01_实施模块/`、list→`02_工程清单/`、ref→`03_参考资料/`)。用 python 重算改动文件的 len+sha256 回填对应行。
4. **shell PATH 偶发丢失**: 命令开头统一 `export PATH=/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.bun/bin`。
5. **drizzle 迁移生成陷阱**:`drizzle-kit generate` 可能因既有表索引 where 大小写漂移 (`is null`↔`IS NULL`) 误产生 DROP/CREATE INDEX—— 生成后必须 diff 确认 SQL 只含本模块新对象；`.prettierignore` 已忽略 `migrations/meta/` 防重排。**`_journal.json` 条目数必须 == `meta/*_snapshot.json` 数**(当前 132/132，latest `0131`；缺 snapshot 会导致下次 generate 炸库)。
6. **dev 基础设施**:`docker-compose/dev/docker-compose.phase0.yml`(项目名 aihub-dev,postgres:5433/redis:6380/rustfs:9010),`bun run db:migrate` 验证迁移；真实登录见 03 文档 (env 直配 Authentik, Generic OAuth 回调为 `/api/auth/oauth2/callback/authentik`)。

## 6. 参考项目 (实现 M11 等身份模块前只读分析)

- Authentik (含钉钉适配):`/Users/konata/code/Authentik`(auth.jiefakj.com)
- EasyAuth (授权中心 + SDK):`/Users/konata/code/EasyAuth`(iam.jiefakj.com)
- EasyTrade (登录 / 鉴权参照):`/Users/konata/code/EasyTrade`(etrade.jiefakj.com)
- 关键代码落点见 `02_开工门禁与业务决策.md` §2.1。

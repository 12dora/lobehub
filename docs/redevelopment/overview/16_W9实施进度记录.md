# W9 实施进度记录

> 更新日期：2026-07-21。本文记录 W8 实现波次关闭后、在 `main` 上独立审核合入的三项补强：**M13 S06** 管理运行时加固、**M13 S07** 安全验收 harness、**M14 O06** Prometheus 参考告警。
> 本波继续采用独立 worktree：编码代理完成一项后由编排 agent 派出独立 reviewer；REWORK 退回原编码代理，同一 reviewer 复核至 PASS 后再合入 `main`。
> **结论**：S06 / S07 / O06 均已以 reviewed merge 合入 `main`（first-parent 终态 `8cea7a61`）。下列事实均来自 `git log --first-parent` / `git log <merge>^1..<merge>^2` / `git show` 与磁盘实测；**不**宣称生产部署、生产告警通知或外部渗透验收。

## 1. 已落地 main 的批次

| 项 | first-parent merge | 主题 | 返工（分支提交消息） | 关键交付（自 merge range 归纳） |
| -- | ------------------ | ---- | -------------------- | -------------------------------- |
| S06 | `d361e91c` `🔀 merge: M13 S06 admin runtime hardening (reviewed)` | 管理运行时加固 | RR1–RR4（`aea2e129` / `0a1a69f4` / `208011c0` / `6de9e6a8`）+ 初版 `5fa953ca` | 管理 mutation 多实例限流（PostgreSQL 为唯一权威计数）；迁移 `0137_m13_admin_mutation_rate_windows`；AI 连接测试 / EasyAuth 等出站路径 SafeOutbound 覆盖（含 Vertex / Azure / Bedrock 返工） |
| S07 | `f4fbce95` `🔀 merge: M13 S07 security acceptance (reviewed)` | 安全验收 harness | 多轮 fix 提交（含 `bc07c155`、`10c0c21c`、`a3d914e3`、`21d43286`、`7ee836d9`、`9143d652`）+ 初版 `3268bd85` | 仓库内 fail-closed 安全验收脚本与 CI workflow；报告写路径与 artifact-bound 完整性；泄漏扫描 baseline 绑定；进程表快照完备性与 fail-closed 进程探测 / 清理 |
| O06 | `8cea7a61` `🔀 merge: M14 O06 Prometheus alerts (reviewed)` | Prometheus 参考告警 | rework r1–r4（`70d41ef9` / `2aa6cd5a` / `02b96842` / `f10a05e6`）+ 初版 `d162a3f4` | `ENTERPRISE_ALERT_INTENTS` ↔ YAML `alert:` 1:1；checked-in PromQL 参考规则；`promtool` fail-closed 校验；compose 规则目录只读挂载；runbook `docs/enterprise/runbooks/enterprise-prometheus-alerts.md` |

证据命令（可复跑）：

```bash
git log --first-parent main --oneline | head -5
# 8cea7a61 🔀 merge: M14 O06 Prometheus alerts (reviewed)
# f4fbce95 🔀 merge: M13 S07 security acceptance (reviewed)
# d361e91c 🔀 merge: M13 S06 admin runtime hardening (reviewed)
# 39147bb9 🔀 merge: M15 production readiness Q06 (reviewed)

git log d361e91c^1..d361e91c^2 --oneline
git log f4fbce95^1..f4fbce95^2 --oneline
git log 8cea7a61^1..8cea7a61^2 --oneline
```

## 2. S06 — 管理运行时加固（merge `d361e91c`）

### 2.1 合入与返工

- first-parent merge：`d361e91c`。
- 功能初版：`5fa953ca`（admin mutation 限流、出站 SafeOutbound、审计缺口收口）。
- 返工提交消息显式编号 **RR1–RR4**：
  - RR1 `aea2e129`：provider SafeOutbound、PG rate fallback、Redis 前缀。
  - RR2 `0a1a69f4`：SafeOutbound 覆盖 Vertex/Azure、**PG-only** rate、迁移 `0137`。
  - RR3 `208011c0`：race-safe cleanup、sargable index、Bedrock abort。
  - RR4 `6de9e6a8`：持久化 `window_ms`、ctid-bounded cleanup。

### 2.2 关键交付（有 diff 证据）

| 交付 | 证据落点 |
| ---- | -------- |
| 管理 mutation 限流器（PostgreSQL 为唯一权威窗口计数；进程内 Map 仅作单测 double） | `apps/server/src/enterprise/security/rateLimit/adminMutationRateLimiter.ts`（S06 range） |
| 表 `platform_admin_mutation_rate_windows`；`window_ms` 为权威窗口时长（副本配置仅在 rollover 时采纳） | 迁移 `packages/database/migrations/0137_m13_admin_mutation_rate_windows.sql`；RR4 `6de9e6a8` |
| AI 连接测试 / EasyAuth 等出站走 SafeOutbound；Vertex / Azure / Bedrock 传输路径在 RR 中补齐 | `connectionTestService*`、`azureFetchHttpClient`、`vertexai` / `bedrock` provider 改动（S06 range） |
| 威胁模型与 admin mutation registry 同步，消除 live gap/planned 残留表述 | `docs/security/enterprise-threat-model.md`、`adminMutationRegistry.ts`（S06 range） |

### 2.3 诚实边界

- 限流与 SafeOutbound 补强是**代码与迁移**合入；**不**等于生产渗透验收或外部对抗演练通过。
- Redis 双写限流路径在 RR2 后移除（PG-only）；勿把初版 `5fa953ca` 的 Redis 路径写成终态。

## 3. S07 — 安全验收（merge `f4fbce95`）

### 3.1 合入与返工

- first-parent merge：`f4fbce95`。
- 功能初版：`3268bd85` `feat(enterprise): M13 PR-S05 security acceptance harness`。
- 后续 fix（merge range 内，非完整枚举可省略次要 integrate merge）：
  - `bc07c155`：bind security acceptance integrity and leakage baseline。
  - `10c0c21c`：security acceptance report write path and baseline freshness。
  - `a3d914e3`：process-tree cleanup and skip multiset。
  - `21d43286`：fail-closed process discovery and existence probes。
  - `7ee836d9`：strict process-table snapshot completeness。
  - `9143d652`：reject unsafe PID values and illegal map entries。
  - `d059f954`：integrate main (S06) into S07（同步合入，非产品 feature）。

### 3.2 关键交付（有 diff 证据）

| 交付 | 证据落点 |
| ---- | -------- |
| 安全验收 harness 与 CLI（`bun run enterprise:security-acceptance`） | `scripts/enterprise/security-acceptance/**`、`package.json` |
| CI workflow | `.github/workflows/enterprise-security-acceptance.yml` |
| Runbook（明确 harness ≠ 外部生产渗透） | `docs/enterprise/runbooks/security-acceptance.md` |
| 报告写路径、artifact-bound 完整性 digest、verify 重算语义 | `10c0c21c`、`bc07c155`；`evaluate.ts` / report schema |
| 泄漏扫描与 reviewed `leakage-baseline.json` 精确指纹绑定 | `leakageBaseline.ts`、`leakage-baseline.json`、`bc07c155` |
| 进程发现 / 存在性探测 fail-closed；进程表快照完备性；不安全 PID 拒绝 | `process.ts`；`21d43286`、`7ee836d9`、`9143d652` |

### 3.3 诚实边界（直接来自 runbook 与 harness 字段）

- harness **是**仓库内确定性自动化（dependency / leakage / pen-regression adapters）。
- harness **不是**已完成的外部 / 人工**生产渗透测试**；报告固定 `evidenceClass: "repository-automation"`、`externalPenetrationTest.status: "not-executed"`。
- 本地 / CI 绿 **禁止**改写为「生产安全已验收」。

## 4. O06 — Prometheus 参考告警（merge `8cea7a61`）

### 4.1 合入与返工

- first-parent merge：`8cea7a61`（亦为当前文档波次基线 `HEAD` 祖先）。
- 功能初版：`d162a3f4` `feat(enterprise): executable Prometheus reference alert rules (W9 O06)`。
- 返工提交消息：**rework** / **rework r2–r4**（共 4 轮）：
  - r1 `70d41ef9`：Prometheus startup、stale alert、OTLP proof。
  - r2 `2aa6cd5a`：per-collector stale、independent probe、fail-closed cleanup。
  - r3 `02b96842`：collector enabled gate + unaltered builders。
  - r4 `f10a05e6`：no-data prose、ready-absent fixtures、vocab import。
- 另有 integrate main `2e5edcb2`（同步 S06/S07，非产品 feature）与文档/样式类提交。

### 4.2 关键交付（有 diff 证据）

| 交付 | 证据落点 |
| ---- | -------- |
| 12 条 `ENTERPRISE_ALERT_INTENTS`，`status: 'reference-rule'`，与 YAML `alert:` **1:1** | `apps/server/src/enterprise/observability/alertIntents.ts` + unit tests |
| 参考规则 YAML + promtool 语义 fixtures | `docker-compose/production/grafana/prometheus/rules/enterprise-platform-alerts.yml`、`.test.yml` |
| compose Prometheus 规则目录**只读**挂载 | `docker-compose/production/grafana/docker-compose.yml`：`./prometheus/rules:/etc/prometheus/rules:ro` |
| 权威校验：Docker 不可用 / 校验失败 **fail-closed** | `scripts/enterprise/prometheus-alerts/checkRules.ts`（`promtool check rules` / `test rules`） |
| OTLP→Prometheus 探针与 collector 校验脚本 | `otlpPrometheusProbe.ts`、`collectorValidate.ts` 等 |
| 运维 runbook（明确无 Alertmanager 接收端、不宣称生产部署） | `docs/enterprise/runbooks/enterprise-prometheus-alerts.md` |

### 4.3 诚实边界

- **未**在参考 compose 中配置 Alertmanager（或其它）接收端；规则可在 Prometheus UI 观察，**≠** 生产通知已开通。
- 阈值 / 路由 / 值班 / 抑制策略属**部署侧**；仓库仅提供参考 PromQL 与校验门禁。
- harness / 本地 Docker 探针 **≠** 生产集群已部署或已验收。

## 5. 数据库与质量状态（磁盘实测）

实测命令（本 closeout 工作区，2026-07-21）：

```text
packages/database/migrations/meta/_journal.json entries == 138
packages/database/migrations/meta/*_snapshot.json == 138
latest journal tag: 0137_m13_admin_mutation_rate_windows
```

- W9 S06 新增迁移 **`0137_m13_admin_mutation_rate_windows`**（表 `platform_admin_mutation_rate_windows`）。
- S07 / O06 **无**新增 Drizzle 迁移（以各自 merge diffstat 为准）。
- W8 终态 `39147bb9` 时为 137/137 / latest `0136`；见 `15_W8实施进度记录.md` §3。二者不要混写。

## 6. 与计划关系 / 禁止表述

- **实现**：W1–W8（M00–M15）+ W9 补强 S06/S07/O06 均已 reviewed 合入 `main`。
- **生产授权 / 外部运维**：**仍未完成**（真实 Vault 告警运营、Alertmanager 接收与值班、外部渗透签字、生产签名 / 租户 / S3 等）；见 `05_后续交接说明.md`。
- **禁止**：把 harness、local、CI、fixture、fake provider、skip、`unverified`、参考规则文件存在写成生产验收或生产已部署。

## 7. 参考路径

| 路径 | 用途 |
| ---- | ---- |
| `docs/enterprise/runbooks/security-acceptance.md` | S07 安全验收 runbook |
| `docs/enterprise/runbooks/enterprise-prometheus-alerts.md` | O06 参考告警 runbook |
| `docs/security/enterprise-threat-model.md` | 威胁模型（含 S06 更新） |
| `packages/database/migrations/0137_m13_admin_mutation_rate_windows.sql` | S06 限流窗口迁移 |
| `apps/server/src/enterprise/observability/alertIntents.ts` | O06 intent 元数据 |
| `docs/redevelopment/list/05_建议PR拆分.md` | 已实施状态（含 W9 三行） |
| `docs/redevelopment/overview/15_W8实施进度记录.md` | W8 终态与非生产边界 |

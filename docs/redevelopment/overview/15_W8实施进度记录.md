# W8 实施进度记录

> 更新日期：2026-07-21。本文记录 M13 安全横切、M14 一致性 / 可观测 / 故障演练、M15 测试 / 迁移 / 上游 Rebase / 生产就绪门禁的实际交付、独立审核与合入结果。
> 本波继续采用独立 worktree：编码代理完成一项后由编排 agent 派出独立 reviewer；REWORK 退回原编码代理，同一 reviewer 复核至 PASS 后再合入 `main`。
> **结论（实现波次）**：`docs/redevelopment` 范围内 W1–W8 / M00–M15 的**开发实现波次已全部合入并独立审核**；本计划**无剩余实施波次**。**生产授权与外部运维证据未完成**，见 §5；本地 / CI / harness 证据**不得**写成生产验收。

## 1. 范围与终态

| 维度             | 状态                                                                                                         |
| ---------------- | ------------------------------------------------------------------------------------------------------------ |
| 计划实现范围     | M13（PR-S01–S05）、M14（PR-O01–O05）、M15（PR-Q01–Q06）代码与门禁 harness                                    |
| 独立审核         | 各 merge 消息均为 reviewed 合入；最终生产就绪 Q06 merge 为 `39147bb9`                                        |
| 本计划下一实现波 | **无**（W1–W8 实现关闭）                                                                                     |
| 生产 / 外部运维  | **未完成**（Vault 生产轮换告警、签名 / HSM、真实租户、私有资产 / S3、生产备份 attest、可执行旧 baseline 等） |

基线：文档收口与 Q06 合入点同为 `39147bb9`（`🔀 merge: M15 production readiness Q06 (reviewed)`）。

## 2. 已落地 main 的主要批次

下列 commit 均来自 `main` 上带 `🔀 merge:` 的 reviewed 合入记录（`git log --merges`），不臆造未合入项。

### 2.1 M13 安全（PR-S01–S05）

| 主题                             | 代表 merge commit | 交付要点                                                                         |
| -------------------------------- | ----------------- | -------------------------------------------------------------------------------- |
| 安全原语（W1 前置，W8 继续消费） | `0c66b95e`        | PlatformSecretService / Redaction / SafeOutboundHttpClient                       |
| Vault Key Provider               | `88d73f8e`        | KV v2 AppRole 优先、root 拒绝、lease/cache/single-flight、fail-closed 不回退 Env |
| 安全策略 / 威胁模型 baseline     | `ad613ed1`        | 机器可读威胁模型与 admin mutation registry 基线                                  |
| Batch 1 集成文档                 | `896b2f1e`        | 交接与 Vault / 威胁模型状态对齐                                                  |
| Secret 轮换 foundation           | `c36f0743`        | 精确 CAS 轮换契约                                                                |
| Secret re-wrap worker            | `a9641699`        | 持久化 rewrap 作业、有界批次、失败 ledger                                        |
| 审计轮换 API                     | `960ea749`        | 受审计的 rotation API 与冲突分类                                                 |
| Admin 授权矩阵 / fixture         | `0f627aa1`        | procedure 授权 registry 与测试工厂                                               |
| Admin reauth 缺口                | `c25d8943`        | identity / EasyAuth 等 reauth 守卫补齐                                           |
| 条件 Secret reauth               | `278d60bf`        | 敏感 Secret 变更路径 reauth 与审计脱敏                                           |

W8 新增迁移：`0132`–`0134`（secret rotation / rewrap 索引与单活约束）。生产 Vault 凭据轮换、告警与灾备**未**在本波执行。

### 2.2 M14 一致性 / 可观测 / 故障演练（PR-O01–O05）

| 主题                        | 代表 merge commit                                                      | 交付要点                                                                           |
| --------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 领域配置缓存                | `5c652744`、`4dab427e`                                                 | 域缓存命名空间隔离；真实 Redis 收敛门禁                                            |
| 实例心跳与收敛              | `f348b5f6`、`8ae51f41`                                                 | 心跳 foundation、收敛状态服务；迁移 `0135`                                         |
| 可观测核心                  | `d699dd00`、`cb4c0cf1`                                                 | 企业平台 OTel / 安全事件核心                                                       |
| 分域 reporters              | `e0730161`、`156bdbdb`、`979b940a`、`1fc29616`、`d074da01`、`e38bd6ac` | 发布 / SSRF / agent materialization / settings·branding / AI·skill / OIDC 登录观测 |
| 运维 metrics                | `f7682ab3`                                                             | operational metrics 生命周期                                                       |
| 故障门禁 harness            | `d40e882b`                                                             | failure-drills collect/verify 与 CI 契约                                           |
| Admin System API / UI       | `945735c0`、`b51be63a`（及后续 UI 对齐 merge）                         | build / dependency / flag / domain / OIDC / instance / job 运维面                  |
| 多实例 Redis / Job 恢复演练 | `a72c25f0`、`e3582d44`                                                 | 可处置环境上的多 suite 真演练证据链                                                |

运行说明：`12_O05a故障门禁运行说明.md`。缺依赖、skip、0 assertion 或清理失败均不得记通过。

### 2.3 M15 测试 / 迁移 / Rebase / 生产就绪（PR-Q01–Q06）

| PR                          | 代表 merge commit                     | 交付要点                                                                                                   |
| --------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Q01 测试 Fixture / 授权工厂 | `0f627aa1`（与 M13 授权矩阵同源合入） | admin authorization fixture 与 registry 测试底座                                                           |
| Q02 权限矩阵回归            | `8b0a0d8a`                            | permission matrix / Managed Guard 回归门禁                                                                 |
| pathBoundaries 收紧         | `9bcde953`                            | 取消 `*.test.*` 全局豁免；改为精确 `(file, importSpecifier)` allowlist                                     |
| Q03 Migration 兼容          | `1d357c74`、`ca48bc60`                | journal/meta 回归；`verify-migration` 与迁移 `0136`                                                        |
| Q04 企业 Admin E2E          | `453d0bb4`                            | `e2e/enterprise-admin/**` Playwright 关键路径与证据脚本                                                    |
| Q05 上游 Rebase 干跑        | `9936d822`、`cb84ffee`                | 只读 rebase-report + `enterprise-upstream-rebase` CI；**无** push / 生产 Rebase 授权                       |
| Q06 生产就绪门禁            | **`39147bb9`**                        | preflight / recovery-drill / runbooks / 签名 provenance 契约；**生产 overall=passed 不可达**（无生产公钥） |

运行说明：

- `13_Q05上游Rebase干跑门禁运行说明.md`
- `14_Q06生产就绪门禁运行说明.md`
- `docs/enterprise/runbooks/**`、`scripts/enterprise/production-readiness/**`

## 3. 数据库与质量状态（诚实记录）

- W8 相关迁移：`0132`–`0136`；当前 Drizzle journal 与 snapshot 均为 **137/137**，latest 为 `0136_m11_identity_secret_state_null_guard`（以仓库 `packages/database/migrations/meta/_journal.json` 为准）。
- 各合入批次经独立 reviewer；合入消息统一为 reviewed merge。本 closeout **不**复跑全量测试套件，也不把历史 harness 结果改写成新的生产通过。
- 已文档化的本地 / 集成门禁边界（摘自既有记录，非本 closeout 新测）：
  - Vault：默认 fake 路径与独立临时 Vault gate 仅证明集成；**≠** 生产 Vault。
  - O05a / Redis / multi-worker drills：需可处置 PG/Redis；缺服务只能报静态校验。
  - Q05：dry-run 证据，非上游同步机器人，非生产 merge 授权。
  - Q06：`validate-harness` / CI 契约绿 **禁止** 发出 production `overall=passed`；仓库 `PRODUCTION_TRUST_POLICY` 无生产公钥且 `productionPassEnabled=false`。
  - Q04 Admin E2E / 外部真实 Authentik·钉钉：缺外部租户凭据时不得记外部验收。
  - baseline ORM：钉死 `4bab1636…`；无完整 baseline 安装时 `baselineExecutable=false` / `unverified`。

## 4. pathBoundaries 与测试豁免（M15 已落地证据）

- 历史遗留「`pathBoundaries` 对 `*.test.*` 全局豁免待收紧」已在 merge `9bcde953`（`M15 enterprise path boundary hardening`）落地：生产 / 测试导入改为精确 allowlist，新增测试须登记 `(file, importSpecifier)` 对。
- 该项从「M15 待办」转为**已实现**；后续仅维护 allowlist 条目，不构成新的实现波次。

## 5. 明确非完成（仍属外部 / 运维，非实现波）

下列项在代码与 harness 中可能已有契约或本地路径，但**生产授权与外部环境证据未完成**，不得写入「生产验收通过」：

1. 经评审的**生产签名密钥 / HSM** 与 release environment 授权。
2. **真实 Vault** 轮换、告警、灾备与生产凭据运营（非临时集成 Vault）。
3. **真实租户** Authentik / 钉钉二维码 / 2FA E2E（fixture PASS ≠ 外部租户验收）。
4. **私有品牌资产 / 受保护 environment / S3** 上的真实 AIHub 桌面签名发布（PR-063 代码合入 ≠ 已发布）。
5. **生产库** backup + source-manifest + recovery-result **attest** 与签字演练。
6. **可执行旧 baseline** 完整 monorepo 环境（当前诚实 `unverified`）。
7. 真实上游 `lobehub/lobehub` 周更 schedule 稳定性、生产 Rebase/merge 执行与发布签字。

## 6. 与本计划的关系

- **开发实现**：W1–W8 / M00–M15 在本仓库 reviewed 合入意义上 **complete**。
- **生产授权 / 外部运维**：**not complete**；交接见 `05_后续交接说明.md`（日期 2026-07-21）与 Q05/Q06 运行说明。
- **禁止表述**：把 harness、local、CI、fixture、fake provider、skip 或 `unverified` 写成生产 acceptance。

## 7. 参考路径

| 路径                                               | 用途                                 |
| -------------------------------------------------- | ------------------------------------ |
| `docs/security/vault-key-provider.md`              | Vault 配置与轮换边界                 |
| `docs/security/enterprise-threat-model.md`         | 威胁模型 baseline                    |
| `docs/enterprise/runbooks/`                        | 发布 preflight / 回滚 / 灾备 runbook |
| `docs/redevelopment/list/05_建议PR拆分.md`         | PR-S/O/Q 实施状态                    |
| `docs/redevelopment/list/07_上游直接修改点台账.md` | W8 上游改动登记                      |
| `docs/enterprise-patches/patch-ledger.md`          | 补丁台账（若有并行登记）             |

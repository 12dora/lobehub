# W1 实施进度记录

> 更新日期:2026-07-16。本文记录 grok build 实施 + Claude 子代理审核的执行结果,供后续开发 agent 直接接续。
> 工作流:每模块一个 git worktree,grok 高强度实施 → 独立子代理审核 → REWORK 则 `grok --resume` 返工至 PASS → 合入 main。所有模块首轮均 REWORK,均已修复合入。

## 1. 已落地 main 的模块(W1)

| 模块                                                | 分支(已合入)                 | merge commit | 审核结论 | 关键修复(返工 1 轮)                                                                                                                                                                                                         |
| --------------------------------------------------- | ---------------------------- | ------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M00 企业扩展骨架 + Feature Flags                    | feat/m00-enterprise-skeleton | `fda917a`    | PASS     | type-check 红灯;Flag 关闭仍发 2 次冷启动请求 → 补第 4 挂载点(`serverConfig.enterprise.enabled` 门控);getCapabilities 收权为 authedProcedure                                                                                 |
| M01 平台数据库/Revision/Audit/Job                   | feat/m01-platform-db         | `0a90412`    | PASS     | 0117 迁移误 DROP 上游 `user_connectors` 索引(drizzle 快照 where 大小写漂移)→ 剥离;审计脱敏漏 camelCase(accessToken/idToken 等)→ 归一化 + 词尾匹配                                                                           |
| M13 安全原语(3 个)                                  | feat/m13-security-primitives | `0c66b95`    | PASS     | SafeOutboundHttpClient 响应体无界缓冲 OOM → 流级 `maxResponseBytes` + 绝对 wall-clock deadline;跨源重定向剥离凭据头                                                                                                         |
| M02 Global RBAC + EasyAuth 同步 + aihub.access 准入 | feat/m02-global-rbac         | `cff5b55`    | PASS     | B2 user_admin 可降级他人 super_admin(越权)→ gate;B1 审计游标破坏 M01 测试 → NaN 守卫;B3 准入 gate 零挂载 → 挂全局 authedProcedure + 登录 hook 同步;B4 0118 迁移缺 drizzle snapshot;M1 last-super TOCTOU → 事务内 FOR UPDATE |

> M02 是全项目授权地基;审核含权限矩阵逐行→测试映射(list/04 全覆盖)+ 自建对抗测试(user_admin 降级 super_admin → FORBIDDEN、并发不清零超管)。

## 2. W1 交付的可复用能力(后续模块可依赖)

- **M00**:`platform.getCapabilities`(authed)/`getPublicSnapshot`(public);企业 Feature Flag(全默认关,生产启用由 M15 控制);能力快照;错误码体系 `PLATFORM_*`/`ADMIN_*`/`MANAGED_*`;路径边界 CI `bun run enterprise:check-paths`;第 4 挂载点 `serverConfig.enterprise.enabled` 门控客户端 platform.* 请求。
- **M01**:18 张平台表 + Migration 0117;不可变 Revision + 发布事务(校验 expectedRevision→写 Revision→更新指针→写 Audit,冲突 `PLATFORM_REVISION_CONFLICT`);append-only 审计 + redaction 守卫;Job 状态机(幂等键/租约/心跳/dead letter);`platformPublisher`/`platformAudit`/`platformConfigInvalidation` 服务。
- **M02**:全局 RBAC(仅 `workspace_id IS NULL` 全局角色过 admin);`withPlatformPermission` 中间件;`enterpriseAccessGate` 挂在全局 `authedProcedure`(未准入 → `PLATFORM_ACCESS_NOT_GRANTED`);EasyAuth 同步(静态 Token → grant 快照 → 内建 RBAC,登录 hook 触发 + 降级用最近快照);bootstrap 超管 + break-glass;`.well-known/easyauth-app.json` 描述符。
- **M13 原语**(供 M07/M09/M11 消费):`PlatformSecretService`(信封加密 AES-256-GCM + 可插拔 KeyProvider；W1 时 EnvKeyProvider 就绪、VaultKeyProvider 为 stub，W8 已完成 Vault 应用接入);服务端 redaction 统一入口 `redactForLog`/`redactForAudit`;`SafeOutboundHttpClient`(SSRF:私网默认放行、云 Metadata 永久阻断、DNS pin、流级限流 + 绝对超时)。

## 3. 遗留事项与移交(按归属模块)

| 事项                        | 说明                                                                                                                                                                                                                                                                                                                                                                                                                      | 归属                        |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| EasyAuth 定时同步 worker    | 登录 hook 已闭环兜底;`platform.easyauth.sync_user` job 仅入队无消费者,需补周期 worker(用 M01 platformJobs)                                                                                                                                                                                                                                                                                                                | 后续模块 / M14              |
| EasyAuth 共享纯函数抽取     | `easyauthLoginSync`/`accessStatus` 因 path-boundary 各重实现了 token 读取/externalUserId 解析/access 映射,有漂移风险,建议抽到 `packages/const` 共享                                                                                                                                                                                                                                                                       | 后续重构                    |
| redaction benign 允许名单   | M01 redact 子串匹配会误伤 `maxTokens`/`contextWindowTokens` 等模型合法字段;发布 Model Catalog Revision 前评估 `isBenignKey` 钩子(M13 原语已预留),方向保持「宁可过度脱敏」                                                                                                                                                                                                                                                 | M07                         |
| VaultKeyProvider 真实接入   | **W1 时为 fail-closed stub，W8 已完成应用接入**：AppRole（优先）/显式低权限 token，`lookup-self` 拒绝 root，KV v2 active/history，lease/cache/single-flight 与有界流/期限；sealed、权限、网络、schema 错误 fail closed，选择 Vault 后绝不回退 Env。默认 fake 测试 `61 passed / 1 real gate skipped`，独立临时 Vault gate `1 passed`；后者不等于生产 Vault 或真实发布验收。剩余全域 re-wrap/job 与生产凭据轮换、告警、灾备 | M13/M14/M15 / 运维(W8 剩余) |
| SafeOutboundHttpClient 残余 | NAT64/SIIT IMDS 编码不解码、无端口白名单、无 content-type 校验(见 `outboundHttp/RESIDUAL.md`);消费方大下载需调高 timeoutMs                                                                                                                                                                                                                                                                                                | M09 / M13 剩余              |
| M13 剩余项                  | W8 已落地威胁模型与可执行 admin mutation registry baseline；继续补齐 registry 显式 gap 的重新认证、审计、共享限流与渗透/生产证据                                                                                                                                                                                                                                                                                          | W8(M13)                     |
| 迁移与并发回归              | PGlite 单连接为交错模拟,`TEST_SERVER_DB=1` 真 PG 竞争进回归矩阵;2.2.10 典型数据快照升级测试;审计/Job 保留期与清理策略;运行手册                                                                                                                                                                                                                                                                                            | M15 / 运维                  |
| M03 路由接线                | `src/enterprise/client/routes` 的 registry getter 待 M03 接线替换静态空数组                                                                                                                                                                                                                                                                                                                                               | M03                         |
| 测试文件路径豁免收紧        | pathBoundaries 对 `*.test.*` 全局豁免,待 M03+ 套件稳定后收紧                                                                                                                                                                                                                                                                                                                                                              | M15                         |

## 4. 上游直接修改点(累计,详见 list/07)

M00:`BusinessDesktopRoutes.tsx`、`BusinessGlobalProvider.tsx`、`lambda/index.ts`、`globalConfig/index.ts`、`types/serverConfig.ts`、`package.json`。
M01:`schemas/index.ts`、`idGenerator.ts`、`.prettierignore`。
M02:`const/rbac.ts`、`models/rbac.ts`、`rbacPermission.ts`、`lambda/index.ts`(admin)、`idGenerator.ts`、`.well-known/easyauth-app.json/route.ts`、`packages/trpc/src/lambda/index.ts`(全局 gate,高冲突风险)、`define-config.ts`(登录 hook)。

## 5. 下一步(W2,尚未启动)

按 `00_实施顺序.md`:M03 管理壳(UI agent,先对 Mock Contract)∥ M04 用户管理(服务端 agent),契约先行,联调后合并。M02 已提供真实授权,W2 管理写接口可安全接入。
